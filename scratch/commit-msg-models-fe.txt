Settings 第一间房：Models——切换是覆盖项，代价写在小字里

双厂商两列（DeepSeek 直连 / OpenRouter），选中的圆点是全页唯一的
强调色；切换存 localStorage 的 caelum-model，Chat 每次带上短名，
再点一下回到默认——没有应用按钮。缓存命中率是只细线环，14 天
用量是十四根细条（今天 accent），按型号的账右对齐。换模型会
整段重读缓存这件事是 config 里立过的规矩，它是一行小字，不是弹窗。
设计哲学：MODELS-仪表的安静-设计哲学.md。
