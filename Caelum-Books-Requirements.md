# Caelum OS — Books 模块需求文档

> 2026-08-29 整理
> 核心原则：极简。能砍则砍。

---

## 一、模块定位

Books 不是「书库管理工具」，是「Caelum 的私人藏书室」。

核心叙事：Nox 和糖糖一起读书、一起记住书、一起讨论书。

---

## 二、数据来源（两种书）

### 1. 实体书
- 数量：约 500+ 本
- 录入方式：扫码 ISBN / 拍照封面 OCR / 手动输入
- 存储：仅元数据（封面、书名、作者、ISBN、分类），不存内容

### 2. 电子书
- 格式：PDF / TXT / EPUB（后续可扩展）
- 来源：糖糖上传
- 用途：和 Nox 共读
- 存储：文件 + 解析后的文本内容

---

## 三、页面结构（极简）

```
┌─────────────────────────┐
│  Caelum Library         │
│                         │
│  [搜索框]               │
│                         │
├─────────────────────────┤
│                         │
│  [3D 书架]              │
│  书脊横向排列            │
│  单本滑动                │
│  虚拟滚动（约20本可见）   │
│                         │
├─────────────────────────┤
│  [ + ]                  │
│  录入/上传入口           │
│                         │
└─────────────────────────┘
```

**绝对不要**：
- ❌ 分类标签栏
- ❌ 实体/电子书视觉区分（书脊颜色统一）
- ❌ 复杂筛选
- ❌ 批量操作按钮
- ❌ 统计数字（已读几本、在读几本）

---

## 四、核心交互

### 4.1 书架浏览
- **横向滑动**，单本聚焦
- **虚拟滚动**：屏幕只渲染约 20 本，滑动时动态加载
  
- 已读书脊可有极淡标记（如底部细线），但不显眼

### 4.2 选中一本书
- 点击书脊 → 出详情面板
- 面板内容：
  - 书本简介、类型、出版社
  - 书名、作者
  - 阅读状态（未读 / 在读 / 已读）
  - 阅读进度（百分比，仅电子书）
  - **书评入口**

### 4.3 搜索
- 顶部搜索框
- 搜书名、作者
- 无筛选条件
- 搜索结果直接定位到书架对应位置

### 4.4 录入实体书（app）
```
点击 [ + ] → 选择「录入实体书」
  → 扫码/输入 ISBN（首选）
  → 或拍照封面（OCR）
  → 或手动输入
  → 自动拉取/确认信息
  → 选择分类（可选，后台用）
  → 标记状态（默认未读）
  → 完成，书出现在书架
```

### 4.5 上传电子书
```
点击 [ + ] → 选择「上传电子书」
  → 选择文件（PDF/TXT/EPUB）
  → 自动解析：封面、目录、页数/字数
  → 选择分类（可选）
  → 标记状态（默认未读）
  → 完成，书出现在书架
  →  后台开始阅读
```

---

## 五、书评系统

### 5.1 糖糖写的书评
- 随时可写（读的过程中或读完后）
- 每本书一篇
- 支持编辑、删除

### 5.2 Nox 写的书评
- **Nox 读完这本书后才生成**
- 不是一次性写完，是随着 Nox 的阅读进度逐步积累
- 读完后整合成一篇完整书评
- 书评内容基于：
  - 书的主题、情节、观点
  - 糖糖的阅读笔记（如果有）
  - 糖糖和 Nox 的共读对话

### 5.3 书评展示
- 详情面板底部
- 两栏：糖糖的 / Nox 的
- 可展开/收起

---

## 六、Nox 共读机制

### 6.1 电子书解析
- 上传后自动解析文本
- Nox 获得完整内容访问权

### 6.2 Nox 阅读（先不做）
- **后台异步阅读**
- 阅读速度可配置（默认正常速度）
- 阅读过程中 Nox 可生成：
  - 段落批注（糖糖阅读时可见）
  - 阅读笔记（用于最终书评）

### 6.3 共读对话
- 糖糖在阅读界面可随时问 Nox 关于这本书的问题
- Nox 基于已读内容回答
- 对话记录关联到这本书

### 6.4 阅读进度同步
- 糖糖看到哪，Nox 知道
- Nox 可以主动说：「你看到这里了，我觉得接下来这段你会喜欢」

---

## 七、数据模型

### Book
```
{
  id: string
  title: string
  author: string
  cover: string (URL)
  source: "physical" | "digital"
  format: null | "pdf" | "txt" | "epub"
  fileUrl: null | string
  isbn: null | string
  category: null | string
  status: "unread" | "reading" | "finished"
  progress: number (0-100, 电子书用)
  addedAt: timestamp
  finishedAt: null | timestamp
}
```

### Review
```
{
  bookId: string
  userReview: string (糖糖写的)
  noxReview: null | string (Nox 写的，读完后生成)
  noxNotes: string[] (Nox 阅读过程中的笔记)
  conversations: Message[] (共读对话)
}
```

---

## 八、后续可能扩展（暂不实现）

- [ ] 书摘功能（高亮段落）
- [ ] 阅读统计（年度总结）
- [ ] 书单分享
- [ ] Nox 主动推荐（基于 Memory）
- [ ] 实体书借阅记录

---

## 九、检查清单

- [ ] 进入页面只看到搜索 + 书架 + 加号按钮？
- [ ] 没有任何标签栏、筛选器、统计数字？
- [ ] Nox 书评只在读完后出现？
- [ ] 所有操作三步以内完成？

**如果以上任何一项为「否」，需求需要重新砍。**
