# CAELUM 时间模型审计（2026-09-14）

> 起因：糖糖报「日常聊天里上午说的话，下午被他说成昨天」，并指出这不是提醒逻辑的
> bug，是**缺一套时间语义**。她的原话：「系统目前有『当前时间』，却没有真正意义上的
> 『事件时间模型』。」
>
> 本文只做审计，**不改代码**。所有结论带 `文件:行号`，是现场读出来的不是凭记忆写的。
> 审计范围：`nox-core/`（Python）+ `bridge/`（JS）。

---

## 〇、一句话结论

**整个项目只有一个地方把时间当模型来建，其余全是拿裸 `datetime` 顶着。**

那个地方是 World Model（`nox-core/world_model/types.py:36-47`）——
它分开了「事情发生的时间」和「我们收到的时间」，并且拒绝无时区的值。
**这套模型没有任何其他模块沿用。** 消息、记忆、纸条、待办、开口，各写各的。

于是同一个问题会在不同地方反复长出来：
2026-08-11 治过一次「前几天的美甲他以为是今天」，2026-09-14 又以
「上午说的话他说成昨天」的形状回来了。**它们是同一件事的两次发作。**

---

## 一、八个概念 × 现状

按糖糖给的分法逐个对照：

| 概念 | 系统里有吗 | 在哪 / 缺在哪 |
|---|---|---|
| **Instant**（某个瞬间） | ✅ 有，但**八份** | `timezone(timedelta(hours=8))` 被独立定义了 8 次，见 F1 |
| **Date**（哪一天） | ⚠️ 有，**四种算法** | 「今天」散在 ~15 处，口径不统一，见 F2 |
| **Duration**（持续多久） | ✅ 有 | `timedelta` 用得还算干净（TTL / 冷却 / 窗口） |
| **Relative Time**（相对表达） | ⚠️ **两套，互不知道** | `timeline.humanize()` 和 `speaker._humanize()`，见 F6 |
| **Event Time**（事情发生时间） | 🔴 **只有 World Model 有** | `world_model/types.py:36`。消息、待办、记忆全没有，见 F4/F5 |
| **Message Time**（消息产生时间） | ✅ 有 | `data/store.py:55 created_at`，而且是消息**唯一**的时间列 |
| **Deadline**（截止） | ⚠️ 分裂 | 待办的 `due` 在 bridge，Core 看不见；Intent 只有 TTL，见 F7/F9 |
| **Recurrence**（周期） | ⚠️ 全在 bridge | `repeat/at/weekdays/times/done_log`，Core 拿不到语义，见 F7 |

---

## 二、链路（糖糖要的那张图）

```
她说一句话
   │
   │  ① message_time = created_at        data/store.py:55
   │     ⚠️ 只有这一个时间。event_time 没有地方放
   ▼
消息落库（messages 表）
   │
   │  ② 读回来时贴日期分隔线              context/timeline.py:129 with_dates()
   │     ⚠️ 2026-09-14 之前只在**换天**时插一行
   │        → 同一天内零时间信息 → 本次 bug 的直接原因
   ▼
历史进上下文
   │                                   ┌── TimeProvider【此刻】  providers/time.py:99
   │                                   │   ⚠️ 只说"现在"，不说"那句话是什么时候"
   ▼                                   │
他的上下文 ◄────────────────────────────┘
   │
   │  ③ 事件时间：**断了**
   │     没有任何地方从她的话里抽「明天」「昨天」是哪一天
   │     （`sources/times.py:65 TimeWake.parse` 解析的是**配置字符串**，
   │       不是自然语言 —— 别被名字骗了）
   ▼
┌──────────┬──────────┬──────────┬──────────┐
│  Memory  │ Attention│   Todo   │ Speaker  │
│          │          │          │          │
│ 只在正文  │ observed │ 周期语义  │ 自己再算 │
│ 里补相对  │ _at 是   │ 全在      │ 一次     │
│ 日期      │ 感知时刻 │ bridge    │ 今天/昨天│
│          │ 不是事件  │ Core 看   │          │
│ timeline │ 发生时刻  │ 不见      │ speaker  │
│ .py:223  │          │           │ .py:135  │
└──────────┴──────────┴──────────┴──────────┘
```

**断在哪：② 到 ③ 之间。** 系统把「这句话什么时候说的」传下去了，
但「这句话说的事情什么时候发生」从来没被提取过，于是每个下游都在
**让模型临场猜**，或者各自补一个局部的相对时间。

---

## 三、发现

### F1 🔴 UTC+8 这个常量被独立定义了 **8 次**

```
personality/mood.py:37            CST      = timezone(timedelta(hours=8))
attention/care/ledger.py:52       LOCAL_TZ = timezone(timedelta(hours=8))
attention/gate.py:45              LOCAL_TZ = timezone(timedelta(hours=8), "CST")
attention/scheduler.py:69         LOCAL_TZ = timezone(timedelta(hours=8), "CST")
attention/sources/daily_card.py:48 _CST    = timezone(timedelta(hours=8))
day/aggregator.py:43              LOCAL_TZ = timezone(timedelta(hours=8))
planner/daily_card.py:48          CST      = timezone(timedelta(hours=8))
tools/record.py:42                LOCAL_TZ = timezone(timedelta(hours=8))
attention/longing.py:85           （内联，连名字都没有）
scripts/migrate_menstrual.py:43   LOCAL_TZ = timezone(timedelta(hours=8))
```

三个名字（`CST` / `LOCAL_TZ` / `_CST`）指同一个东西。
眼下值一样所以看不出问题——**这正是它危险的地方**：
哪天要支持她出国、或者要把「她的一天」从 00:00 挪到 04:00（她凌晨才睡），
得改十处，漏一处不报错。

### F2 ⚠️ 「今天是哪天」有四种算法，散在约 15 处

```python
now_cst().date()                                   # api/server.py:363, providers/health.py:102
(now).astimezone(LOCAL_TZ).date()                  # attention/gate.py:118, speaker.py:151
datetime.now(LOCAL_TZ).strftime("%Y-%m-%d")        # tools/record.py:81
datetime.now().astimezone()                        # topic_pool/filter.py:103  ← 机器时区
```

前三种结果相同，第四种**依赖机器时区**。
`personality/mood.py:28` 那段注释早就写明了为什么不能这样：

> 哪天服务器重装成 UTC，时段判断就整体偏 8 小时，**而且不会报错**。

### F3 ⚠️ 无时区的 `datetime` 还在流通

- `attention/sources/curiosity.py:91` — `now = now or datetime.now()`，**naive**。
  它往下走到 `topic_pool/store.py:233 open_topics()`，而 `_iso()`（`store.py:55`）
  对 naive 值调 `.astimezone(utc)`，Python 会**按机器本地时区**解释。
  线上机器恰好是 +8，所以现在是对的 —— **这是巧合不是设计**。
- `context/cache.py:41,94` — naive，但两端都 naive 且只算时长差，可接受（该加一行注释说明）。

对照：`attention/events.py:103` 和 `world_model/types.py:46` **都立了规矩**
（naive 直接抛 ValueError）。规矩有，但只管住了这两处。

### F4 ✅🔴 event / message 的区分**只有 World Model 做对了**

```python
# world_model/types.py:36-38
observed_at: datetime            # 事情**发生**的时间
ingested_at: datetime = ...      # 我们**收到**的时间
```

而且 `:46` 强制带时区。**这正是糖糖要的那个模型，它已经存在，写得也对。**
问题是没人用它——消息、待办、记忆、纸条一个都没沿用。

`topic_pool/pool.py:79` 是第二个撞到这件事的地方，它的解法是局部再加一个字段：

> `observed_at` 记的是**进池子的时刻**，不是文章发布时间

同一个概念，第二次被独立发明。**这就是「平行实现反复长出来」。**

### F5 🔴 消息只有 message_time，没有 event_time，也没有人去抽

`data/store.py:50-58` 的 `messages` 表：`created_at` 是**唯一**的时间列。

全仓找不到任何从她的话里解析事件时间的东西。
唯一名字像的 `attention/sources/times.py:65 TimeWake.parse()`，
解析的是配置字符串 `"12:00:午饭,18:30:晚饭"`，不是自然语言。

所以糖糖举的那个例子：

```
14:00  她：「我昨天去健身了」
       message_time = 09-14 14:00   ✅ 有
       event_time   = 09-13         ❌ 没有，也没人算
```

### F6 ⚠️ 相对时间两套实现，口径不同、互不知道

| | 位置 | 入参 | 粒度 |
|---|---|---|---|
| A | `context/timeline.py:183 humanize(days)` | **天数** | 今天/昨天/前天/N天前/上周/N周前/上个月… |
| B | `attention/speaker.py:135 _humanize(when, now)` | **两个时刻** | 今天/昨天/N 天前 |

B 在 2026-09-14 之前是按**小时差**算的（`hours < 20 → 今天`），
那是**错的时间模型**不是边界 bug——「昨天」是日历概念：

```
23:59 → 00:01     过了 2 分钟，已经是「昨天」
02:00 → 23:00     过了 21 小时，仍然是「今天」
```

A 一直是对的（按天数入参），B 直到今天才对齐。**两套共存本身就是缺陷**。

### F7 ⚠️ Recurrence 和 Deadline 全在 bridge，Core 拿不到语义

`bridge/server.js:365-383` 的 `todos` 表有完整的周期模型：

```
repeat   = once | daily | weekly | weekly_count | anytime
at       = "HH:MM"
weekdays = "1,4"
due      = "YYYY-MM-DD"
times    = 每周几次（weekly_count）
done_log = "2026-08-18,2026-08-20"   算周配额用
fired_on = "YYYY-MM-DD"              今天追过没
```

**模型是够的。** 但 Core 侧的 `attention/sources/todo_due.py` 只消费
`/api/todo/due` 的结果列表——拿不到 `times` / `done_log` / 本周剩几天。

后果（今天实测）：她的臀腿训练是 `weekly_count times=3`，
本周（09-14 周一 ~ 09-20）做了 0 次。系统**周一下午就开始追**，
而且一天最多追 3 次（`todo_due.py:MAX_CHASE`）。
他说不出「本周还剩六天，不急」——**因为那个数根本没传给他**。

### F8 ⚠️ 「推迟」这个概念已经存在——在纸条上，不在待办上

```python
# attention/wakeup.py:99
def defer_past_night(when: datetime) -> datetime:
    """落在她睡觉时段的唤醒往后推到早上。
    **顺延不是取消** —— …那张纸条该在早上兑现，不该被扔掉。"""
```

纸条有 `wake_at`（`wakeup.py:122`）可以被改写、被顺延。
而待办只有 `pending / completed / fired`（bridge），
**没有 `deferred`**，也没有「重新评估时间」。

糖糖要的正是这个区分：

> 你不需要把任务标记为完成。只是把「今天继续提醒」变成
> 「今天停止追问，明天重新评估。」

而 `todo_due.py` 开头那句「她说一句『在忙』并不代表运动做了」**并不冲突** ——
它说的是不能标完成，没说不能改下次介入时间。**这两件事在代码里被混成了一件。**

### F9 ⚠️ Intent 只有 TTL，没有 Deadline

`attention/intent.py:76-77`：`created_at` + `expires_at = created_at + DEFAULT_TTL`。

这是 Duration 算出来的过期，不是「这件事该在什么时候之前完成」。
所以「她说明天要去训练」这种带截止的事，在 Intent 层没有位置放。

---

## 四、把这些摆在一起看

糖糖那句「原来之前很多地方其实都在拿一个 `datetime` 硬扛完全不同的问题」，
在这个仓库里的具体形状是：

| 一个 `datetime` 同时被当成 | 出现在 |
|---|---|
| 消息时刻 **和** 事件时刻 | 消息历史（只有 `created_at`），模型得自己猜 |
| 感知时刻 **和** 发生时刻 | `sources/metrics.py:204` `observed_at=now`——收到的那一刻当成发生的那一刻 |
| 过期时刻 **和** 截止时刻 | `intent.py:77 expires_at` |
| 绝对时刻 **和** 相对表达的锚 | `speaker._humanize` 改之前按小时差 |

---

## 五、这次已经改的（`c3218c4`，**未部署**）

改了三处，都属于「② 历史时间标记」这一层：

1. `with_dates` 三条件插标记：换天 / 跨时段 / 隔 45 分钟
2. 标记带完整日期：`（9月14日 周一 下午15点）`
3. `speaker` 的 prompt 带日期；`_humanize` 改成日历天

**在新模型下它该怎么摆**：它是 ② 的正确实现，可以留。
但糖糖提的分层表达（`【今天 09:12】` / `【昨天 23:51】` / `【2026-09-10 21:34】`）
比现在这版更省 token 也更好读 —— 区别是它**需要一个渲染期的"今天"**，
而历史是冻住的（`timeline.py:34-49` 那条约束）。

> ⚠️ 这里有个真冲突，动手前必须想清楚：
> 「今天/昨天」是**相对**表达，写进冻住的历史就会腐烂，而且每天重算一次
> 会让整段历史的缓存每天失效一次（DeepSeek 是自动前缀匹配）。
> 现在这版用绝对日期正是为了绕开它。
> 要上分层表达，得先决定**在哪一层渲染** —— 历史里存绝对、
> 渲染时再换成相对，意味着历史不能再直接当缓存前缀用，那是有成本的决定。

---

## 六、建议：Temporal Understanding 该长什么样

不实现，只给形状，等她定。

```
┌─ 时间语义层（新）────────────────────────────┐
│                                              │
│  · 一个 tz 常量（吃掉 F1 的八份）              │
│  · today(now) / same_day(a,b)（吃掉 F2）      │
│  · 一套 relative(when, now)（吃掉 F6 的两份）  │
│  · TimeRef { message_time, event_time,        │
│              precision, deadline, recurrence }│
│    ← 沿用 World Model 已经做对的那个分法       │
└──────────────┬───────────────────────────────┘
               │
   ┌───────────┼───────────┬──────────┬─────────┐
   ▼           ▼           ▼          ▼         ▼
 历史渲染    Memory     Attention    Todo    Speaker
```

**最小可行的第一刀**（我的建议，按性价比排）：

1. **收 tz 常量**（F1）——纯机械，零行为变化，但它是后面每一步的地基
2. **统一 relative**（F6）——两套并一套，把 `speaker` 的那套删掉
3. **event_time 进消息**（F5）——理解层已经在跑 LLM 推断了
   （`attention/appraisal_llm.py`），让它顺带吐一个 `event_time` 比新写一套解析器便宜
4. **Todo 加 `deferred`**（F8）——形状 `wakeup.defer_past_night` 已经有了，照抄语义

1 和 2 今天就能做，且**不改任何对外行为**。
3 和 4 是真的功能改动，要她点头。

---

## 附：审计用的检索口径

```bash
grep -rnE "datetime|created_at|updated_at|due|remind_at|今天|昨天|明天|timedelta" \
     nox-core --include=*.py | grep -v /tests/
grep -rnE "^(CST|LOCAL_TZ|_CST)\s*=|timezone\(timedelta\(hours" nox-core --include=*.py
grep -rnE "datetime\.now\(\)|utcnow\(\)|date\.today\(\)" nox-core --include=*.py
```

命中密度前五：`attention/service.py`(50) · `data/store.py`(44) ·
`orders/store.py`(28) · `day/aggregator.py`(28) · `attention/wakeup.py`(27)。
