# CAELUM 架构审计报告

> 审计人：外部 CTO / 首席架构师（敌对式审计，非项目成员视角）
> 审计日：2026-09-10
> 范围：`nox-core` / `bridge` / `Ombre-Brain` / `nox-app`（双前端）/ 卫星服务 / 部署与备份链 / 测试与可观测性
> 方法：6 个并行子 agent 实读代码 + 本人独立复核关键结论。**所有结论都带 `文件:行号`**；本人未能亲自验证的，在「未证实」里单列，不与已证实结论混排。
> 本报告不新增功能建议、不谈 UI、不谈商业化。

---

## ⚠️ 优先级修订（2026-09-10 业主实测后）

> 本节由**业主亲自实测**补充，推翻了本报告原先的第一优先级。原文保留不改，但**读优先级时以本节为准**。

### 我（审计方）的方法错误

我把「**泄露在仓库里**」和「**外部人现在就能利用**」混为一谈了。报告开头写"这是唯一一条现在、立刻、外部人就能做到的风险"，**这个前提依赖仓库是公开的 —— 而它是私有的**。前提不成立，排名就不成立。

**教训**：排优先级要按**已证实的暴露面**排，不能按"如果前提成立"排。报告里的「未证实清单」是对的，但我把未证实项的严重度当成已证实的用了。

### 四条已核实（原「未证实清单」第 1–4 条）

| # | 原问题 | 实测结果 | 影响 |
|---|---|---|---|
| 1 | bridge 有没有 `NOX_TOKEN` | ✅ **有**。不带 token 打 `/api/usage-stats`、`/api/foods` 全部 **403** | 鉴权 fail-open 那条**最坏情况不成立**（但 fail-open 形状本身仍是缺陷） |
| 2 | GitHub 仓库公开吗 | ✅ **私有**。`caelum` 与 `nox-app` 匿名查都是 404 | 「凭证泄露在 git」从 **Critical 降为 High**；从"今晚"降为"这周" |
| 3 | 云安全组开了哪些端口 | ✅ **12 个里 11 个被挡住，只有 9333 漏出来** | 「10+ 个 0.0.0.0 无鉴权服务」这个 Critical 集群**降级为纵深防御项** |
| 4 | 备份备的是真数据吗 | ✅ **是**。`/root/ombre-brain/buckets` 里 **249 个记忆桶**；`ombre-brain-data` 根本不存在 | 排期 **0.2 划掉**。⚠️ 但线上 unit 没设 `OMBRE_BUCKETS_DIR`，而**仓库里的 unit 有** —— 这是"仓库与线上漂移"的活样本，直接加强排期 **4.8（部署版本化）** 的理由 |

### 实测新发现：真正"陌生人现在就能做到"的只有一条

| 实测 | 谁能利用 | 判定 |
|---|---|---|
| 🔴 `http://43.133.211.140:9333/latest` → **HTTP 200，1214 字节，零鉴权**（`TOUCH_TOKEN` 在 unit 里没有；`touch_server.py:35` 空 token 分支直接跳过校验） | **任何陌生人，只要知道 IP** | **唯一的真开门。她的身体接触记录公网匿名可读** |
| 🟠 `/uploads/<文件名>` → HTTP 200，下载 306 KB | 需要**同时猜中毫秒时间戳 + 6 位 UUID 前缀（16.7M）** —— 文件名是 `${Date.now()}-${randomUUID().slice(0,6)}${ext}`（`server.js:563-565,1109`），实践上猜不出。真实性质是"**一旦 URL 从别处漏出就永久无法收回**"（无鉴权 + 30 天 immutable + 无吊销），**不是敞开的门** | **降级为纵深防御项，往下排** |
| 🟠 `/ombre/<hex>/recent` → 200，10 KB；`/breath-hook` → 26 KB | 需要先知道那个 hex，而 hex 只在**私有**仓库里 → 被仓库私有性挡着 | OB 四个端点无鉴权这条**仍然成立**，但严重度从 Critical 降为 **High**（取决于仓库可见性，不取决于代码） |

### 修订后的优先级

- **唯一"今晚就该做"的**：`9333` 收口（排期新增 **0.0**）。注意：**"去掉注释就完事"会打断 ESP32**，见排期 0.0 的说明。
- **降级但仍要做**：密钥轮换（尤其 **OpenRouter 那把在 master 上**，30 分钟）、git 清理、绑定收口。理由不是"现在会被利用"，而是：私有仓库的读权限会经由协作者 / 被钓鱼的 GitHub 账号 / 泄露的 PAT / CI 日志扩散；而且**这个项目历史上真的发生过 `NOX_TOKEN` 在公网裸奔**（`PROJECT.md`）—— "曾经公开过"在这个项目里不是假设。
- **`/uploads` 往下排**（见排期 0.8）。
- **fail-closed 那条照做**（`server.js:118`）：最坏情况虽然不成立，但"配置疏漏即全开且无告警"这个形状本身是错的，改动 30 分钟。

---

## 〇、判决摘要

**CAELUM 不是一个"个人玩具项目"，它是一个已经跑在生产上、装着真人隐私、能花真钱、能驱动物理设备的单点系统 —— 但它的工程质量标准仍停留在"个人项目"。这两件事的错配，就是本次审计的核心结论。**

好的部分是真的好，我先把它们说清楚，后面的批评才站得住：

| 做得好的 | 为什么好（不是客套） |
|---|---|
| **Attention Registry 的惰性衰减** (`attention/registry.py:114-124`) | `strength + last_updated` 读时现算，而不是靠定时任务扫表。理由极硬：**服务会停、会重启，定时任务停了衰减就停了**——惰性算的话停机一个月回来强度仍然正确。这个取舍比大多数生产系统做得好。 |
| **`upsert` 取 max 而非相加** (`registry.py:199-208`) | 每小时报一次的 Source 不会在几天内把强度顶到 1.0。**这是防"假紧急"的关键一笔**，很多人会写错。 |
| **Care Orchestrator 的「分母」设计** (`care/orchestrator.py:104,221-238`) | 每个念头都有下场（spoke/held/dropped/failed），且区分「他自己判断不说」（SKIP）和「规则不让说」（BLOCK）。**没有这个分母，你永远分不清"他变冷淡了"和"栏杆太紧了"。** 现实中 90% 的推荐/通知系统缺的就是这个。 |
| **`gate.py` 的时区纪律** (`gate.py:43-45`) | 服务器在东京、她在 UTC+8，安静时段用固定 offset 而非服务器本地时间，且注释写清 1991 后无夏令时。 |
| **Agent Loop 的结局分类** (`agent/loop.py:50-77,192-202`) | `answered / refused / truncated / tool_stuck / exhausted / error` 六分。**`for-else` 那一段是"打转"和"答完"的唯一区分点**——把 max_tokens 截断当成完成，是 LLM 系统最常见的静默错误，这里没犯。 |
| **工具失败原样回传** (`agent/guard.py:66-79` + `loop.py:373-375`) | 不美化、不吞、带参数回给模型，并区分「调用失败」与「查不到」。`guard.py` 开头那段"不要用一个会编的东西去检查另一个会不会编"是对的设计哲学。 |
| **瑞幸下单的结构闸门** (`api/server.py:1609-1639` + `tools/luckin.py`) | `claim()` 原子翻转状态、重跑 preview 比对指纹、失败不重试、付款链接不进任何持久化。**"一次审批绑定一份完整订单快照"是支付系统的标准做法，个人项目能做到这一步是超出预期的。** 对应测试也是真的（`test_orders.py:502-513` 双点 HTTP 只下一单）。 |
| **Context Provider 的失败降级** (`context/base.py:140-153`) | 失败退回 stale 并标注 `stale_age_s`，从未成功过才 `available:False`。**"旧值 + 我知道它旧"比"没有值"信息量大得多。** |
| **备份脚本本身** (`scripts/caelum-backup.sh`) | `.backup` 在线热备 + 逐库 `integrity_check` + 重试一次 + AES-256 + 轮转 14 份 + 连 systemd unit 和 .env 一起备份（"没有它恢复不了"）。恢复步骤写在文件末尾。**这份脚本的设计水平高于项目其他部分。** |
| **`docs/LOGGING.md` 与 `log-digest.py`** | 反例墙每条都对应真实事故；log-digest **刻意不用 LLM**（"一个和被监控对象共享故障模式的监控不是监控"），并因为第一版用 `capture_output=True` 把 VPS 干趴而改成流式 + 30 万行硬闸 + `RLIMIT_AS 256MB`（`PROJECT.md` 48.2）。这是真正的工程反思。 |
| **nox-core 测试体量与形态** | 1542 个测试用例（实测 `--collect-only`）、95 个文件、**0 个 async 测试、`unittest.mock` 只有 1 处 import** —— 依赖替身是手写 Fake 并断言调用参数/次数，不是 mock 断言。**这是这份代码里最值得保护的东西。** |

然后，以下是判决：

| 维度 | 分数 | 一句话理由 |
|---|---|---|
| **架构设计** | **5.5 / 10** | 分层意图清晰、边界法则写得很漂亮；但**边界法则实际是文档而非代码**，模块间存在真实循环依赖，关键抽象（Relationship / Understanding / Resonance）是空壳或影子。 |
| **工程质量** | **6.5 / 10** | nox-core 的代码纪律、注释质量、错误处理（裸 `except:` 全仓 0 处）**明显高于平均水平**；但部署不在版本控制、文档系统性过期、生产库迁移零测试。 |
| **可靠性** | **4 / 10** | **异地备份已连续 5 天静默失败；鉴权 fail-open；10+ 处绑定 0.0.0.0 的无鉴权服务靠"隐蔽路径"当门；无请求超时、无并发上限、无资源限制**。这不是"未来会炸"，是**已经在漏**。 |
| **扩展性** | **3.5 / 10** | 无任何数据保留/删除策略（Observation 契约写死"永不删除"、桶文件只归档不删、conversations/usage_log 永不清理），OB 每次操作全库遍历，60G 磁盘已用 38%，全部数据在一台 2C/3.6G 单机。**增长 10 倍不是"边界能不能维持"，是磁盘和内存先到顶。** |
| **AI Agent 设计** | **6 / 10** | 在"主动性收敛 + 决策记账 + 记忆按需检索"这几件事上有真正的思考，超过多数同类项目；但**能动性是假的**（见第五节），**关系演化是 0%**（见第三节），**理解层默认关着**，模型生成物会无标记地固化成"事实"。 |

**一句话：这是一个"思考得比实现得好"的系统。设计文档里的判断力（P90）远超落地纪律（P40）。最危险的不是某个 bug，而是"文档描述的架构"和"实际运行的架构"之间的距离 —— 而这个距离在持续扩大。**

---

## 一、系统边界审计

### 1.1 模块职责：意图清晰，但抽象层是空壳

| 模块 | 文档声称 | 代码实际 | 判定 |
|---|---|---|---|
| **Relationship** | 关系演化、亲密度、信任度、care_topics 权重学习 | `attention/relationship.py` 71 行 dataclass，`closeness=0.92` / `trust_level=0.85` **写死**；`avoid_topics` 永远是空 set；`RelationshipState()` 在所有调用点**新建默认实例**，无持久化、无写入路径 | 🔴 **不是模块，是常量表。产品目标里的"关系演化"当前实现度 = 0%** |
| **Understanding** | 理解她这句话意味着什么，驱动记忆召回 | `appraisal_llm.py` 存在且写得不错（锚点归一、`is_injected` 防注入）。但 `NOX_LLM_APPRAISAL` 默认 `"off"`（`appraisal_llm.py:217`），且 `MemoryProvider` 的加载条件①（活跃锚点）在转正前恒为 False | 🟡 **写好了但默认不跑**；线上实际只有规则版 appraiser 在跑 |
| **Resonance** | 六个 Drive 是"内心状态"，参与开口决策 | `service.py:372-373` tick 里只打日志；`drives()` 只喂 `/health` 和 context provider；`restlessness` 被刻意压在阈值下 | 🟡 **只读装饰层**，文档说"积累"（`CAELUM-RESONANCE-ARCHITECTURE.md:238`），积累结果不影响任何一条 push 决策 |
| **World Model** | "事实的收口"，观察/推断分层 | 契约设计确实好（Observation 只追加 + Evidence 强制来源 + State 三态）。但**写入者主要是 Attention 心跳**，且 `observations` 表**永不删除**（`world_model/store.py:8`） | 🟡 契约好，但**无保留策略 = 无界增长**（见第六节） |
| **Memory** | 唯一真源 | 职责是干净的：`nox-core/memory/` 只是客户端，OB 是唯一存储 | ✅ **这一条是真的** |
| **Attention / Care** | 主动性收敛到唯一出口 | 见第五节 —— **"唯一出口"是假的** | 🔴 |
| **Router** | 极简两条路 | `router/` 纯规则，零延迟，**刻意不做 LLM 分类**。这个取舍是对的（分类的收益 < 延迟 + 成本 + 不确定性） | ✅ |

### 1.2 循环依赖与分层倒置（实测 import 图）

我用脚本扫了 `nox-core` 全部包级 import，得到真实的依赖图：

```
agent     -> tools
tools     -> agent, attention, context, orders, personality, planner
context   -> agent, attention, memory, personality, tools
memory    -> agent, context, personality
attention -> agent, planner
personality -> agent
planner   -> agent, context, personality
router    -> agent, personality
data      -> agent, context
api       -> 全部（这是 composition root，正确）
```

**发现（全部经我本人复核到具体 import 行）：**

- 🔴 **[High] `agent` ↔ `tools` 真循环。** `agent/loop.py:23` `from tools import context as tool_context`；同时 27 个工具模块 `from agent.llm import ToolSpec`（`tools/luckin.py:16`、`tools/mcd.py:16`、`tools/computer.py:36` … 共 27 处）。
  **根因是 `ToolSpec` 这个纯数据契约住在 `agent/llm.py` 里。** 工具层要描述自己，就必须依赖"大脑的 LLM 适配器"。这是教科书级别的错位。
- 🔴 **[High] `context` ↔ `memory` 真循环。** `context/providers/memory.py:63` `from memory.ob_client import ...`；`memory/tools.py:16` `from context.timeline import relativize`。
- 🔴 **[High] 分层倒置：`context`（感知/输入）依赖 `attention`（决策/主动性）。** `context/providers/understanding.py:48` `from attention.appraisal import ANCHOR_PREFIX`。
  **输入装配层去读决策层，意味着你无法单独测试或替换任一方。**
- 🟠 **[Medium] `personality`（应当是最底层的常量）依赖 `agent`。** `personality/prompt.py:16` `from agent.guard import GUARD_INSTRUCTION`；`personality/mood.py:22` `from agent.llm import HER_EMOTIONS`。人设 prefix 是静态缓存前缀的核心，它不该依赖运行时循环。
- 🟠 **[Medium] `tools` 依赖 `attention`。** `tools/remind.py:28-29` `from attention.care.signal import ...`、`from attention.wakeup import ...`。动作层依赖主动性决策层的常量。

**为什么这在"增长 10 倍"时会致命：**
现在能跑，是因为 Python 的模块级 import 顺序恰好没踩到环（谁先被 import 谁先初始化）。但只要你想做任何一件事——把 `ToolSpec` 抽出来、把工具拆成插件、给 Attention 做独立测试、或者换掉 LLM adapter——你就会发现**没有任何一个模块能被单独触碰**。10 倍代码量下，每一次改动的影响半径都是整个 codebase。

**修复（最小、低成本、可增量）：**
1. 新建 `nox-core/contracts/`（或 `kernel/`）：`ToolSpec`、`ToolResult`、`Message`、`Usage`、`StreamEvent` 搬进去。`agent/llm.py` 只留适配器实现。**这一步就切断了 agent↔tools 环。**
2. `ANCHOR_PREFIX`、`FOLLOWUP/TASK`、`MIN/MAX_AFTER_MIN` 这类常量搬进 `contracts/` 或各自的 `*.types` 模块。切断 `context→attention`、`tools→attention`。
3. `relativize` 搬出 `context/timeline` 到 `contracts/timefmt`。切断 `context↔memory`。
4. 加一条 CI 规则：**禁止 `contracts/` 依赖任何业务包**，并用 `import-linter` 或一个 30 行的 AST 脚本把上面的方向写成契约，红了就拦。现在 `scripts/check-boundaries.sh` 只 grep 字面量，管不了这个。

### 1.3 边界法则 R1–R8：写得最好的部分，和执行得最差的部分

`CAELUM-MAP.md` 第二节的八条法则是这份代码里最有价值的资产之一。**问题是它是文档，不是代码。**

`scripts/check-boundaries.sh` 全文 54 行，实际只做 6 条 grep：

| 法则 | 哨兵实际检查什么 | 漏洞 |
|---|---|---|
| R1 主动开口唯一出口 | grep 字面量 `api/push/send` | **"任何『他为什么说话』必须能沿账本溯源"这半条完全没查。** 而前端 `Movies.jsx:282` 每 5s 轮询后直接 `streamChat()` 生成弹幕——它走 `/api/chat`，不走 `push/send`，**绕过 CareLedger 也绕过 DailyGate**（本地配额 15 次/片、间隔 150s）。**R1 的实质被绕开了，而哨兵看不见。** |
| R2 话题池不说话 | grep `push/send|speaker|core.chat` in `topic_pool/` | 新增第三条不含这些词的通道即绕过 |
| R3 不直读别人家 SQLite | grep `nox-bridge|/data/nox` 字面量 | **`bridge/server.js:3267` 直接 `new Database(HEALTH_DB, {readonly:true})` 读 health-mcp 的 health.db。** R3 只约束 nox-core，bridge 侧这个口子无人管 |
| R4 记忆只经 ob_client | grep `8002|ombre/mcp` | URL 从 env 来，绕过成本为零 |
| **R5 前端不直接推消息** | grep `api/push/send` | **不覆盖 `/api/chat`**（同上，共影就是这么绕的） |
| R6 测试会话不污染 | `tests/test_test_session_isolation.py`（10 条真测试） | ✅ **唯一被真行为测试守住的法则** |
| **R7 前端只打 bridge** | 文档自己写"哨兵 = 人工review" | **零自动化。** 且例外已从 3 条变 4 条，表格未更新 |
| **R8 花钱的动作够不着** | grep **只扫 `tools/luckin.py`** | 🔴 **`tools/mcd.py` 完全没覆盖。** `ACTION_TOOLS`（`mcd.py:39`）定义后全仓无人使用，`_SPECS = SPECS + ACTION_SPECS`（`mcd.py:219`）无条件注册，确认制只是描述里一句话（`mcd.py:155-158`）。**麦当劳能真下单，闸门是提示词。** `CAELUM-MAP.md:69` 自己承认这是欠账 |

- 🔴 **[Critical] 这个哨兵没有任何自动化入口，而且在本机根本跑不起来。** 全仓 grep `check-boundaries` 只出现在**文档**里（`CLAUDE.md:7`、`CAELUM-MAP.md:6`、`PROJECT.md`）；没有 `.github/`、没有 CI、没有 git hook（`.git/hooks` 只有 `*.sample`）。
  **"改完必跑"是纯约定。实际执行率 = 0。**
- 🟠 **[Medium] 文档自相矛盾：** `CLAUDE.md:7` 和 `PROJECT.md:6554` 说"七条边界法则"，`CAELUM-MAP.md` 的表是 R1–R8 共八条。

**修复：**
1. `check-boundaries.sh` 挂进 pre-commit + CI（这一步 10 分钟，收益最大）。
2. **R8 从"grep 一个字面量"升级成"结构声明"**：给 `ToolSpec` 加 `side_effect: none|read|write|spend|irreversible` 字段，工具注册时必填；`AgentLoop._execute` 在调用前对 `spend/irreversible` 类工具做统一拦截（没有确认令牌就拒绝）。**这一步同时把 R8 从瑞幸推广到 mcd / 淘宝 / didi / galatea，而且不再依赖 grep。**
3. R1/R5 的哨兵必须改成"检查 `Movies.jsx` 之类的客户端是否有定时 `streamChat`"——即**检查"谁在无用户输入的情况下发起对话"这个语义**，而不是检查一个字符串。

### 1.4 回答"增长 10 倍，边界还维持得住吗"

**维持不住，但原因不是边界法则失效，而是三件事同时发生：**

1. **物理上限先到**：全部数据在一台 2C/3.6G/60G（已用 38%）的单机上；OB 每次 `hold`/`breath`/`touch` 都全库遍历（`bucket_manager.py:383,623-646`）；`world.db` 的 observations 契约写死"永不删除"；`conversations`/`usage_log`/`topic_pool.topics` 都没有保留策略。**10 倍数据量下，磁盘和内存先炸，架构讨论没有机会登场。**
2. **循环依赖让重构成本指数上升**，而 10 倍代码量必然需要重构。
3. **唯一能发现问题的机制是人**（`morning-check.sh` 每天一张 260 字卡片 + 没人读的 `monitor.sh` 日志）。10 倍系统规模下，人的注意力是第一个耗尽的资源。

---

## 二、数据流审计

### 2.1 一次用户输入的完整链路（带状态变更标注）

```
[1] 她说话
     │  Caelum App (PWA) / Caelum OS (Electron：主进程本地转发到 noxtang.com)
     ▼
[2] Caddy :443 → bridge :3003   POST /api/chat
     │  · ensureApiAuth  ← ⚠️ fail-open：NOX_TOKEN 未配则全放行（server.js:118）
     │  · saveMessage(sid,"user",text)  → DURABLE (conversations)
     │  · 图片：写 uploads/ + 进 gallery（同步 fs.writeFileSync，server.js:1110）
     ▼
[3] nox-core :8100  POST /chat/stream  (FastAPI 同步路由 → 线程池, 默认 40 worker)
     │
     ├─ sessions.get(sid)  → 缓存未命中则读盘并【写缓存】(读操作带副作用, server.py:365-367)
     │
     ├─ _turn_starts(sid)  ← ★★ 在"理解消息"之前改变全局状态 ★★
     │     ├─ core.current_session_id = sid      (进程级全局可变, server.py:821)
     │     └─ attention.wakeups.cancel_for(sid)  (撤销该会话全部待唤醒纸条, server.py:826)
     │
     ├─ core.chat_stream()
     │     ├─ _see() 视觉转述（可能改 text, nox.py:551-578）
     │     ├─ _dynamic()  ← 感知装配
     │     │     ├─ classify() / classify_context()
     │     │     ├─ has_live_anchor(attention)   ← 感知层读决策层（跨层）
     │     │     ├─ context.render(names)  TTL 缓存：health 6h / todo 30min / memory 5min / music 3min
     │     │     └─ 【预算外追加】voice scene(≈400字) + session span + card_debt(≈120字)  (nox.py:600-623)
     │     ├─ AgentLoop（≤12 轮）
     │     │     ├─ llm.stream()  90s timeout × 最多 5 次尝试
     │     │     ├─ 工具执行 → 任意副作用（写文件 / 控家电 / 下单 / 公开发帖）
     │     │     │   · 工具返回文本【无长度上限】(guard.py:50-63)
     │     │     └─ 单轮工具调用数【无上限】(loop.py:324-329)
     │     └─ mood.extract + mood.update（进程内，不落盘）
     │
     ├─ sessions.put(sid, history)  → DURABLE (sessions.db, Store.sync) + 缓存（无锁 OrderedDict）
     ├─ _warn_if_claimed_without_doing（仅流式路径有, server.py:2305）
     ▼
[4] _turn_ends(sid)  ← 后置，全部是"事后"
     ├─ 后台线程：compactor 压缩（调 utility LLM，无并发去重）
     ├─ 后台线程：_appraise_async 理解层（默认 off / shadow 只记日志）
     ├─ Drive 回落、ExperienceEvent 喂 Attention → Registry 全量重写 attention.db
     └─ Archiver → OB grow（会话压 200 字 → 拆桶入库）
     ▼
[5] SSE done → bridge 记账 usage_log → 前端渲染
     ▼
[6] 下一轮的 Context 读到的是【上一轮的】理解结果
```

### 2.2 状态在哪里产生 / 修改（分类）

| 类别 | 例子 | 风险 |
|---|---|---|
| **DURABLE** | `sessions.db`（消息/标题/摘要水位）、`attention.db`（Registry 全量重写 + 5 个 source_state JSON blob）、`world.db`、`orders.db`、`topics.db`、`attention.db`、bridge 的 `nox-bridge.db`（18 张表）、GitHub 私有仓库 | 迁移零测试；无保留策略 |
| **仅内存** | `core.current_session_id`、`Sessions._cache`（无锁）、`Nox.mood`、`core.card_debt`、Attention 全部状态、`AttentionRegistry._items`（无锁）、`_integration_cache`（无锁） | 重启丢失；跨线程竞争 |
| **隐式副作用** | 工具执行（一次 tool_call 可以写文件/开 shell/下单/公开删帖）、`sessions.get()` 缓存未命中写缓存、`_appraise_async` 花钱、失败工具的参数原文进历史（`guard.py:72`） | 难以审计 |

### 2.3 "现在能跑，但未来一定会炸"——已核实清单

> 这里只列**有代码证据**的。前六条是"已经在漏"，不是"将会漏"。

1. 🔴 **[Critical] 异地备份已连续 5 天静默失败。**
   `backups/vps/pull.log` 实证：09-05 只有一次手动成功，**09-06 / 07 / 08 / 09 / 10 全部 `scp FAILED`**，错误是
   `scp: open local "D:/claude-code/backups/vps" : No such file or directory`。
   根因：`scripts/pull-vps-backup.cmd:15` 的 `"%DST%\"` —— **尾反斜杠把引号转义了**。
   无任何告警：`doctor.sh:86-95` 与 `morning-check.sh:34-47` **只检查 VPS 本机那份**，不看异地。
   **今天她全部的记忆、日记、聊天记录，异地唯一副本停在 2026-09-05。**
   （讽刺的是 `PROJECT.md` 第十一节第 4 条早就写过"Windows 引号非常容易炸"。）

2. 🔴 **[Critical] 一个通过 pydantic 校验的 payload 就能改全局状态。**
   `api/server.py:821-822` 在**任何意图分类之前**写 `core.current_session_id`；`:826` 撤销该会话全部待唤醒纸条。没有任何"这真的是她发来的吗"的门。

3. 🔴 **[Critical] 跨线程共享可变状态，零锁。**
   - `Sessions._cache`（`api/server.py:341,348-408`）—— 只在 `Store` 层有锁，缓存层没有。
   - `AttentionRegistry._items`（`attention/registry.py:162`）—— 写方是后台线程（`server.py:1133,1148`、`speaker.py:225`、`waker.py:228/258`），读方是每个请求线程（`understanding.py:132`、`resonance.py:131`）。
   - `core.current_session_id` —— 请求线程和后台 Care 线程都会碰。
   后果：偶发 `dictionary changed size during iteration`、OrderedDict 结构错乱、**主动说的话挂到别的会话上**。全是静默、不可复现的，正是这个项目最擅长掩盖的一类 bug。

4. 🔴 **[Critical] 状态写入与上下文缓存之间没有失效链路。**
   全仓生产代码**零次**调用 `registry.invalidate()` / `provider.invalidate()`（只命中定义和测试）。
   她在 App 里记了经期（`POST /api/nox/record/period`，`server.py:1453` 立刻写 world.db），但 HealthProvider 的缓存是 **6 小时**（`providers/health.py:64`）；加待办 30 分钟；音乐 3 分钟。
   **他会反复说错她刚记下的事实，而且不报错。**

5. 🔴 **[Critical] 单条消息的最坏时长/成本没有上界。**
   `timeout=90s` × `max_retries=4`（实际最多 5 次尝试）× `max_iterations=12` ≈ **90 分钟**；而 `uvicorn` 没有 `timeout_keep_alive` / `limit_concurrency`（`api/server.py:2380-2399`），FastAPI 零中间件。
   叠加"单轮工具调用数无上限" + "工具返回文本不截断"：一个半死的 provider 能让 40 个线程池 worker 全部挂在同一个请求上，**Core 停止响应，而她只看到"一直在转"**。

6. 🔴 **[Critical] 长期记忆对公网可读（唯一屏障是一条写进 git 的路径）。**
   `/breath-hook`（`server.py:329`）、`/recent`（`:395`）、`/memory/{bucket_id}`（`:446`）、`/dream-hook`（`:483`）**都不调 `_require_auth`**，而全部 `/api/*` 都调（我本人核对了这四处 + 无全局 middleware）。
   服务 bind `0.0.0.0`（`server.py:117,2234`）+ CORS `allow_origins=["*"]`（`:2226-2232`）。
   线上经 Caddy `handle_path /ombre/<48位hex>/*` 反代，**该 hex 明文提交在 tracked 的 `touch-mcp/Caddyfile:2`**，而仓库有 GitHub remote（`https://github.com/Iristt-boop/caelum.git`）。
   **凡是能看到这个仓库的人，就能读完她的全部长期记忆。**

7. 🔴 **[High] 信任边界已经破了：HA 令牌 + 家庭 WiFi 密码 + OpenRouter key 都在 git 里。**
   我实测 `git grep -l "eyJhbGciOi"` → `scratch/tmp_check_ha.sh`、`tmp_check_ha2.sh`、`tmp_fix_zone.sh`、`tmp_ha_user.sh`、`tmp_zone.sh`（**5 个 tracked 脚本，明文 HA 长期令牌**）；
   `git grep -l "sk-or-v1"` → `vps-scripts/update-bridge-env.py`（把 key 写进 systemd unit）；
   `fsr402-wifi/fsr402-wifi.ino:15-16`、`fsr402-test/fsr_wifi.py:26-27`（**家庭 WiFi SSID + 密码**）；
   `vps-scripts/ob.tar.gz` + `full-ob.tar.gz`（**tracked**）内含 `ombre-brain/config.yaml`，明文 DeepSeek + Gemini key（我解出验证过）；
   `vps-scripts/buckets-data.tar.gz`（**tracked**）内含 `buckets/` 目录 + `embeddings.db` —— **她的记忆数据本身也进了仓库**。
   `bridge/server.js:536-537` VAPID **私钥**作兜底硬编码（`PROJECT.md:359` 早已标记"待修"，至今未修）。

8. 🔴 **[High] 花钱的动作有一半没闸门。** 见 1.3 的 R8。

9. 🟠 **[High] 无界增长，全系统零保留策略。**
   `world.db.observations` 契约写死"永不删除"；OB 桶只归档不删（`bucket_manager.py:707-722`，且**无 unarchive**，全仓 grep 零命中）；`dehydration_cache.db` 只增不减（`invalidate_cache` 无调用者）；`WakeBook`/`IntentBook` 只标记不删（`wakeup.py:357`、`intent.py:236`，每次 `_persist` 全量 JSON 重写）；`topic_pool.topics` 只把 status 改成 expired、行不删；bridge 的 `conversations`/`usage_log` 永不清理。
   **10 倍数据量下，60G 磁盘（已用 38%）是第一个硬墙。**

10. 🟠 **[High] 备份源路径可能与线上不一致。**
    `scripts/caelum-backup.sh:43,53` 备 `/root/ombre-brain/buckets`，而 `Ombre-Brain/ombre-brain.service:15` 设 `OMBRE_BUCKETS_DIR=/root/ombre-brain-data`；目录缺失时脚本只 `WARN` 然后 `exit 0`（`:47`）。
    **若 unit 为准，整份每日备份都在备份一个空目录，而且从不报错。**（我无法从仓库判定哪个是真值 —— 必须上 VPS 核实。）

11. 🟠 **[High] 前端定时发起完整 agent 调用，不进任何账本。**
    `nox-app/caelum-os-ui/src/pages/Movies.jsx:302` 每 5s 轮询，`:282/:400/:615` 三处 `streamChat()`。
    一部 2 小时的电影最多 15 条主动消息，**不进 CareLedger、不吃 DailyGate、无法回答"他为什么说话"**。

---

## 三、Agent 生命周期审计

### 3.1 链路与偏差

```
输入 → 理解 → 思考 → 行动 → 记忆 → 关系变化
         ↑                            ↑
      滞后一轮                    不存在
```

- 🟠 **[High] 行动先于理解，且理解永远滞后一轮。**
  `_turn_starts`（`server.py:821-826`）在理解之前改全局状态；理解层（`_appraise_async`）在 `_turn_ends` 里跑，是**事后**从 `text + reply` 里抽取。
  于是 `MemoryProvider` 的"加载条件①：有活跃理解锚点"**永远指的是上一轮的理解**。
  后果：**她第一次提到一个新话题时，系统不会去翻记忆**（除非"指向过去"的正则命中）。这是设计选择，但文档没有把它写成一条显式的时序契约，未来一定会有人按"理解驱动召回"的字面意思去实现新功能而踩空。

- 🟠 **[Medium] `_warn_if_claimed_without_doing` 只在流式路径存在**（`server.py:2305`），非流式 `/chat`（`:2196-2248`）没有。任何走非流式的客户端会重现"他说卡发你了但没发"的 bug（这个 bug 在 `PROJECT.md` 46.3 被记录过）。

- 🟡 **[Medium] 主动消息不走 `_turn_ends`。** `attention/speaker.py:219-225` 直接 `sessions.get → core.chat → sessions.put`，于是"他主动说的那句话"进了上下文，但**不 rebase 纸条、不重置 longing、不跑任何收尾逻辑**。行为不一致且难复现。

### 3.2 记忆污染：五条已核实的链条

**这是本次审计里我认为最需要立刻处理的设计缺陷：模型生成物会无来源标记地固化成"事实"，并最终冻结进每一轮 prompt。**

| 链 | 路径 | 后果 |
|---|---|---|
| **1** LLM 重写后入库，原文不留 | `grow` → `dehydrator.digest`（`dehydrator.py:63-101`）→ `_parse_digest` 原样采用模型返回的 content（`:590-612`）→ 落库 | **她说的原话从不保存。** 下一轮 breath 把模型的重写当"记忆"喂回 |
| **2** LLM 合并覆盖正文 | `server.py:556-564` → `MERGE_PROMPT`"以新内容为准"（`dehydrator.py:109`）→ 整段替换正文 | 原始记录与模型创作**再也分不出来**，且无修订历史 |
| **3** 自我反思 → pinned → 冻结进静态前缀 | `dream()` 教模型 `hold(feel=True)`，反复出现时 `hold(pinned=True)` 升级（`server.py:1528-1533`）→ importance=10 永久桶（`:991-1007`）→ 下次启动 `_build_prefix` 无预算返回全部 pinned（`:662-674`）→ 渲染成"=== 关于你和糖糖的核心记忆 ==="（`personality/prompt.py:78-79`） | **模型自己的猜测变成它此后每个进程生命周期的自我指令。** 而且 pinned 循环**不受 token 预算约束**，pinned 越多，后面的动态记忆越全被 break 掉 |
| **3b** 更隐蔽的一条 | `split_breath` 在没有 `=== 核心准则 ===` 头时把整段归为 dynamic（`ob_client.py:73-80`），而 `core_principles` 返回 `core or dynamic`（`:226-228`） | **没有 pinned 桶时，随机浮现的动态记忆会被当成"核心准则"冻进静态前缀** |
| **4** 推断被当事实读回 | `NOX_LLM_APPRAISAL=on` 时 evidence 写成"她说：X（他理解成：Y）"（`evaluator.py:380-382`）→ `UnderstandingProvider` 每轮渲染（`understanding.py:151-157`） | 模型的推断成了"关于她的事实"，而且是 Care 开口的依据 |
| **5** 网页内容 → 长期记忆 | unTrusted 标记只管 Work Grant（`tools/untrusted.py:81-110`），**memory 工具零调用**（`memory/tools.py:205-226`），桶里没有"外部来源"字段 | 搜索引擎返回的注入文本经 `remember` 洗白后就是永久记忆 |

**这五条合起来，就是"长期运行半年后 Nox 会不会逐渐偏离最初人格"的答案：会，而且机制是明确的、可复现的。**
偏离不是通过"人格漂移"发生的，而是通过**静态前缀被模型自己的输出逐渐占满**发生的。

### 3.3 错误理解与错误关系状态的永久化

- 🔴 **错误理解可以永久化**：Registry 的 `upsert` 取 max（`registry.py:199-208`），**没有"这是错的"这条路径**。`drop()` 只在测试里被调用；`weaken()` 只在 `evaluator` 的恢复分支里自动调用。生产里没有人能主动纠正一条错误的 concern —— 它只能衰减。
- 🔴 **错误关系状态无法积累，因为关系状态根本不存在**：`RelationshipState` 无持久化、无写入路径（见 1.1）。`avoid_topics` 永远是空 set —— 也就是说，**她说"别老问我这个"这件事，系统无法记住**。文档把它列为 M5 待做，但架构上没有任何位置存放它。
- 🟠 **错误记忆可以修正，但只有模型能修**：OB 有 `trace(content=..., delete=True)`，所以错的记忆**能**改。但没有任何面向她的界面，也没有修订历史（不知道谁改的、改成什么）。
- 🟠 **不可回滚的状态变化**：
  - OB 归档**无 unarchive**（全仓 grep 零命中），而 `trace(resolved=0)` 会回一句"已重新激活，将参与浮现排序"（`server.py:1285-1289`）—— **这句话是假的**，文件还在 `archive/` 里，`list_all(include_archive=False)` 不遍历它（`bucket_manager.py:630-632`）。
  - 桶文件是 `open(path,"w")` **原地截断写**（`bucket_manager.py:186,290,304,366,416,717`），崩在中间就是记忆静默消失（`_load_bucket` 返回 None，只留一条 WARNING）。**而同一个仓库的 `import_memory.py:305-308` 用的是 tmp+`os.replace`** —— 作者知道正确模式，只是没用在记忆文件上。
  - Registry 只增不删（Intent/WakeBook/Thread/Ledger）。

### 3.4 一个真实的"死代码被静默吞掉"案例

`server.py:1348` 的 `pulse()` 遍历 `buckets`，而 `buckets` 只在 `:1371` 才被赋值 → `NameError` → 被 `:1366-1367` 的 `except: pass` 吃掉。**"沉底候选"这个功能从未运行过一次，而且没人知道。**
这不是孤例，它是 `except: pass` 这个模式必然的产物。

---

## 四、Memory 系统审计

### 4.1 职责划分：这一层的**边界是清晰的**

| 存储 | 职责 | 判定 |
|---|---|---|
| OB 桶（Markdown + YAML） | 关系记忆、带情感坐标（Russell 效价/唤醒度） | ✅ 唯一真源，职责清楚 |
| OB embeddings.db | 向量索引 | ✅ 定位正确 |
| OB dehydration_cache.db | 脱水摘要缓存 | 🟡 查询不用 `model` 列 → **换模型后永远返回旧模型的摘要**；`invalidate_cache` 无调用者；只增不减 |
| `world.db` | 客观事实（观察/推断分层） | ✅ 契约好，🔴 但无保留策略 |
| `sessions.db` | 会话历史 + 压缩摘要 | ✅ 保留原文、增量压缩水位设计正确 |
| `nox-core/attention/*` | 关心登记表 | ⚠️ 与"记忆"的边界靠文档维持，代码上无隔离 |
| `RelationshipState` | — | 🔴 空壳 |

**"Vector Memory / 结构化 Memory / Relationship Memory 职责是否清晰"的答案：前两者的边界清晰且正确；第三个（Relationship Memory）根本不存在。**

### 4.2 检索：设计合理，但有三个会随规模恶化的点

评分公式（`bucket_manager.py:514-527`）：
`total = topic×4.0 + emotion×2.0 + time×1.5 + importance×1.0`，normalized = total/8.5×100，≥50 入候选。
`topic` 用 `rapidfuzz.partial_ratio`（子串匹配，短 query 命中正文任意位置即 100）；`emotion = 1 − 欧氏距离/1.414`；`time = exp(−0.02×days)`。

- 🟠 **[High] 阈值过松（可算出）**：无情感坐标、刚更新的桶，只要 domain 字符串被子串命中，就够过 50（≈54.7）。**域字段一撞就召回**，噪声会挤掉真相关。
- 🟠 **[High] pinned 循环无预算判断**（`server.py:346-349,667-674`）：pinned 有多少就脱水追加多少，随后 `token_budget` 变负，动态记忆全被 break。
- 🟠 **[High] 向量预筛没有分数下限，却替换整个候选集**（`bucket_manager.py:480-490`）：向量一旦是垃圾（换模型/零向量/配额），top-50 就是库内任意序，精排永远只看那 50 条。`sim>0.5` 这个门槛只出现在后置通道（`server.py:811`）。**换一次 embedding 模型，召回静默崩坏且不报错。**
- 🟠 **[High] token 估算对非中文严重低估**（`utils.py:227-229`：中文×1.5 + 拉丁词×1.3 + `len×0.05`）：1 万字符 emoji/假名 ≈ 500 "token"。`dehydrator.py:258-259` 用同一估算判定"太短不压缩" → **大段非中文内容原样直通**。
- 🟠 **[High] 每次 `touch()` 触发全库遍历**：`touch → _time_ripple → list_all()`（全库 walk + 逐个 frontmatter 解析，`bucket_manager.py:383`），再加最多 5 次文件读写。**一次 6 命中的 recall = 6 次全库遍历。** 这条在交互路径上，随记忆量线性恶化。
- 🟡 **不确定性是设计的一部分**：`os.walk` 顺序 + `random.shuffle` + 40% 随机漂移 + `random.sample`。**同一个问题两次问会得到不同记忆。** 作为"像人一样忽然想起来"是合理的，但**故障排查时会要命**——建议把随机性隔离成"附加段"并标注，而不是混进主结果。

### 4.3 写入路径：三个结构性缺陷

- 🟠 **[High] 去重是建议不是约束。** `hold` 搜一次，score≥85 拒收、>75 走合并提示词（`server.py:974-987,539-572`）—— 全部只是**返回一句字符串**。换个措辞让相似度掉到 85 以下就是一个新桶；**没有唯一约束、没有内容哈希、没有事实表**。同一件事半年后会碎片化成十几个桶，每次浮现都是重复。
- 🟠 **[High] 冲突处理只有一句提示词。** 无冲突检测、无按时间仲裁、无版本。合并后旧正文不可恢复。
- 🟡 **[Medium] 删除无回收站**：`bucket_mgr.delete` 就是 `os.remove`；`/api/import/review` 的 delete **直接 `os.remove` 且不清 embeddings 行**（`server.py:2150-2153`）→ 孤儿向量永久占位。
- 🟡 **[Medium] 重要度通胀**：合并取 max、`pulse` 只警告不校准。

### 4.4 衰减/遗忘：有真代码，但不可逆、不可观测、可能从不启动

- ✅ 衰减是**真的实现了**（不是只在文档里）：指数惰性衰减，半衰期 slow 7d / normal 2d / fast 6h，地板 0.05，`prune()` 清理。设计正确。
- 🟠 **[High] 但它是"懒启动"而不是定时任务。** `decay_engine.ensure_started()` 只在 breath/hold/grow/dream 四个工具里被调用（`server.py:613,909,1097,1426`），仓库内**没有对应的 timer/cron**。进程重启后若无人调用工具，**一次衰减都不跑**；跑完立刻睡 24h。
- 🟠 **[High] 归档不可逆**：无 unarchive；归档桶永不衰减、永不浮现；`trace` 谎报已激活。
- 🟡 **[Medium] `_time_ripple` 给 ±48h 邻居无上限 +0.3**（`bucket_manager.py:415`），而打分含 `activation^0.3` → 密集时间簇的桶持续获得"免衰减"漂移。
- 🟡 `dream_select.py` 是死代码，且 `BUCKETS` 硬编码 `/root/ombre-brain/buckets`，与 systemd 的 `OMBRE_BUCKETS_DIR=/root/ombre-brain-data` 不一致 —— **连诊断工具都读错目录**。

### 4.5 并发与耐久：OB 是整套系统里最薄的一环

- 🔴 **[Critical] OB 侧 SQLite 无 WAL、无 `busy_timeout`、无重试**（我 grep 确认全仓零命中）。每次操作新建连接（`embedding_engine.py:77,102,151,162,170,217`），默认 5s 超时 → `SQLITE_BUSY` 只变一条 WARNING，**桶静默没有向量**。
  **对照：nox-core 的三个库都设了 WAL**（`attention/store.py:104`、`data/store.py:108`、`world_model/store.py:71`）。同一套系统里两套标准。
- 🔴 **[Critical] 桶文件有 6 个进程写者、零文件锁**：OB 服务、`write_memory.py:78-81`、`reclassify_api.py:107-120`（设计成服务运行时跑）、`reclassify_domains.py:185`、`migrate_to_domains.py:116`、`server.py:2151-2153` 裸 `os.remove`。**一次并发写就能让正在写的桶消失，无痕迹。**
- 🟠 **[High] `_matrix_cache` 只在**本进程**写删时失效**（`embedding_engine.py:73,158,166`）→ 别的进程补完向量，服务一直用旧矩阵，召回质量静默劣化。

---

## 五、Attention / 主动性审计

### 5.1 全部 12 条"让 Nox 先开口"的路径

| # | 路径 | 周期 | 过 Care Orchestrator？ | 吃 DailyGate(3/天)？ | 记账？ |
|---|---|---|---|---|---|
| P1 | 唤醒链 waker | 慢线 900s | ❌ **自己调 `push()`**（`waker.py:230,260`） | ❌ | 部分 |
| P2 | sleep | 900s | ✅ | ❌ `takes_quota=False`（`service.py:227`） | ✅ |
| P3 | time（午饭/晚饭/睡前） | 900s | ✅ | ✅ | ✅ |
| P4 | random 惦记 | **60s** | ✅ | ❌（`service.py:251-253`） | ✅ |
| P5 | location 出门/到家 | 60s | ✅ | ❌ | ✅ |
| P6 | todo 到点追 | 900s | ✅ | ❌ | ✅ |
| P7 | topic 池子 | 60s | ✅ | ✅（唯一一个 `takes_quota=True`） | ✅ |
| P8 | card 知识小课堂 | 60s | ✅ | ❌ | ✅ |
| P9 | 早报 | 每天 10:00 | ❌ **完全不过** | ❌ | 只记账 |
| P10 | 晨检 | 每天 08:00 | ❌ | ❌ | ❌ |
| P11 | **共影主动弹幕** | **前端每 5s** | ❌ **完全不过**（`Movies.jsx:282`） | ❌ | ❌ |
| P12 | 手动 tick | 人工 | ✅ | — | ✅ |

- 🔴 **[Critical] 所谓"唯一出口"覆盖不到 P1 和 P11。**
  `waker.py:230,260` 在 `orchestrator.py` 之外直接调 `push()`；P11 在浏览器里自己 `streamChat()`。
  `orchestrator.py` 开头的文档写着"任何一个都不许自己开口"——**对这两条不成立**。
  更值得注意的是 `_deliver` 的分派表漏了 `"wake"`（`service.py:518-531`），末尾只 `logger.warning("Care 收到不认识的来源")` —— **这不是"没搬完"，是两套 push 实现并存。**
- 🔴 **[Critical] `DAILY_QUOTA = 3` 事实上只约束 2 条源。** 12 条里 10 条不消耗额度。
  `gate.py:1-32` 的文档说"三条线都过同一个 Gate，额度是共享的"——**与 `service.py:227-272` 的 policy 表直接矛盾。**
- 🟠 **[High] `Scheduler.base_interval=3h` 全局冷却是死代码**：`note_spoke` 仍被每条路径写入 `_last_spoke_at`，但读它的 `Scheduler.tick` 只被 P2 调用；其它路径直接构造 `SchedulerDecision` 绕过。
- 🟠 **[High] 唤醒链不受安静时段约束**：`waker.run()` 里没有任何 `can_speak` 调用；`defer_past_night` 只作用于排期时刻。一张过期的纸条在重启后落进凌晨 2:00 会照发。
- 🟡 **[Medium] 三份互相矛盾的安静时段常量**：`gate.py` 1:00–9:00；`scheduler.context_fit` 2:00–8:30；`wakeup.QUIET_START/END` 2:00/8:30。`wakeup.py:79` 的注释自己都写了"和 bridge 的 1-9 点对不上"。

### 5.2 防骚扰机制：有，但**没有用户可控的开关**

- ❌ **没有全局每日上限**：`CareOrchestrator._quota_blocked` 算的是"**一小时内**有没有开过新链"（`orchestrator.py:65,192-200`）。
- ❌ **没有用户可见的 mute**：`Setting.jsx:272-290` 只有"发送测试推送"。唯一手段是改环境变量 `NOX_ATTENTION` / `NOX_ATTENTION_LIVE` / `NOX_WAKE_LIVE` 并重启服务——**而这会连带关掉睡眠关心和所有 Drive。**
- 🔴 **[Critical] 持久化失败 → 重启后重放**：`_persist()` 吞掉所有异常只记日志（`service.py:850-859`），落盘失败期间内存继续走、消息继续发；重启后 `gate.load_state` 读回**旧的** `spoken` 计数、`Scheduler` 读回旧 `last_spoke_at`。**DB 只读/写满一次，重启就是重复开口 + 额度静默归零。**
- 🔴 **[Critical] 可复现的重复发送窗口**：`book.reschedule`/`stop` 只改内存，`_persist` 在 900s 慢线末尾。推送成功 + 进程在 900s 内崩溃 → 纸条从库里读回来仍是 pending、`wake_at` 已过期 → **立即重发同一句话**。
- 🟡 **[Medium] `times.py:140-144` 先写 `fired_today` 再产事件** —— 如果被闸拦下，当天的午饭/晚饭/睡前**永久丢失**，账本上记的是一句 BLOCK 而不是"被吃掉"。

### 5.3 `Agency` 还是 `Automation`？——**Automation，但是带仪表盘的自动化**

**结论明确：当前主动性是规则/阈值驱动的自动化，不是能动性。** 证据：

1. 全部 12 条路径的触发条件都是"时间到了"或"阈值过线"：`context_fit` 是硬编码小时表、额度是 60min 常数、`GENERATE_THRESHOLD=0.55`、P4 是 `random.uniform(20*60, 90*60)`、P11 是一串 `if not … return`。
2. **唯一让模型自己决定的地方是措辞，不是决策**：`speaker` 让模型输出 `[SKIP]` 放弃（`speaker.py:239-241`）。规则决定"该开口了"，模型只能决定"这次算了"。**决策权始终在规则手里。**
3. **最像能动性的东西（Drive/Resonance）没有接进决策**：`service.py:372-373` 打完日志就没了。文档说 Drive"积累"，但积累结果不影响任何一条 push 决策。
4. **最能说明性质的是"反噪声规则的数量"**：`watching`、`defer_past_night`、`_HOLD` 正则、`SUBJECT_STREAK_MAX`、5 层 if —— **全是用规则去堵规则漏出来的洞**，这是自动化的典型形态。

**但要公平地说一句：它有一件大多数"自动化"没有的东西——决策账本。**
`orchestrator.py:240-322` 记录每个念头的下场，区分"他自己判断不说"（SKIP）和"规则不让说"（BLOCK）。
这个账本**确实被消费了**：`service.py:240-253` 的注释记录了"上线第一个完整早上，账本给出答案：13 次惦记里 6 次被一小时新链拦下，全卡在 43~59 分钟"——**然后人读了账本，改了策略。**
所以：**它是"有反馈回路的自动化"，但回路的最后一环是人，不是代码。** 这在当前规模是健康的；在 10 倍规模下，人会成为瓶颈。建议把账本变成可查询的指标（不是自动改策略，而是让"栏杆太紧"这件事自己浮出水面）。

---

## 六、AI 生成代码风险审计

> 这一节的判断依据是**代码里的实际模式**，不是"是不是 AI 写的"。

### 6.1 先说好的（避免误伤）

- ✅ **裸 `except:` 全仓 0 处**（nox-core）。
- ✅ **`print(` 在生产代码里 0 处**（只有 CLI main）。
- ✅ **所有 `urllib` 调用 8 处全部带 `timeout`**。
- ✅ **所有 `JSON.parse` 都在 try/catch 内**。
- ✅ **无 `subprocess` / `os.system` / `eval` / `exec`**（shell 能力在她电脑上，走本仓之外的网关）。
- ✅ **`except Exception` 175 处几乎全部带 `logger.exception`**，符合 `docs/LOGGING.md`。
- ✅ 已知良性静默只有 2 处，且都在 `LOGGING.md` 里点明了。

**这一节的结论是：nox-core 的错误处理卫生是合格的，bridge 不是。**

### 6.2 会导致 CPU / 内存 / 磁盘 / 雪崩的具体问题

| 严重度 | 问题 | 证据 | 触发后果 |
|---|---|---|---|
| 🔴 Critical | **异地备份 5 天静默失败** | `backups/vps/pull.log` + `pull-vps-backup.cmd:15` | 灾难恢复点倒退 5 天以上，且会一直倒退 |
| 🔴 Critical | **鉴权 fail-open** | `bridge/server.js:118` `if (!AUTH_TOKEN) return next();` + `bridge.service` 里**没有 NOX_TOKEN**、`server.js` 不读 .env | 一次配置疏漏 = 全部 `/api/*`（含真实下单、全部聊天记录、推送她手机）对公网开放，**无任何告警** |
| 🔴 Critical | **一排 0.0.0.0 无鉴权服务，唯一门禁是 git 里的路径 hex** | `ha-mcp/main.py:218`、`toy-mcp/main.py:67`、`netease-music-mcp/server.py:375`、`fsr402-touch-server/touch_server.py:21,34-37`、`Ombre-Brain/server.py:2234`；hex 在 tracked 的 `touch-mcp/Caddyfile` | 家电控制、物理设备、她的网易云、她的全部记忆，全部取决于"48 位 hex 没被人看到" |
| 🔴 Critical | **单请求最坏 90 分钟 + 无并发上限** | `config.py:153,156,433` + `api/server.py:2380-2399` | 一个半死 provider → 40 个 worker 全挂 → Core 整体不响应，无告警 |
| 🔴 Critical | **HA 长期令牌是全系统单点** | `ha-mcp/main.py:9`；`nox-core/config.py:258-260` 注释自述"共用同一把"；git 里 5 个 tracked 脚本有明文 JWT（实测长度与 nox-core/.env 那把不同 → **至少两把在役**） | 一把泄露 = 整个家。而且**轮换必须三处同步**，否则"修 ha-mcp 白名单"没有意义 |
| 🔴 Critical | **`hass_set_state` 不校验白名单** | `ha-mcp/main.py:168-176`：`domain = entity_id.split(".")[0]` → `POST /api/services/{domain}/{service}`，`DEVICES`（`:22-33`）**从未被任何代码校验** | 可达 `homeassistant.*` / `script.*` 等域（HA 侧是否配 lock/alarm/shell_command 未能验证） |
| 🟠 High | **`mcd_create_order` 无结构闸门** | `tools/mcd.py:39,219,155-158` | 提示注入或幻觉 → 真下单 + 绑她的支付方式 |
| 🟠 High | **`toy_set` 只有提示词护栏** | `tools/intimate.py:100-117`，`nox.py:152` 无条件注册 | 幻觉/注入 → 驱动物理设备，无审计 |
| 🟠 High | **`app.use("/uploads", express.static(...))` 挂在 `/api` 之外** | `bridge/server.js:1822` | **所有上传照片公开可下载**，且 `maxAge:30d, immutable` |
| 🟠 High | **上传扩展名来自客户端 → 同源存储型 XSS** | `server.js:563-565`（`.html`/`.svg` 存活）+ `:1822` 按扩展名推 Content-Type | 与 PWA 同源，`localStorage['nox-auth-token']` 可被读走 |
| 🟠 High | **主令牌接受 URL 查询参数** | `server.js:48`（前端 WS 正是这么用） | 令牌进 Caddy/journald 访问日志、浏览器历史、Referer |
| 🟠 High | **全局 CORS 通配** | `server.js:36` `app.use(cors())` | 配合 localStorage 令牌，任意站点可代为调用并读响应 |
| 🟠 High | **无请求体/结果集上限** | `server.js:37`（30MB）+ `:3391-3395`（`?limit=999999999`）+ `:3583-3599`（把整张 `usage_log` 拉进内存求和） | OOM / 事件循环卡死 |
| 🟠 High | **请求路径里的同步阻塞 IO + 无界循环** | `server.js:1104-1117` 对客户端可控 `images[]` 逐个 `fs.writeFileSync`；`:1279` 同 | 30MB body 可打满磁盘 IO、卡死事件循环 |
| 🟠 High | **无界状态累积** | `WakeBook`/`IntentBook`/`topics`/`dehydration_cache`/`observations`/`conversations`/`usage_log` 全无保留策略 | 磁盘线性增长到满 |
| 🟠 High | **定时器上的 LLM 重试循环** | `planner/daily_card.py:166-173` 失败返回 None 不写状态 + `:79-83` 每 60s 重试 → 6:00–21:00 窗口内**最坏 ≈900 次/天** | 静默烧钱，无人知道 |
| 🟠 High | **UTILITY 全线 401 跑了 30+ 小时没人发现** | `config.py:192-204`（注释自述）；症状是"表面上他好好的" | 这是"静默失败"的教科书案例，且**已经发生过** |
| 🟡 Medium | **单机 2C/3.6G 无资源限制** | `bridge/bridge.service` 全文无 `MemoryMax`/`CPUQuota`，`User=root` | 一次 OOM 拖垮整机（`PROJECT.md` 48.2 已经真实发生过一次：日志工具的 `capture_output=True` 把 4G 机器干趴，只能去云控制台硬重启） |
| 🟡 Medium | **`monitor.sh` 漏掉 `nox-core`，且告警写进没人读的文件** | `monitor.sh:23-31`（SERVICES 里没有 nox-core；含大概率已不存在的 `cloudflared-*`）；`:46-48` 只 `log()` 进 `/var/log/nox-monitor.log`；而 `log-digest.py:61-62` **只读 journalctl** | "服务 DOWN / 内存 95% / 磁盘 95%" 全部写进一个无人读取的文件 |
| 🟡 Medium | **`uncaughtException` 只打日志不退出** | `server.js:3893-3898` | 进程"活着但已死"，而唯一的恢复机制 `Restart=always` 永不触发 |

### 6.3 一个必须点名的系统性模式

**这个项目的失败模式是"静默降级"，而修复模式是"逐个补丁"，不是"消灭类别"。**

证据链：
- 2026-08-04 早报没走 `core.chat()` → 她回复时他不知道自己说过什么 → 修了。
- 2026-08-07→08-14 会话超 40 条后**所有消息 8 天没落库**（`PROJECT.md:1164`）→ 改成内容锚点 → 修了。
- 2026-08-24 测试会话污染生产 Registry（`CLAUDE.md` 记录）→ 加 `is_test_session` 前缀闸门 → 修了**这一条路径**。
- 2026-09-08 utility 401 跑了 30 小时 → 加 `log-digest.py` → 修了**这一个症状**。
- 2026-09-09 log-digest 自己 OOM 干趴 VPS → 给它加 `RLIMIT_AS 256MB` → **没有给任何其他进程加 `MemoryMax`**。
- 2026-09-10 异地备份失败 5 天 → **尚未发现**。

**每一次都修了那一个实例，没有一次修了那个类别。** 这是这个项目最贵的债：不是某个 bug，而是**没有把"静默失败"变成一个架构级约束**（例如：所有后台任务必须有 last-success 时间戳并在 `/api/health` 上暴露；所有持久化失败必须进入一个显式的、会被读的通道）。

---

## 七、部署架构审计

### 7.1 现状（实测）

```
腾讯云轻量 · 东京 · 2核 / 3.6G / 60G（已用 38%）/ 30Mbps
 Ubuntu 22.04, Python 3.10.12（nox-core 自带 venv）
 Caddy :443  ← 唯一 TLS 终结
  ├── noxtang.com            → bridge :3003（0.0.0.0）
  ├── /ombre/<hex>/*         → Ombre-Brain :8002（0.0.0.0, 无鉴权路由）
  ├── /tracker/<hex>/*       → app-tracker :8000（0.0.0.0）
  ├── /toy-mcp/<hex>/*       → toy-mcp :8003（0.0.0.0, 无鉴权, DNS-rebinding 保护关闭）
  ├── /ha-mcp/<hex>/*        → ha-mcp :8004（0.0.0.0, 无鉴权）
  ├── /touch/<hex>/*         → touch-mcp :9336（0.0.0.0）
  ├── /health/*              → health-sync :8102
  ├── /watch/<hex>/*         → co-watching :3200
  ├── /agent/<hex>/*         → nox-core :8100（127.0.0.1, HMAC ✅）
  ├── reading.noxtang.com    → co-reading :3100
  ├── ha.noxtang.com         → Home Assistant :8123（代理层无白名单/限流/fail2ban）
  ├── music.noxtang.com      → eryu :9090
  ├── netease-mcp.noxtang.com→ netease-mcp :3456（0.0.0.0, handler 无鉴权, CORS *）
  └── panel.noxtang.com      → 3x-ui :41729 + :2096
 systemd ×10+，**无一个 unit 有 MemoryMax / CPUQuota / NoNewPrivileges**，多数 User=root
 定时：nox-daily.timer 10:00 · cron 04:17 备份 · cron */5 monitor.sh · cron 晨检
```

### 7.2 应该在哪：分层与隔离建议

**核心判据：越是"能造成物理/金钱后果"的能力，越应该靠近物理世界、越不应该由云端持有凭证。**

| 层 | 放什么 | 硬约束 |
|---|---|---|
| **T0 公网边缘（唯一暴露面）** | Caddy 或 Cloudflare Tunnel。**只有 bridge 一个后端** | 除 bridge 外**任何服务都不得绑定 0.0.0.0**；随机路径**不得**作为鉴权手段 |
| **T1 应用平面（私有网络）** | nox-core、OB、各 MCP（ha/toy/touch/health/app-tracker/netease/eryu/co-reading/co-watching） | 全部绑 `127.0.0.1` 或独立 docker network；**每个服务一把自己的 token**；服务间只走 REST/MCP，不直读对方 DB |
| **T2 数据平面（最小权限 + 可恢复）** | 6 个 SQLite + OB 桶 | 专用非 root 用户；WAL + `busy_timeout`；**每个库都要有保留策略**；备份到**对象存储**（不是同机 `/root/backups`）+ **每次备份后验签 + 每月真恢复演练** |
| **T3 家庭节点（权威物理层）** | Home Assistant、ESP32/触摸、她电脑上的 local gateway | **只有家庭节点持有能控设备的凭证**；云端通过**家庭节点主动发起的 mTLS 长连接**下指令，云端不持有 HA token；HA 不暴露到公网（走 Tailscale/WireGuard 或 Tunnel + Access） |
| **T4 秘密** | 全部密钥 | `/etc/nox/<service>.env`，0600，属主是服务用户；**代码里零兜底默认值，缺失即拒绝启动**；轮换 runbook；**永不进 git** |

**成本考量**：一台 2C/3.6G 跑得动当前负载，**不建议**为了"架构好看"拆微服务——那会制造更多网络边界而收益为负。真正需要花的钱只有两笔：
1. **异地备份的目的地**（对象存储，几块钱/月）——目前异地副本是"她 Windows 上的一份文件"，而它已经 5 天没更新了。
2. **告警通道**（一条能推到手机的 webhook）——目前"告警"等于写入无人读取的日志文件。

**可维护性最关键的一条：部署必须进版本控制。**
`PROJECT.md:1213` 自己写着"**逐文件传，线上不是 git 仓库**"，并且记录了 2026-08-31 的真实事故：同一天改的两个文件，`service.py` 传上去了、`computer.py` 没有，**只看本地 git 完全看不出这个差别**。
`Ombre-Brain` 已经有了正确形态（它有自己的 git repo + 一个 `vps` remote），**把同样的模式推广到 nox-core / bridge / 各 MCP，是这份报告里性价比最高的一条修复。**

### 7.3 第二轮补充：部署脚本与主机侧（全部经复核）

> 这一批是"运维层自己就是攻击面"的证据。

| 严重度 | 发现 | 证据 |
|---|---|---|
| 🔴 Critical | **`update-caddy.py` 一跑就把记忆的门拆掉。** 它写入的 Caddyfile 是 `handle_path /ombre/* → localhost:8002`——**没有随机路径前缀、没有任何鉴权**。而 OB 的 `/recent`、`/memory/{id}` 本身无鉴权。跑一次这个脚本，她的全部长期记忆就变成 `noxtang.com/ombre/*` 全网可读 | `vps-scripts/update-caddy.py:12-22`（我逐行读过） |
| 🔴 Critical | **在用的 OpenRouter key 就在 `origin/master` 上，且与本机 `nox-core/.env` 里的 key 逐字节相同。** `git merge-base --is-ancestor 5144121 origin/master` 退出码 0，`git show origin/master:vps-scripts/update-bridge-env.py` 里 `Environment=API_KEY=sk-or-v1-…` 明明白白 | `vps-scripts/update-bridge-env.py:46` |
| 🟠 High | **历史提交里有旧 VPS 的 root 口令**（16 字符真实口令，11 个脚本 + 1 个文档），且该提交在当前 master 可达。后续提交改成读环境变量，但提交信息自己写着"如果那个 root 密码还有效，要改掉" | commit `5144121` |
| 🟠 High | **真实密钥进了 PowerShell 命令历史**：`ConsoleHost_history.txt` 里有 `ssh root@… "…ZHIPU_API_KEY=eyJ… >> /root/nox-core/.env"`——智谱 key 明文，本机任一可读 profile 的进程都能拿 | `%APPDATA%\…\PSReadLine\ConsoleHost_history.txt:185,189` |
| 🟠 High | **12 个运维脚本指向早已退役的旧 IP（`47.93.219.252` / `47.84.92.71`）**，且用 `paramiko.AutoAddPolicy()`（**不校验主机密钥**）+ 环境变量里的 root 口令。旧 IP 若已被云厂商重新分配，跑一次就是**把 root 口令交给陌生人**；AutoAddPolicy 则让它同时是一次 MITM 靶子 | `migrate-ob.py`、`fix-ob.py`、`fix-assets.py`、`find-buckets.py`、`extract-all.py`、`check-tunnels.py`、`check-domain.py`、`check-vps-files.py`、`bridge-logs.py`、`deploy-monitor.py`、`update-caddy.py`、`update-bridge-env.py` |
| 🟠 High | **两个脚本关闭 TLS 校验，其中一个明文 HTTP 传记忆桶**：`check-frontend.py:3` `verify_mode=CERT_NONE`；`migrate-buckets.py:7-11` 同前 + `http://47.84.92.71:8002/mcp` 明文 POST | 同上 |
| 🟠 High | **主 token 从 644 的 unit 文件里被 grep 出来塞进 curl argv**：`TOKEN=$(grep -oP 'Environment="?NOX_TOKEN=\K[^"]+' /etc/systemd/system/bridge.service)`，然后 `curl -H "X-Nox-Token: $TOKEN"`。同时 `monitor.sh:63-64` 把 `ps aux`（含完整命令行）写进 `/var/log/nox-monitor.log`。**而仓库里没有任何脚本给 unit 设过权限**（`chmod` 全仓只命中备份包的 `:82`） | `scripts/morning-check.sh:200,205-207`；`vps-scripts/monitor.sh:63-64` |
| 🟠 High | **`nox-app/frontend/public/toy.html:102` 硬编码玩具钥匙**（`const TOKEN = "…"`），该文件会被构建进 `dist/` 公开提供；bridge 侧 `server.js:113` 接受它走 `/toy/*`，能力是远程改设备状态 | `nox-app/frontend/public/toy.html:102,186,204` |
| 🟠 High | **所有 unit 无资源上限，而机器 4GB 无 swap、且已经真实 OOM 过一次**（内核杀掉 `anon-rss:370MB` 的进程，journald watchdog 崩溃，整机 SSH/HTTP 不通，只能去云控制台硬重启）。对 6 个 `.service` grep `MemoryMax/MemoryHigh/CPUQuota/TasksMax/OOMScoreAdjust` 零命中 | `PROJECT.md` 48.2；`vps-scripts/log-digest.py:105-109,148-160` |
| 🟠 High | **服务普遍以 root 运行，部分 unit 连 `User=` 都没有**（systemd 缺省即 root）。全部无 `ProtectSystem` / `PrivateTmp` / `NoNewPrivileges`。**任一 HTTP 服务的 RCE = 整机 root**，可直接读 bridge unit 里的主 token、`/root/nox-core/.env`（HA token + 付费 key）、`/root/.backup-pass` | `bridge.service:7`、`touch-mcp.service:7`、`app-tracker.service:6`、`ombre-brain.service:7`；`co-watching.service:5-8` 与 `fsr402-touch-server/touch-server.service:5-8` **无 `User=`** |
| 🟠 High | **Windows 开发机是信任根，但 ACL 是"本机用户可读、Authenticated Users 可改"**（实测 `BUILTIN\Users Allow ReadAndExecute`、`NT AUTHORITY\Authenticated Users Allow Modify`）。这棵树里并列着 `nox-core/.env`（OpenRouter key + HA token）、`.mcp.json`、`backups/BACKUP-PASS.txt`、`backups/vps/*.enc`、`secrets/*`，**以及免密 SSH 私钥**。本机任一账户或沙箱进程 = 全屋控制 + 全部付费额度 + 备份钥匙 + VPS root 通道 | `Get-Acl` 实测（本报告审计过程） |
| 🟡 Medium | **崩溃循环无退避，`monitor.sh` 每 5 分钟补刀**：`monitor.sh:100-113` 无 backoff 无告警；`RestartSec=3~5` 且未设 `StartLimitBurst`（systemd 默认 5 次/10s，5s 间隔永远触发不到限制）→ 坏服务以 5 秒节奏无限重启（**含 caddy，反复重启 = 全网 502**） | `monitor.sh:100-113`、`deploy-monitor.py:55-56` |
| 🟡 Medium | **日志无轮转边界**：全仓 grep `logrotate` / `SystemMaxUse` / `OnCalendar` 零命中；唯一自轮转是 `monitor.sh:36-44` 针对自己的 5000 行文件。journald 承接 8 个服务的 stdout 可无限增长撑满盘 | — |
| 🟡 Medium | **备份口令权限只在文档里"声称"，代码从不设置**：`caelum-backup.sh:10,14` 只检查存在、不 `chmod`（`:82` 只给加密包）；手工创建用默认 umask 就是 644 | `LOCAL-SECRETS.md:20` vs `caelum-backup.sh` |
| 🟡 Medium | **备份前不检查剩余空间、失败留半截文件**：直接 `tar \| openssl` 写盘，失败留下的半截 `.enc` 不会被清（只有 latest 软链在成功后才更新）；SQLite 热备只重试 2×3 秒；失败仅 `exit 2` 无告警；备份含无保留策略的 `/root/data/uploads` | `caelum-backup.sh:23-34,80-83,88-92` |
| 🟡 Medium | **`monitor.sh:66` 在内存压力下 `drop_caches`** —— 在 4GB 无 swap 的机器上主动丢 page cache 会造成读写风暴，反而推高 OOM 概率 | `monitor.sh:66` |
| 🟢 Low | `update-bridge-env.py` 本身是坏的（`os` 在 `:6` 使用、`:15` 才 import）→ **运行即 `NameError`，说明这批脚本从未被验证过** | `update-bridge-env.py:6,15` |

**绑回环的更正与补全（第二轮实测）：**
`app-tracker:8000`（`main.py:178`）、`touch-mcp:9336`（`server.py:144`）、`co-watching:3200`（`app.py:977`）、`co-reading:3100`（`server-sse.js:10`）也全部绑 `0.0.0.0`。
**全仓只有 `nox-core:8100`（`config.py:436`）和 `health-mcp:8101/8102`（`health_mcp.py:31` 注释写着"绑 0.0.0.0 就等于把糖糖的健康数据摆在公网上"）绑了回环。**
→ **"绑回环"是本仓库已有的正确做法，只是 10 个服务没照做。** 这不是"不知道怎么做"，是"没有统一执行"。

### 7.4 必须立刻处理的部署问题（按优先级）

1. **吊销并重建全部已泄露凭证**：OpenRouter key（**在 master 上且与在用 key 同值 —— 第一优先级**）、HA 长期令牌（至少 2 把）、家庭 WiFi 密码、DeepSeek / Gemini / 智谱 / Notion key（tracked 的 `ob.tar.gz`、`full-ob.tar.gz`、`config-tmp.yaml`）、VAPID 密钥对、旧 VPS root 口令。然后 `git filter-repo` 清历史，**并把 `vps-scripts/*.tar.gz` 从仓库移除**（`buckets-data.tar.gz` 里是她的记忆）。
1b. **删掉 `vps-scripts/update-caddy.py`**（跑一次就把记忆库的门拆掉），并归档其余 11 个指向退役旧 IP 的 paramiko 脚本。
1c. **本机（Windows）当作已失守处理**：清 PowerShell 历史、凭据进 DPAPI、收窄 ACL、给免密 SSH 私钥加口令。
2. 🔴 **从 git 里撤下"门禁路径"**：`touch-mcp/Caddyfile`（tracked）含 `/ombre/`、`/tracker/`、`/toy-mcp/`、`/ha-mcp/`、`/touch/` 的真实 hex。
3. 🔴 **把所有 0.0.0.0 收回 127.0.0.1**（bridge 也要：`server.listen(PORT, "127.0.0.1")`），给每个服务加 token，`AUTH_TOKEN` 缺失时 `process.exit(1)`。
4. 🔴 **修异地备份**：`"%DST%\"` → `"%DST%/%NAME%"`；把"异地副本新鲜度"纳入告警；**跑一次真恢复演练**。
5. 🔴 **核实 `/root/ombre-brain/buckets` vs OMBRE_BUCKETS_DIR=/root/ombre-brain-data**，并让备份脚本从 unit 读路径、缺目录 `exit 1`。
6. 🟠 **所有 unit 加 `MemoryMax` + 专用用户 + `NoNewPrivileges` + `ProtectSystem`**。
7. 🟠 **`monitor.sh` 的 SERVICES 对齐 `doctor.sh`（含 nox-core），并把告警走 journald 或 webhook**。
8. 🟠 **删除 `uncaughtException` 的"只打日志不退出"**，改成记完 `process.exit(1)` + systemd watchdog。

---

## 八、测试体系审计

### 8.1 覆盖地图（实测）

| 子系统 | 有测试？ | 真断言？ | 最大盲区 |
|---|---|---|---|
| Router | ✅ 10 直接 | ✅ 真（断言 Intent 枚举） | 只有手写词表，无 property/fuzz；SMALL_TALK 误判的信息损失无测试 |
| Context providers | ✅ 12 文件 | ✅ 真（装配/预算/缓存） | **`providers/location.py` 零测试**；无"这轮哪些 Provider 命中"的任何记录 |
| Agent loop | ✅ 7 文件 | ✅ **全仓最强** | loop 全文只有 1 行 logger；无并发/中断/断流测试 |
| Tools | ✅ 20+ 文件 | ⚠️ 混杂：computer/room/search 真；watching/eryu/cn_mcp 是字符串断言 | `tools/mcd.py` 只有"描述里有『确认』"；`test_eryu_params` 的期望值抄自**仓外文件** |
| Attention / Care | ✅ 13 文件 ~180 例 | ✅ **真** | "为什么不说"的决策行是 DEBUG 级、线上被吞；CareLedger 落盘无断言 |
| **Memory client (`ob_client.py`)** | ❌ **零** | — | **R4 指定的唯一咽喉，13.5KB，breath/hold/trace 解析与重试全未测** |
| World model | ✅ 9+20 | ✅ 真 | 只有 9 个测试守着"事实冻结只追加"契约 |
| Orders / 闸门 | ✅ 29 例 | ✅ **真（含真 HTTP）** | **只覆盖瑞幸**；麦当劳是提示词确认制 |
| Bridge API | ✅ 90 例 / 8 文件 | ⚠️ 5/8 真 HTTP+真 SQLite；3/8 是源码文本正则 | **~20 处内联 DB 迁移零测试**；`tickets.test.js` 隐式真外网且无超时；`/api/health` 零覆盖 |
| OB scoring/decay | ✅ 27+8 | ⚠️ 时间曲线真；**复合分数只有不等式** | `server.py`（2236 行，全部 MCP 工具）零测试；`run_decay_cycle`/`archive` 零测试 |
| **运维脚本** | ❌ **零** | — | `check-boundaries` / `doctor` / `morning-check` / `caelum-backup` / `monitor` / `log-digest` 全部无测试，且**都不在任何 CI 里** |

### 8.2 已经找到的假绿（比"没测试"更危险）

- 🔴 `test_daily_card.py:82-83`：`assert parse_card(...) is not None or True` —— **`or True` 让它永远通过**。
- 🔴 `Ombre-Brain/tests/test_scoring.py:259`：`assert any("编程" in d for d in domains) or True`。
- 🔴 `test_cn_mcp_tools.py:225`：`assert "确认" in s.description` —— **这正是 `CAELUM-MAP.md:65-68` 自己批判过的那种自证测试，至今还在。**
- 🟠 `test_feel_flow.py:214`：整个测试函数体只有 `pass`。
- 🟠 `test_feel_flow.py:147-177`：测试自己调 `bm.update(...)` 再断言 `is True` —— 被测的 `server.py hold` 逻辑从未被调用。
- 🟠 **6 条零断言测试**：`test_computer_tools.py:331-337`（注释自认"没抛出来就算过"）、`test_dejection.py:225-229`、`test_local_link.py:346-349` 与 `:361-368`、`test_media_title.py:90-92`、`test_attention_scheduler_streak.py:200-206`。
- 🟠 **5 处"手抄生产逻辑、测试打在副本上"**，其中两处**已与生产实质分歧**：
  `test_state_cares_split.py:47-51`（把 `not in` 改成 `in` 后 8 条测试仍全绿）；
  **`test_model_switch.py:80-97`**（抄 `nox.py:509-549` 的 `adapter_for`，缓存键用全名 vs 生产用短名，还漏了 `if not cfg.usable: return None`）。
  这就是 `PROJECT.md:7279` 自己总结的"**测试覆盖的是没在跑的那条路**"的同一个病，只是换了个位置。
- 🟠 **时间炸弹**：`test_archiver.py:112` `assert "今天是 2026-" in ...` + 真实未冻结时钟 → **2027-01-01 必挂**；`test_split.py:160-161` 两次独立读钟后截到分钟比较 → 跨分钟假失败。
- 🔴 **测试会在仓库工作目录里创建真实数据库**：`api/server.py:730` `OrderStore(Path(core.cfg.db_path).parent / "orders.db")`（同模式还有 attention/world/topics 三个）。`test_api.py:50` 把 `db_path` 设成 `":memory:"`，但 `Path(":memory:").parent == Path(".")` —— **只救了 sessions.db，另外四个落在 CWD**。磁盘实证：`nox-core/orders.db`（24576 B, 09-06）、`nox-core/topics.db`（32768 B, 08-31）确实存在，且被 `**/*.db` 忽略，**不会被 review 也不会被 git clean**。
  **这意味着测试不是幂等的，且带跨运行状态。** 受影响约 45 条测试。这跟 08-24 的 Registry 污染是同一类事故。

### 8.3 最大盲区（排序）

1. 🔴 **进程之外没有任何东西能在无人盯着时发现"他坏了"。**
   无 CI（`.github/` 不存在、无 `.gitlab-ci.yml`、无 git hook）；`check-boundaries.sh` **只被文档提及、在本机连 bash 都没有**；`monitor.sh` 漏掉 nox-core 且告警写进无人读的文件；Ombre-Brain 的 requirements 连 pytest 都不含。
   **"测试全绿"和"系统健康"之间没有因果链。** 唯一自动发现机制是每天早上那张 260 字的卡片。
2. 🔴 **"他为什么说话/不说话"在日志里不存在。**
   `agent/loop.py` 全文 **1 个** logger 调用；`attention/engine.py:93` 的"忽略 event + reason"是 DEBUG 而根级写死 INFO，**全仓无级别开关**；**没有任何 logger 记录 prompt 组成或召回内容**；跨进程**零关联 id**（`server.js:1096` 的 requestId 不出 bridge）。
   发生"他忽然不再想起她"（`LOGGING.md:12` 记录过的真实事故）时，唯一可查的是一个 300 条上限的 blob。
3. 🔴 **DB 迁移 / 并发 / 半写：全链路零测试。**
   nox-core/tests 对 `migrat|ALTER TABLE|schema_version|dbTry` **零命中**；bridge 有约 20 处内联 `dbTry(ALTER TABLE)` 而测试零命中；OB 桶是原地覆盖写。**备份是唯一兜底，而备份本身 5 天没成功。**
4. 🔴 **记忆咽喉与记忆数学没有行为测试。**
   `ob_client.py` 在 pytest 里零引用；OB 复合 decay 分数**没有任何硬编码期望值**，只有不等式；`server.py` 零测试。
5. 🔴 **"确认制"和"边界法则"主要靠字符串 grep 和人的记性。**
   R1–R5 全是字面量 grep，R7 文档写"人工review"，R8 脚本只扫 luckin，而整个脚本没有自动化入口。

### 8.4 CAELUM 必须拥有的测试金字塔

**Unit（已有 1383 个，保持；补 5 个硬缺口）**
1. `memory/ob_client.py`：breath 解析（正常/截断/空/超长）、hold 幂等、trace 无副作用、MCP 超时与重试、畸形 JSON —— **假 transport，不打网**。
2. `attention/engine.py` 的忽略分支：把 `:93` 那行 debug 提成 info（或加 `NOX_LOG_LEVEL`），断言"被忽略时 reason 可读"。
3. `agent/loop.py`：加结构化 turn 日志 + 断言含 `outcome/iterations/tools/model/tokens`。
4. OB `decay_engine.calculate_score`：补**手算硬编码期望值**（新桶 / 3 天边界 / 30 天），替掉 `test_scoring.py:167-173` 和 `:299-300` 两处同义反复；删掉 `or True` 和无断言测试。
5. 删掉"描述里有『确认』"这类断言，换成结构断言。
6. **修掉测试污染 CWD 的问题**：`create_app` 的四个路径改从显式注入的 `data_dir` 取（测试传 `tmp_path`），并删掉误建的 `nox-core/orders.db`、`topics.db`。

**Integration（现在几乎为零，补 4 类）**
1. **迁移**：把 20 处 `dbTry(ALTER TABLE)` 变成可测的 migration 列表；"旧 schema 库 → 跑迁移 → `integrity_check` + 旧行可读"。
2. **并发/半写**：两个连接同时写 attention.db / embeddings.db；桶文件写入中途 kill，断言不产生截断文件。
3. **崩溃 mid-turn**：`_persist()` 之后立刻重开库，断言 CareLedger / registry / gate / threads 五样齐全且一致。
4. **恢复演练**：拿 `caelum-backup.sh` 的产物做 decrypt → restore → `integrity_check` → 行数比对的**自动化**测试。

**Contract（补 3 条，防跨仓漂移）**
1. bridge ↔ nox-core 的接口契约（`/chat/stream` 事件形状、`/api/push/send` 请求体、`/api/nox/orders/{id}/confirm` 状态机）用 fixture 固化，**取代"读对方源码字符串、文件不在就 skip"那种做法**。
2. `tools/eryu.py` ↔ `/root/eryu/server/eryu.py` 的参数契约（现在期望值是抄来的魔法数）。
3. **工具表契约**：把 `test_tool_names_unique.py` 的模块清单改成自动发现（现在只扫 10 个模块，漏了 computer/room/notion/reading/tracker/search/galatea），扩成"每个工具要么只读、要么在确认端点后面"的机器可验清单。

**E2E（补 3 条，唯一能抓"单测全绿、串起来断在最后一步"的层）**
1. 照 `test_orders.py:568` 的模式（它抓到过 `luckin_order_flow` 未导入的 NameError），给麦当劳、给"共影不发声"、给"早报断了"各补一条真端点测试。
2. 一条"她说话 → 记忆召回 → 组装 prompt → 模型 → 工具 → 落库"的全链路。
3. `/api/health` 的 7 项探测：至少断言 degraded 时的输出形状。

**Chaos（现在为零，补 2 条）**
1. 依赖逐个不可用：OB 超时、bridge 502、MCP 无响应、LLM 429 —— 断言降级到有意义的值而不是静默（**`LOGGING.md` 反例墙里的每一条都该变成一个用例**）。
2. 时间/SQLite 压力：跨午夜、时钟回拨、时区漂移、WAL 增长。

---

## 九、CAELUM Technical Debt Map

> 只列真正危险的。**Critical = 现在就在漏，或一次配置/一次点击就能造成不可逆后果。**

| 问题 | 严重程度 | 影响 | 建议 |
|---|---|---|---|
| 异地备份连续 5 天 scp 失败、无告警（`pull-vps-backup.cmd:15` 反斜杠转义 bug） | **Critical** | 灾难恢复点倒退 5 天并持续倒退；她全部记忆/日记/聊天只有 VPS 一份 | 改 `"%DST%/%NAME%"`；异地新鲜度纳入告警；**跑一次真恢复演练** |
| HA 长期令牌 / 家庭 WiFi 密码 / OpenRouter key / Gemini key 明文进 git（5 个 tracked 脚本 + 2 个 tracked tar + tracked .ino/.py） | **Critical** | 一把泄露 = 整个家 + 账单；且已 push 到 GitHub remote | 全部吊销重建；从仓库移除 tar；`git filter-repo` 清历史 |
| 她的记忆数据本身在 git 里（`vps-scripts/buckets-data.tar.gz` 含 buckets/ + embeddings.db） | **Critical** | 隐私彻底失效 | 同上；并把 secrets 与数据目录纳入 `.gitignore` 白名单审查 |
| OB 四个读端点无鉴权 + bind 0.0.0.0 + CORS `*`，唯一门禁是 tracked Caddyfile 里的路径 hex | **Critical** | **全部长期记忆可被任意人读走** | 加 token；CORS 白名单；路径 hex 不再承担鉴权职责 |
| bridge 鉴权 fail-open（`server.js:118`）且交付 unit 里没有 `NOX_TOKEN` | **Critical** | 一次配置疏漏 = 全系统（含真实下单）对公网开放，无告警 | 缺失即 `process.exit(1)`；`EnvironmentFile=/etc/nox/bridge.env` |
| 在用的 OpenRouter key 就在 `origin/master` 上、且与本机 `.env` 逐字节相同（`vps-scripts/update-bridge-env.py:46`） | **Critical** | 任何能读该仓库的人可直接烧她的额度 | 立刻吊销重发；`git filter-repo` 清历史 |
| 5 个公网 MCP 的应用层零鉴权，唯一门禁是 tracked `touch-mcp/Caddyfile:2,5,8,11,14` 的 hex；`PROJECT.md:5122` 还完整贴出 touch URL | **Critical** | 能读仓库 = 能读她的触摸记录/App 使用/家庭设备，并驱动设备 | 五个前缀全轮换；从 tracked 文件移除；五个服务各加 token 头校验 |
| `toy-mcp` 无鉴权 + 公网 + **关闭 DNS-rebinding 防护**，可驱动贴身设备（`main.py:16,67-69,39-51`） | **Critical** | 任何拿到 hex 的人可远程调设备强度/模式，无"她当下同意"的回执 | 公网入口删除，或 HMAC 签名 + 只允许 Core 带票调用 |
| `fsr402 touch-server` 鉴权被注释掉（`touch-server.service:13`）→ `auth -> OFF`，公网 IP 直连，`/latest` 无鉴权回最后 10 条触摸记录，JSONL 无轮转 | **Critical** | **她的身体接触日志公开可读可写** | 启用 `TOUCH_TOKEN`（或防火墙只放行 ESP32）；JSONL 按日轮转 |
| `netease-music-mcp` 零鉴权 + 独立公网域名 + CORS `*`，持有她的网易云 Cookie（建/改歌单、点赞、点播） | **Critical** | 任何人可操作她的音乐账号并控制房间播放 | 今天就把站点块删掉或加 basic auth |
| `co-reading` 鉴权 fail-open（`server-sse.js:51` `if (!authToken) return true`）+ Caddy 里唯一没有 path token 的卫星 | **Critical** | 若 VPS 未设 `MCP_AUTH_TOKEN`，她的书籍与私密批注公开 | 设 token；Caddy 层加 path token |
| `co-watching` 服务端零鉴权，而其 token **已被编进发布产物**（`.env.local:12` 的 `VITE_WATCH_URL` 被内联，实测命中 `dist/assets/index-Cvw-VnjU.js`） | **Critical** | 拿到 Electron 安装包的人即获得全部权限 | 改短时效票（同 scribe-token 模式）+ 轮换 hex |
| 语音重连风暴：600ms 无退避无限重连 × 每次重新签发 scribe-token × bridge 侧无限流（`engine.js:320-325,266-277`；`bridge/server.js:1589-1608`） | **Critical** | 上游秒关时 ≈1.6 次/秒 无限签发 ElevenLabs 实时会话，按音频秒计费 | 指数退避 + 5 次上限 + `switchToBatchStt`；bridge 侧按小时配额 |
| 语音 `/chat` 与 `/tts` 无超时，网络半开时通话永久卡死且失败静默（`engine.js:783-794,463-475,909-918`） | **High** | 她只能挂断重打，且界面零反馈 | 加 `AbortSignal.timeout`；失败必须 emit 到界面 |
| 手机端乐观消息按【响应头】标 `sent`，60s 轮询整数组覆盖（`Chat.jsx:1433,1322`）；无 `AbortController`，切页后模型继续生成烧钱 | **High** | 她的消息显示已发送但服务端没有；tokens 白烧 | 按服务端确认对账；轮询改按 id 合并；加 unmount 守卫 |
| 双前端重叠功能约 95% 重复实现（Chat 2166 vs 322、Diary 815 vs 664、Falling.jsx MD5 相同），**手机端零测试** | **High** | bridge 改一次 SSE 分帧，两端以不同方式静默坏掉 | SSE 解析 + 消息对账提进 `shared/`（一处实现一份测试） |
| `bridge → core → co-reading → core` 且 `core ↔ bridge` 互相调用（`co-reading/src/nox-client.js:107` 直连 core 绕过 bridge） | **High** | 任一侧改合约两边同时坏，无单向依赖可隔离 | 明确"core 只出不进"或把桥接层写进边界法则 |
| R5 边界检查 grep `nox-app/frontend/src`，而 root 仓库 `.gitignore` 忽略整个 `nox-app/`（实测 `git ls-files nox-app` = 0） | **Medium** | **在干净 clone 上这条检查永远绿**，什么也没检查 | 要么把 nox-app 纳入，要么改成跨仓检查 |
| 边界法则完全不覆盖卫星层（touch-mcp / co-watching / app-tracker / toy-mcp / ha-mcp / netease / eryu 在规则里一次都没出现） | **Medium** | 卫星层的越界无人拦 | 扩规则 + 卫星各自加 token 检查 |
| `touch-mcp` 与 `fsr402` 共享 JSONL 文件耦合，解析失败 `except: continue` 全吞（`server.py:48-49`） | **Medium** | 上游改字段格式 → 静默返回空 | 加 schema 版本 + 不吞异常 |
| 死重 2GB+（stackchan firmware 1.1GB、worktrees 183MB、ob-tools/audio 177MB、backups 151MB、archive/backend 27.6MB） | **Medium** | 备份体积、clone 时间、检索噪声 | 见 §11.6 清单 |
| `update-caddy.py` 一跑就用"无前缀无鉴权"的 Caddyfile 覆盖线上，直接拆掉记忆库唯一屏障（`vps-scripts/update-caddy.py:12-22`） | **Critical** | 执行一次 = 全部长期记忆变成 `noxtang.com/ombre/*` 全网可读 | **删掉这个脚本**（它同时指向旧 IP） |
| 历史提交 `5144121` 含旧 VPS root 口令，且在当前 master 可达 | **High** | 旧机若口令未改，clone 历史者即拿到 root | 无条件轮换两台旧机口令；清历史 |
| 真实密钥进 PowerShell 命令历史（智谱 key，`ConsoleHost_history.txt:185`） | **High** | 本机任一可读 profile 的进程可拿 | 清历史文件；改用凭据管理器 |
| 12 个运维脚本指向退役旧 IP + `AutoAddPolicy()`（不校验主机密钥）+ root 口令走环境变量 | **High** | 旧 IP 被重分配 = 把 root 口令交给陌生人；AutoAddPolicy = MITM | 全部删除/归档；SSH 只走密钥；禁 root 密码登录 |
| `toy.html:102` 硬编码玩具钥匙并随 dist 公开 | **High** | 任何人可远程改设备状态（能力已被 bridge 的 TOY_TOKEN 限住，但钥匙是公开的） | 改为运行时向后端换一次性票 |
| 全部 unit 无 `MemoryMax`/`CPUQuota`，机器 4GB **无 swap 且已真实 OOM 一次**（`PROJECT.md` 48.2） | **High** | 一次 OOM 拖垮整机，只能云控制台硬重启 | 每 unit 加 `MemoryMax` + `MemorySwapMax=0`；加 swap |
| 服务普遍 root 运行，部分 unit 无 `User=`；全部无 `ProtectSystem`/`NoNewPrivileges` | **High** | 任一 HTTP 服务的 RCE = 整机 root + 主 token + HA token + 备份口令 | 专用用户 + systemd 加固 |
| `morning-check.sh:200` 从 644 的 unit 里 grep 主 token 塞进 curl argv；`monitor.sh:63` 把 `ps aux` 写进世界可读日志 | **High** | 主 token 落进日志，等于把万能钥匙摊开 | `EnvironmentFile`(600) + `umask 077` + 不 dump argv |
| Windows 开发机 ACL 是"Users 可读 / Authenticated Users 可改"，而这棵树里是全部密钥 + 免密 SSH 私钥 | **High** | 本机任一账户或沙箱进程 = 全屋 + 全部额度 + 备份钥匙 + VPS root 通道 | 凭据进 DPAPI/WinCred；收窄 ACL；SSH 私钥加口令 |
| 备份前不检查磁盘、失败留半截文件、无告警；备份口令权限只在文档里声称 | **Medium** | 备份"看起来在跑"但可能是坏的或半截的 | 加空间检查 + 失败清理 + 告警 + 真恢复演练 |
| 崩溃循环无退避 + `monitor.sh` 每 5 分钟补刀（含 caddy） | **Medium** | 坏服务 5 秒一次无限重启，caddy 反复重启 = 全网 502 | `StartLimitBurst`/`StartLimitIntervalSec` + backoff + 告警 |
| 无 logrotate / 无 journald 上限配置 | **Medium** | 日志撑满盘（`log-digest.py` 已记录 48 小时 30 万行） | `SystemMaxUse` + 保留策略 |
| 10+ 个服务绑 0.0.0.0 且无鉴权（ha-mcp / toy-mcp / netease / touch / app-tracker / Ombre / bridge） | **Critical** | 家电、物理设备、她的网易云、照片，全部取决于"hex 没被看到" | 全部收回 127.0.0.1；每服务一把 token；安全组只留 22/80/443 |
| `/uploads` 静态目录挂在 `/api` 之外（`server.js:1822`） | **Critical** | 所有上传照片公开可下载，`immutable` 缓存 30 天 | 挪进鉴权路由或改签名 URL |
| 上传扩展名来自客户端 + 同源提供（`server.js:563,1822`） | **Critical** | 存储型 XSS → 读走 localStorage 里的主令牌 → 全系统接管 | 扩展名白名单 + `nosniff` + `Content-Disposition: attachment` |
| 单请求最坏 90 分钟 + 无并发上限 + 无请求超时（`config.py:153,156,433` + `server.py:2380-2399`） | **Critical** | 一个半死 provider → Core 整体不响应，她只看到"一直在转"，无告警 | 请求级 deadline（180s）+ `limit_concurrency=8`；`max_retries` 降到 2 |
| `hass_set_state` 不校验 `DEVICES`（`ha-mcp/main.py:168-176`） | **Critical** | 可达任意 HA 服务域 | `entity_id ∈ DEVICES` + 域白名单 |
| VAPID 私钥硬编码兜底（`server.js:536-537`，`PROJECT.md:359` 标记"待修"至今） | **High** | 可向她 iPhone 伪造 Web Push | 轮换密钥对；删兜底；缺失即拒绝启动 |
| `mcd_create_order` 无结构闸门（`tools/mcd.py:39,219`） | **High** | 注入/幻觉 → 真下单 | 照抄瑞幸：出卡 + confirm 端点；顺手把 `orders/store.py` 已有的 `merchant` 列用起来 |
| R8 哨兵只扫 luckin（`check-boundaries.sh:51`） | **High** | 闸门扩不到新商户，且靠 grep 维护 | 升级成 `ToolSpec.side_effect` 声明 + 统一拦截 |
| 跨线程共享可变状态零锁（`Sessions._cache` / `AttentionRegistry._items` / `core.current_session_id`） | **High** | 偶发错会话、字典迭代崩溃、主动消息挂错 session，全静默 | session id 进 `ToolContext`；两处容器加锁 |
| 状态写入与上下文缓存无失效链路（全仓零次 `invalidate()`） | **High** | 他反复说错她刚记下的事实，她越记越不信，无报错 | 写路径挂 `invalidate()` 回调 |
| 全部数据无保留策略（observations 契约"永不删除"、桶只归档不删、conversations/usage_log/topics 不清理） | **High** | 60G 磁盘（已用 38%）是第一个硬墙 | 每库定保留策略 + 归档到对象存储；`world_model` 契约改为"原始事实归档而非在线保留" |
| OB 无 WAL / 无 busy_timeout / 无重试（与 nox-core 三库标准不一致） | **High** | SQLITE_BUSY 静默丢向量；召回质量无声劣化 | 统一 PRAGMA + 有限重试 |
| 桶文件非原子写 + 6 个进程写者零文件锁 | **High** | 一次并发写/断电就让记忆静默消失，且无 unarchive 可救 | 全部改 tmp+`os.replace`；写路径收归 `BucketManager`；加 unarchive |
| 模型生成文本无来源标记固化成"事实"，可被提升为 pinned 冻结进静态前缀 | **High** | **半年后人格偏离的机制本身就是它** | 桶加 `source: user|model|external` + `inferred` 标记；pinned 纳入预算；`split_breath` 无头时不得回退成 core |
| pin 循环无 token 预算（`server.py:667-674`） | **High** | pinned 越多，动态记忆越被全 break 掉 | 纳入同一预算循环 |
| 向量预筛无分数下限却替换整个候选集（`bucket_manager.py:480-490`） | **High** | 换一次 embedding 模型，召回静默崩坏 | 预筛加 `sim≥0.5`，否则退回全量 fuzzy |
| `count_tokens_approx` 对非中文严重低估（`utils.py:227-229`） | **High** | 大段非中文内容原样直通，预算形同虚设 | 真 tokenizer 或非 CJK 用 `len/2` |
| "唯一出口"是假的：12 条 push 路径里 10 条不吃额度；2 条绕过 Orchestrator（P1 waker、P11 共影前端） | **High** | 主动消息量无法从代码回答；她一旦觉得吵只能关掉整条线 | P1 收进 `_deliver`（补 `"wake"` 分支）；P11 改走 Core 的 Care 出口 |
| 无用户可见的 mute / 免打扰 | **High** | 她唯一的控制手段是改 `.env` 重启，且会连带关掉睡眠关心 | 加一个持久化开关 + quiet hours 可配 |
| `_persist()` 吞异常 → 重启后重复开口、额度静默归零（`service.py:850-859`） | **High** | DB 抖动一次 = 重放；看不出是故障 | 落盘失败必须在 `/api/health` 上暴露；关键状态改同步落盘 |
| `check-boundaries.sh` 无自动化入口且本机跑不了 | **High** | 八条边界法则实际执行率 0 | pre-commit + CI；本机补 bash 或重写成 node/python |
| 部署不在版本控制（逐文件 scp，`PROJECT.md:1213` 自述） | **High** | 线上与仓库漂移已发生过（08-31 事故）；无法回滚 | 照 Ombre-Brain 的 `vps` remote 模式，全部服务改成 git 部署 + 原子发布 + 回滚 |
| 所有 systemd unit 无资源限制、以 root 运行 | **High** | 一次 OOM 拖垮整机（已发生：`PROJECT.md` 48.2） | `MemoryMax` + 专用用户 + `NoNewPrivileges` + `ProtectSystem` |
| `uncaughtException` 只打日志不退出（`server.js:3893-3898`） | **High** | 进程"活着但已死"，`Restart=always` 永不触发 | 记完 `process.exit(1)` + watchdog |
| `monitor.sh` 漏 nox-core + 告警写进无人读的文件 | **High** | OOM / 磁盘 95% / 服务 DOWN 全部无人知晓 | SERVICES 对齐 doctor.sh；告警走 journald/webhook |
| 定时器上的 LLM 重试循环（daily card 失败时最坏 ≈900 次/天） | **High** | 静默烧钱 | 失败写状态 + 指数退避 + 单日次数上限 |
| 无界结果集 / 无界全表读进内存 / 请求路径同步 IO（`server.js:3391,3583,1104`） | **High** | OOM / 事件循环卡死 | `Math.min(limit,200)`；SQL 聚合；异步写 + 长度上限 |
| `ob_client.py` 零测试（R4 的唯一咽喉） | **High** | 记忆解析/超时/重试坏了没有任何测试会红 | 补假 transport 的单测 |
| 测试在 CWD 创建真实库（`server.py:730` + `test_api.py:50`） | **High** | 测试不幂等、带跨运行状态；与 08-24 Registry 污染同类 | 显式注入 `data_dir`；删掉误建的 db |
| 模块循环依赖：`agent↔tools`、`context↔memory`、`context→attention`、`personality→agent` | **Medium** | 任何重构的影响半径 = 整个 codebase | 抽 `contracts/` 包；加 import 方向契约检查 |
| `RelationshipState` 无持久化、无写入路径、`avoid_topics` 永空 | **Medium** | "关系演化"实现度 0%；她说"别问我这个"记不住 | 把 M5 提前，或**明确从产品目标里删掉**（现状是文档承诺与实现不符） |
| 无跨进程 trace id；`agent/loop.py` 只有 1 行日志；决策行是 DEBUG 而根级 INFO | **Medium** | 无法回答"他为什么在 T 时刻说话" | 统一 trace id；turn 结构化日志；`NOX_LOG_LEVEL` |
| 迁移 / 并发 / 半写零测试；约 20 处内联 `dbTry(ALTER TABLE)` | **Medium** | 一次列变更就能静默坏数据 | migration 列表化 + 集成测试 |
| 假绿测试（`or True` × 2、无断言 × 6、手抄生产逻辑 × 5、2027-01-01 时间炸弹） | **Medium** | 覆盖率是假的，比没测试更危险 | 删/改成真断言；引入 conftest 冻结时钟 |
| 文档系统性过期（工具数 63 vs 实际 ~156、bridge "无测试" vs 90 例、sql.js vs better-sqlite3、七条 vs 八条法则） | **Medium** | 决策依据不可信；`PROJECT.md` 自己也承认"验配置从使用方断言"是踩坑学来的 | **把"文档里的数字"改成从代码生成**，或删掉数字只留指引 |
| `NOX_CHAT_CONCERN` 默认开（`evaluator.py:126`），且 `api/server.py:992-995` 的注释与代码相反 | **Medium** | 一个会打扰她的自主行为默认开启；排障时按注释判断方向全错 | 收进 config、默认 false 跑影子；修注释 |
| `summary_budget_tokens` 是死配置（全仓 0 引用，compactor 用硬编码） | **Low** | 运维调它没反应 | 删掉或真的用上 |
| `scratch/` 里有 4000+ 行生产代码副本（`vps-server.js` 等，且 tracked） | **Low** | 改错文件的概率 | 归档/删除 |

---

## 十、最终 CTO 报告

### A. 当前 CAELUM 架构评分

| 维度 | 分数 | 判据 |
|---|---|---|
| **架构设计** | **5.5 / 10** | 分层意图与边界法则的设计水平很高（P90）；但循环依赖真实存在，"关系/理解/共鸣"三个抽象是空壳或影子，R1 的"唯一出口"是假的。**设计意图 8 分，落地 4 分。** |
| **工程质量** | **6.5 / 10** | nox-core 的代码纪律、注释质量、错误处理（裸 `except:` 0 处、175 处带日志）**明显高于个人项目平均**；1542 个真测试；瑞幸下单闸门是专业水准。扣分在：部署不在版本控制、文档系统性过期、bridge 是 3905 行单文件且 26 处 catch 无留痕。 |
| **可靠性** | **4 / 10** | 异地备份 5 天静默失败、鉴权 fail-open、10+ 无鉴权公网服务、无请求超时/并发上限/资源限制、无告警通道。**这不是"未来会炸"，是已经在漏。** |
| **扩展性** | **3.5 / 10** | 零保留策略 + OB 全库遍历 + 单机 60G 已用 38% + 循环依赖。**10 倍规模下磁盘和内存先到顶。** |
| **AI Agent 设计** | **6 / 10** | "主动性收敛 + 决策记账 + 记忆按需检索 + 观察/判断分层"这四件事上有真正的判断力，超过多数同类项目。但能动性是假的、关系演化 0%、理解层默认关、模型输出会无标记固化成事实。 |

**总分 ≈ 5.1 / 10。**
用一句话概括：**这是一个"想得比做得好"的系统 —— 架构文档里的判断力（P90）与工程落地纪律（P40）之间存在持续扩大的缺口，而弥补这个缺口的手段（测试、CI、可观测性、版本化部署）恰好是缺失的那部分。**

### B. 最大的 5 个风险

1. **凭证与隐私已经泄露在版本控制里，且仓库有 GitHub remote。**
   在用的 OpenRouter key（**就在 `origin/master` 上，与在用 key 逐字节相同**）、HA 长期令牌（5 个 tracked 脚本）、家庭 WiFi 密码、Gemini/DeepSeek/智谱 key（tracked tar + tracked `.py`）、VAPID 私钥、旧 VPS root 口令（历史提交），**以及她的记忆数据本身**（`vps-scripts/buckets-data.tar.gz`）。
   叠加：OB 的四个无鉴权读端点 + **五个公网 MCP 的应用层零鉴权**（Ombre / app-tracker / toy-mcp / touch-mcp / ha-mcp）+ **唯一门禁是同样 tracked 的路径 hex（`touch-mcp/Caddyfile` 五个）+ `PROJECT.md:5122` 把 touch URL 完整贴出** + **一个 `update-caddy.py` 会在重跑时把这道门彻底删掉**。
   再叠加：`fsr402` 触摸接收服务鉴权被注释掉且公网直连（`/latest` 回她的身体接触日志）、`netease-music-mcp` 零鉴权独立域名持有她的账号 Cookie、`co-watching` 的 token 已被编进发布 bundle。
   → **这是唯一一条"现在、立刻、外部人就能做到"的风险。**

2. **备份链已经断了，而没有任何东西知道。**
   异地副本 5 天没更新且会继续失败；备份源路径可能与 systemd 配置矛盾（缺目录只 WARN 且 `exit 0`）；恢复从未演练。
   → **出任何一次数据事故，恢复点是 2026-09-05，或者更早。**

3. **跨线程共享可变状态无锁 + 状态写入无失效链路。**
   `Sessions._cache`、`AttentionRegistry._items`、`core.current_session_id` 三方无锁；全仓零次 `invalidate()`。
   → 表现是**静默的**：主动消息挂错会话、他反复说错她刚记下的事实、偶发字典迭代崩溃。**这是这个项目最擅长掩盖的一类 bug，也是最贵的。**

4. **"唯一出口"和"确认制"这两条最重要的架构承诺是纸面的。**
   12 条 push 路径有 10 条不吃额度、2 条完全绕过 Orchestrator（唤醒链直调 `push()`、共影前端自建通道）；`mcd_create_order` 的闸门只是一句话。
   → **半年后没有任何人能回答"他今天为什么说了这么多"，也没有任何代码能阻止一次注入真的下单/控家电。**

5. **单请求最坏 90 分钟、无并发上限、无请求超时、无资源限制、无告警通道。**
   → 一个半死的外部 provider 就能让整个大脑停摆，而她看到的是"他一直在转"，你看到的是空日志。**`PROJECT.md` 48.2 已经真实发生过一次同类事故（自己的日志工具把 VPS 干趴），而修复只加在了那一个脚本上。**

### C. 未来 3 个月应该停止做什么

1. **停止手工 scp 部署。** 线上不是 git 仓库这件事已经造成过一次"传了一个文件没传另一个"的事故，而你在 `PROJECT.md` 里记下了它却没有改变流程。
2. **停止用"随机路径"当鉴权。** 它已经被提交进 git，保护等于零；而且它给人一种"已经安全了"的错觉，比没有防护更危险。
3. **停止在没有预算和来源标记的情况下，把模型生成的文本写回长期记忆和静态前缀。** `dream → hold(pinned=True) → core_principles → 静态前缀` 这条链是在**让模型给自己下永久指令**。
4. **停止新增主动开口通道。** 在 12 条路径收敛到真正的单一出口之前，每加一条都会让"他为什么说话"更难回答一分。已经在计划里的新来源先冻结。
5. **停止往 `bridge/server.js` 里加功能。** 它已经 3905 行、承担 8 类职责、26 处 catch 无留痕，而且今天还在长（一天涨了约 300 行）。**下一件事应该是在它上面切出模块边界，不是加第 9 类职责。**
6. **停止维护"带数字的架构文档"。** 工具数 63 vs 实际 ~156、bridge "零测试" vs 90 例、sql.js vs better-sqlite3、七条 vs 八条法则 —— 每一处过期都会误导一次决策。**数字要么从代码生成，要么别写。**
7. **停止把"结构性欠账"记成文档里的 `⚠️ 待做`。** `麦当劳还是提示词确认制`、`VAPID 私钥硬编码`、`OB 配置明文密钥` 都已经被写在文档里很久了 —— **文档记录不能替代修复，它只给了"我已经知道了"的安慰。**

### D. 未来 3 个月应该优先做什么

**第 1 周（止血，不做别的）**
1. 吊销并重建全部已泄露凭证（HA 令牌 ×2、WiFi、OpenRouter、Gemini、DeepSeek、VAPID、NOX_TOKEN）。
2. `git filter-repo` 清历史；从仓库移除 `vps-scripts/*.tar.gz`（尤其 `buckets-data.tar.gz`）与 `touch-mcp/Caddyfile`。
3. 所有服务收回 `127.0.0.1`；给 OB 的四个读端点加鉴权；`bridge` 的 `AUTH_TOKEN` 缺失即退出。
   **实测绑回环的只有 `nox-core:8100` 与 `health-mcp:8101/8102`；其余 10 个（bridge/app-tracker/touch-mcp/co-watching/co-reading/eryu/netease/Ombre/ha-mcp/toy-mcp）全是 `0.0.0.0`。这是本仓库已有的正确做法，只是没统一执行。**
3b. 删掉 `vps-scripts/update-caddy.py`；归档其余 11 个指向退役旧 IP 的 paramiko 脚本；`scripts/morning-check.sh:200` 改成 `EnvironmentFile` 读 token，不再 `ps aux` / curl argv 暴露。
3c. **卫星层收口（与第 3 条同批做）**：五个路径 hex 全轮换 + 从 tracked 文件移除；`netease-mcp.noxtang.com` 站点块删除或加 basic auth；`touch-server.service:13` 启用 `TOUCH_TOKEN`（或防火墙只放行 ESP32）；`app-tracker`/`toy-mcp` 恢复 DNS-rebinding 防护并加 token；`co-reading` 设 `MCP_AUTH_TOKEN`（它是 fail-open）；`co-watching` 的 `VITE_WATCH_URL` 改成短时效票并轮换。
4. 修 `pull-vps-backup.cmd` 的引号 bug；**跑一次真实的 decrypt→restore→integrity_check 演练**；异地新鲜度纳入告警。
5. 上 VPS 核实 `OMBRE_BUCKETS_DIR`，让备份脚本从 unit 读路径、缺目录 `exit 1`。

**第 2–4 周（结构性，三条线并行）**
6. **超时与并发**：`/chat` 加请求级 deadline（180s）+ `limit_concurrency=8`；`max_retries` 降到 2；单轮工具调用数上限；**工具返回文本截断**。
7. **线程安全**：session id 进 `ToolContext`（删掉实例属性）；`Sessions._cache` 与 `AttentionRegistry._items` 加锁。
8. **缓存失效**：所有写路径挂 `invalidate()`。
9. **R8 结构化**：`ToolSpec.side_effect` 声明 + 统一拦截；把麦当劳迁到 confirm 端点（`orders/store.py` 已有 `merchant` 列，成本很低）。
10. **边界哨兵进 CI**：pre-commit + 一个最小的 GitHub Actions（跑 `check-boundaries` + `pytest` + `node --test`）。**这一条是性价比最高的单点动作。**

**第 5–8 周（可观测性 —— 这是"静默失败"的解药）**
11. **"他为什么说话"可追溯**：结构化决策日志（每条 push 决策带 source/reason/thread/额度余量）+ 跨进程 trace id（bridge → core → MCP → LLM）。
12. **把"last success"变成健康信号**：每个后台任务/定时器暴露最后成功时间戳，`/api/health` 汇总；"backup / decay / attention / archiver / grow" 全部纳入。
13. **告警通道**：让 `monitor.sh` 和 `doctor.sh` 的结果能推到她/你能看到的地方（promise: 至少推一条到已有的推送口）。SERVICES 补齐 nox-core。
14. **资源上限**：所有 unit 加 `MemoryMax` + 专用用户；`uncaughtException` 改为退出。

**第 9–12 周（数据生命周期与记忆卫生）**
15. **保留策略**：每个库定 TTL/归档规则；`world_model` 的"永不删除"契约改成"原始事实归档而非在线保留"。
16. **OB 加固**：统一 WAL + `busy_timeout`；桶写改 tmp+replace；加 unarchive；写路径单写者化；`pinned` 纳入预算。
17. **记忆来源标记**：桶加 `source: user|model|external` + `inferred`；修 `split_breath` 无头回退 bug；evidence 渲染标注"这是他的推断"。
18. **测试补三个硬缺口**：`ob_client.py`、DB 迁移、恢复演练；清掉所有 `or True` / 零断言 / 手抄生产逻辑的假绿。
19. **部署版本化**：照 `Ombre-Brain` 的 `vps` remote 模式，把 nox-core / bridge / 各 MCP 全部改成 git 部署 + 原子发布 + 回滚。

### E. 如果我接手这个项目，我会怎么重构

我会明确拒绝"重写"。这个系统里最值钱的东西是那些**用真实事故换来的判断**（惰性衰减、max 而非相加、决策分母、观察/判断分层、静态前缀冻结、瑞幸闸门），重写会把它们全丢掉。我会做四件事，按顺序：

**第一件：先给系统装上"不会静默失败"的骨骼（2 周，零功能变更）**
- 所有后台任务/定时器/持久化路径必须要么成功，要么在 `/api/health` 上可见。**"静默"从一种可接受的失败模式变成一种架构违规。**
- 所有跨进程调用带 trace id。所有 LLM 调用、工具调用、push 决策各写一条结构化日志。
- 所有 unit 加资源上限。所有告警有去处。
- **判据：随便挑一个"他今天怪怪的"的时刻，我能在 10 分钟内从日志回答"这一轮 prompt 里有什么、他调了什么、他为什么决定说/不说"。**
  现在做不到，这是所有其他工作的前置条件。

**第二件：把"不可逆能力"全部收进结构闸门（2 周）**
- `ToolSpec.side_effect` 声明；`spend` / `irreversible` 类工具统一走"出卡 + 确认端点"。**麦当劳、淘宝、galatea 公开发帖、`toy_set` 一体处理。**
- `hass_set_state` 校验 `DEVICES` + 域白名单。
- 反向：把 HA 凭证从云端拿走，云端不持有能 blanket-control 家的钥匙。
- 随机路径不再承担鉴权职责。

**第三件：切断耦合，让模块可以单独动（3–4 周，纯搬家，零行为变更）**
- 抽 `contracts/`：`ToolSpec` / `Message` / `Usage` / 共享常量。**这一步消掉 `agent↔tools`、`context→attention`、`tools→attention`、`context↔memory` 四个环。**
- 用 `import-linter`（或 30 行 AST 脚本）把方向写成契约，进 CI。
- 把 `bridge/server.js` 按职责切文件：auth / static / push / chat-proxy / business-crud / tts / proxy。**不加功能，只搬家。**
- **判据：`ToolSpec` 改动不再需要碰 `agent/llm.py`；`Attention` 能在没有 `agent` 的情况下被 import 和测试。**

**第四件：给数据一个生命周期，给记忆一个来源（4 周）**
- 每个库：保留策略 + 归档 + 迁移测试 + 可恢复性验证。备份目标改成对象存储，**每月自动演练一次恢复**。
- 记忆：来源标记（user/model/external/inferred）、写路径单写者 + 原子写、unarchive、pinned 进预算。
- **判据：任何一条记忆都能回答"这是谁说的/谁写的、什么时候、依据是什么"。** 这是"不偏离最初人格"唯一的技术基础 —— 现在它靠的是"模型不会乱写"，而这已经被证明不成立。

**我不会做的：**
- 不会拆微服务（当前负载一台机器足够，拆了只会增加网络边界）；只做信任分区 + 每服务 token。
- 不会为了"能动性"去接 Drive 到开口决策 —— **在闸门和可观测性修好之前，让系统更自主是在放大风险。**
- 不会重写 nox-core。它的错误处理、结局分类、决策记账是全项目最好的部分。

**最后一句作为 CTO 的忠告：**
这个项目现在最大的问题不是任何单个 bug，而是**"文档里写的架构"和"实际跑的架构"之间的距离在持续扩大，而没有任何机制在阻止它扩大**。文档越详细（你们写得很好），这个距离越危险 —— 因为它会让每一次决策都建立在错误的现状认知上。

**修好 CI、修好备份、修好告警、把边界法则从 grep 变成代码。** 这四件事都不产生新功能，但它们是让后面所有事情可以被信任的前提。

---

## 十一、客户端与卫星服务审计（第三轮补充）

> 这一轮把"卫星层"补齐了。**结论：客户端层的卫生比预期好（定时器/监听器清理基本干净），灾难在卫星层。**

### 11.1 公网 MCP 的唯一门禁写在 git 里 —— 而且不止记忆那一个

| 服务 | 应用层鉴权 | 公网入口 | 判定 |
|---|---|---|---|
| `Ombre-Brain` (8002) | 四个读端点无鉴权 | Caddy `/ombre/<hex>/` | 🔴 |
| `app-tracker` (8000) | **无**，且 `main.py:11` `enable_dns_rebinding_protection = False` | `/tracker/<hex>/` | 🔴 |
| `toy-mcp` (8003) | **无**，且 `main.py:16,67-69` 关闭 DNS-rebinding 防护 | `/toy-mcp/<hex>/` | 🔴 |
| `touch-mcp` (9336) | **无**，CORS `*` | `/touch/<hex>/` | 🔴 |
| `ha-mcp` (8004) | **入站无**（出站 HA_TOKEN 强制） | `/ha-mcp/<hex>/` | 🔴 |
| `fsr402 touch-server` (9333) | **鉴权被注释掉**（`touch-server.service:13` `# Environment=TOUCH_TOKEN=…`）→ `touch_server.py:101` 自打印 `auth -> OFF`；`:35` 的空 token 分支直接跳过校验 | **公网 IP 直连**（`test_remote.py:5` 打 `http://43.133.211.140:9333/touch`） | 🔴 |
| `netease-music-mcp` (3456) | **完全没有**；CORS `*`；持有她的网易云 Cookie | **独立域名** `netease-mcp.noxtang.com` | 🔴 |
| `co-reading` (3100) | **fail-open**：`src/server-sse.js:51` `if (!authToken) return true` | `reading.noxtang.com` —— **Caddyfile 里唯一没有 path token 的卫星** | 🔴 |
| `co-watching` (3200) | **服务端零鉴权**，token 就是全部门禁 | `/watch/<24hex>/` | 🔴 |
| `health-mcp` | ✅ `X-Auth-Token`，**缺 token 直接拒绝启动** | 仅 `/health/*` | ✅ **唯一做对的** |
| `caelum-room` | ✅ `X-Room-Token` 全端点强制，<16 字符拒绝启动 | 不暴露 | ✅ |
| `stackchan-mcp` | ✅ HTTP 非 loopback 强制 bearer | 未部署到 VPS | ✅ |

**五个 hex 明文写在被 git 跟踪的 `touch-mcp/Caddyfile:2,5,8,11,14`**；而 `PROJECT.md:5122`（同样 tracked）把 `https://noxtang.com/touch/REDACTED-TOUCH-TOKEN/mcp` **完整贴了出来**，`:5124` 还自述"路径里 24 字节 hex 是门禁"。
→ **能读仓库 = 能连全部 MCP：读她的触摸记录、App 使用、家庭设备状态，驱动她的设备。**

而且这条门禁是**易碎**的：`co-watching` 的 token 已被编进发布产物 —— `nox-app/caelum-os-ui/.env.local:12` 的 `VITE_WATCH_URL=https://noxtang.com/watch/<REDACTED-WATCH-TOKEN>` 被 Vite 内联，**我实测在 `dist/assets/index-*.js` 里 grep 到了这个字符串**。文件注释写着"别贴进任何 .md 或源码"——**但构建会把它贴进 JS**。任何拿到 Electron 安装包的人即获得 co-watching 全部权限（而它服务端零鉴权）。

> ⚠️ **2026-09-12 补正：轮换它没用，别再往那个方向使劲。**
>
> 我一开始把"轮换这个 hex"当成修法，写进了上面的建议里。想清楚之后是错的：
> **这个 token 按设计就必须在客户端里** —— Caelum OS 的 Movies 房要调
> `https://noxtang.com/watch/<token>/api/...`，不带它就调不通。所以它出现在
> `.env.local`、被 Vite 内联进 `dist/assets/index-*.js`，**不是泄露，是它的工作方式**。
> 轮换只能管用到下一个安装包为止 —— 新 token 照样会被打进去。
>
> **真正的修法是短时效票**（和 `musicSig` 同一套）：客户端不再持有一把永久钥匙，
> 而是向 bridge 换一张**带签名、会过期**的票。这样"安装包里有个固定字符串"
> 就从"永久全权"降级成"过期作废的一小段"。
>
> 顺带：这一节里我自己也犯过一次 —— 写这份审计的时候我把完整的 token
> 贴进了本文档（`CAELUM-架构审计-2026-09-10.md:894`），而这份文档在仓库里、
> 推在 GitHub 上。2026-09-12 已打码。**"别贴进任何 .md"这句警告，我自己没做到。**

### 11.2 语音栈：成本失控 + 可永久卡死 + 静默失败

- 🔴 **[Critical] 重连风暴。** `shared/voice/engine.js:320-325` 的 Scribe `onclose` 里 `setTimeout(connectScribe, 600)`，而 `connectScribe` **每次都重新 `POST /api/scribe-token`**（`:266-277`）。全文件 grep `attempt|retry|backoff|maxRetries` **零命中**；bridge 的 `/api/scribe-token`（`server.js:1589-1608`）**无限流、无计数、无预算**。上游一旦"接受会话后立刻关闭"（配额/风控/抖动），一通电话就是 **≈1.6 次/秒 无限签发 ElevenLabs 实时会话**，按音频秒计费。
- 🟠 **[High] 网络挂起 = 通话永久卡死。** `engine.js:783-794` 的 `/chat` fetch 只带打断用的 signal，**无 timeout**；`:805` 的 `reader.read()` 无超时；`fetchTts:463-475` 同样。TCP 半开时 `phase` 永远停在 `"processing"`，她只能挂断重打。
- 🟡 **[Medium] 失败完全静默。** `engine.js:909-912` catch 只 `console.warn`，`:918` 600ms 后回去听。**她说完一句、网络断了、界面上什么都没发生。**
- ✅ **做对的**：浏览器只拿 ElevenLabs 一次性票（`bridge/server.js:1593`），API key 不下发；`desktop/preload.js` 不暴露 `CAELUM_UI_TOKEN`；音频 Scribe 直连省掉跨国来回是有理由的优化。
- ✅ **但 token 走 URL query**（`frontend/src/auth.js:23-28`、`caelum-os-ui/src/lib/voice.js:137`）→ 进 Caddy/journald 访问日志和浏览器历史。
- 🟠 **音频确实出境**：Scribe 路 base64 直发 `api.elevenlabs.io`；批量路更糟 —— `bridge/server.js:1279-1292` 把 WAV 写进 `uploadDir` 再生成**公网 URL** `${publicBaseUrl}/uploads/<name>` 交给 DashScope，而 `/uploads` 是公开前缀（**见 §六 `/uploads` 无鉴权**）。随机文件名是唯一遮蔽。

### 11.3 手机端聊天：乐观消息会被 60s 轮询无声抹掉

- 🟠 **`Chat.jsx:1420-1422` 先画一条本地 id 的 `status:"sending"`，`:1433` 在【响应头到达时】就改成 `"sent"`** —— 不是服务端确认落库时。流中途报错走 `:1556-1557` 只加一句"抱歉，卡住了"，**那条用户消息永远显示已发送，而服务端根本没有它**。
- 🟠 然后 `:1367` 每 60s `fetchMessages`，`:1322` **整数组替换** `setMessages(m.slice(-200))`，唯一护栏是 `:1317` 的 `if (loadingRef.current) return`。**上一轮发失败的那条会在 60 秒内被无声抹掉。** 同期 `:1323` 还把服务端数组写回 localStorage，覆盖 `:1379` 写的本地版本 —— **同一个 key 两个写者，无版本号、无 id 对账。**
- 🟠 **无取消**：`nox-app/frontend/src` 全仓**零 `AbortController`**；`Chat.jsx:1427-1544` 的 SSE reader 没有 unmount 守卫。切页即卸载组件，但 **reader 继续拉、模型继续生成、tokens 继续烧**。
- ✅ 定时器清理**没查到泄漏**（逐点确认都有 cleanup）。

### 11.4 双前端：重叠功能里约 95% 是两份各写各的

共享代码只有 `nox-app/shared/` 1,705 行，且只被 3 处 import。逐行文本比对（非空、>3 字符）：

| 文件对 | 手机 | 桌面 | 相同行 |
|---|---|---|---|
| `pages/Diary.jsx` | 815 | 664 | 352 |
| `pages/Chat.jsx` | 2166 | 322 | 157 |
| `pages/Health.jsx` | 1401 | 623 | 159 |
| `components/home/House3D.jsx` | 184 | 184 | **138** |
| `lib/schedule.js` | 73 | 73 | **53** |
| `memes.js` | 54 | 55 | **51** |
| **合计** | | | **≈1174** |

`frontend/src/components/Falling.jsx` 与 `caelum-os-ui/src/components/home/Falling.jsx` **MD5 完全相同**。
规模 10,231 vs 21,542 行 → **重叠功能里约 95% 是重复实现，不是共享库。**

- 🟠 **两套主题系统**：手机是模块级命令式 `applyTheme()`（写 `data-theme` + `theme-color` meta + `colorScheme`，`themes.js:21-37`）；桌面是 React Context（`ThemeProvider.jsx:7-34`）。**颜色共享，行为不共享** → 桌面切主题不更新 iOS 状态栏、不改 `colorScheme`。
- 🟠 **两套 SSE 解析，分帧规则都不一样**：手机按单 `\n` 切并要求 `line.startsWith("data: ")`（`Chat.jsx:1470-1471`）；桌面按 `\n\n` 切帧（`lib/chat.js:287`）。**bridge 改一次分帧格式，两边会以不同方式静默坏掉。**
- 🟠 **测试完全不对等**：桌面 15 个测试文件 + vitest；**手机 `package.json` 连 `test` script 都没有、零测试**。而两端共用的 `shared/voice/engine.js`（1,178 行、能开麦克风、能烧钱的状态机）**只有桌面侧一个测试文件在测**。
- 🟡 手机端 70 处裸 `fetch(`，靠 `main.jsx:10-27` **全局改写 `window.fetch`** 塞 token —— `VoiceCallDesktop.jsx:345` 的 `fetchApi` 也依赖这个补丁，**谁动 `main.jsx` 谁就把通话鉴权打断**。

### 11.5 仓库级循环依赖（新增一条）

`bridge:762,865` → `NOX_CORE_URL`；`bridge:2100` → co-reading(:3100)、`:1893` → eryu、`:1953` → netease、`:3869` → watch；
反向 `co-reading/src/nox-client.js:28,107` → **直接 `POST ${CORE_URL}/chat`（绕过 bridge 的鉴权/日志/预算层）**；
而 `nox-core/config.py:297` + `nox.py:137-139` 又建 bridge 客户端。
→ **`bridge → core → co-reading → core`，且 `core ↔ bridge` 互相调用。** 任一侧改合约两边同时坏。

🟠 **边界法则完全不覆盖卫星层**：`check-boundaries.sh` 的 R1/R1b/R2/R3/R4/R5/R8 全在 grep `nox-core`、`bridge`、`nox-app`；`touch-mcp`/`co-watching`/`app-tracker`/`toy-mcp`/`ha-mcp`/`netease-music-mcp`/`eryu` **一次都没出现**。
🟠 **而且 R5 会永远绿**：我实测 `git ls-files nox-app` = **0**，root `.gitignore` 忽略整个 `nox-app/`（它是独立仓库 `Iristt-boop/nox-app`）。**一条边界检查依赖了本仓库不跟踪的路径** —— 在干净 clone 上跑，它什么也检查不到。
🟠 **跨服务共享数据文件**：`touch-mcp.service` 里 `TOUCH_DATA_FILE=/root/touch-server/data/touch_moments.jsonl`，touch-mcp 直接 open 读；无 schema 版本、无锁、解析失败 `except: continue` 全吞（`touch-mcp/server.py:48-49`）。fsr402 改字段格式 → touch-mcp 静默返回空。

### 11.6 死重（体量实测）

| 路径 | 体量 | 结论 |
|---|---|---|
| `stackchan-mcp/firmware/`（整份 ESP-IDF + lvgl） | **1,112 MB / 14,018 文件** | delete |
| `stackchan-mcp/gateway/.venv` 等 | 344 MB / 8,122 文件 | archive |
| `.claude/worktrees/kind-bartik-1f9833/`（整份仓库副本） | 183 MB / 676 文件 | delete |
| `ob-tools/audio/`（107 个 mp3） | 177 MB | delete |
| `backups/` | 151 MB / 13,724 文件 | archive（移出工作树） |
| `pixel-beads-generator/uploads/` | 50 MB 测试产物 | delete |
| `nox-app/archive/backend/`（Render 时代 + node_modules + `.env`） | 27.6 MB / 2,793 文件 | delete |
| `dsh-vscode-layout/tools/github-mcp-server.exe` | 24 MB 单体二进制 | delete from repo |
| `caelum-room/…/manual/`（与已优化素材重复的原图） | 17 MB | delete |
| `fsr402-test/`（15 个手写调试脚本） | — | delete（已被 `fsr402-touch-server` + `fsr402-wifi` 取代） |
| `.trash-2026-08-08/`、`tsdown-probe/`、`pnpm-shim/` | — | delete |
| `nox-app/**/probe-*.jsx`（13 个，不进 bundle）、两个 `.bak-single-chat`（2,234 行）、`bridge/server.js.bak-*` | — | 移出/删除 |
| `secrets/proxy-links.txt` + `nodes.txt` | 25 KB，**含可用 trojan:// 节点 UUID** | **keep 但立刻轮换**（已被 `.gitignore` 挡住，但躺在工作树里，一次 `git add -A -f` 就带走） |

### 11.7 修正一处我先前的表述

我在 §1.3 / §三 引用过 `nox-app/caelum-os-ui/src/pages/Movies.jsx` 等路径 —— **它们在独立仓库 `Iristt-boop/nox-app`，不在 `Iristt-boop/caelum` 里**。这不影响结论（共影的绕过行为是真实的），但"仓库有 GitHub remote"这句话现在要乘以二：**两个仓库都有 remote，可见性都未确认**。

---

## 附录：未证实 / 无法判定（不与已证实结论混排）

1. **VPS 安全组实际放行哪些端口** —— 仓库内 grep `ufw|iptables|nft|firewalld|fail2ban` 零命中，无防火墙配置可查。第一节/第六节所有"0.0.0.0 绑定"是否真的公网直达，**我无法证实**。若安全组封了，严重度下降；但这不能替代服务层加固。
2. **线上 `/etc/caddy/Caddyfile` 是否与仓库一致** —— 仓库有两个版本（tracked 的 `touch-mcp/Caddyfile` 与 untracked 的 `scratch/Caddyfile`，后者多出 `/watch/`、`/agent/`、`panel.noxtang.com`），**两者都无 basic_auth**。若线上有 Basic Auth，暴露度下降。
3. **线上 bridge 到底有没有 `NOX_TOKEN`** —— 交付 unit 没有，`server.js` 也不读 `.env`。若线上也没配，`server.js:118` 意味着**此刻 `/api/*` 全部无鉴权**。
4. **`/root/ombre-brain/buckets` vs `OMBRE_BUCKETS_DIR=/root/ombre-brain-data`** —— 两者矛盾，直接决定每日备份是否在备份真数据。
5. **GitHub 仓库 public/private** —— `https://api.github.com/repos/Iristt-boop/caelum` 返回 404（本人网络不通，未能独立验证）。**按已公开处理。** `nox-app` 是另一个仓库（`Iristt-boop/nox-app`），可见性同样未确认。
5b. **`.env.local` 是否含 `GITHUB_TOKEN`** —— 两份子审计结论冲突（一处读到 `ghp_…` len=40，一处读成仅 1 行注释 / 177 字节），**未决**，我未能定论。建议直接打开确认。
5c. **沙箱/Codex 账户的存在** —— 实测 ACL 里有 `DESKTOP-PJA0G2M\CodexSandboxUsers Allow Modify`。它的实际权限边界与是否能读取同树的密钥，我无法判定。
6. **仓库里那两把 HA JWT 与 nox-core/.env 那把是否同值、是否都在役** —— 只验证了长度与 SHA-256 不同（165 vs 183 字符）。**至少两把存在。**
7. **HA 是否配了 `lock` / `alarm` / `script` / `shell_command`** —— 仓库内无 `configuration.yaml`，无法判定 `hass_set_state` 的最坏可达范围。
8. **线上实际生效的开关**（`NOX_ATTENTION` / `_LIVE` / `NOX_WAKE_LIVE` / `NOX_LLM_APPRAISAL` / `NOX_TIME_WAKES`）—— 本地 `.env` 未设置，VPS 上的 `.env` 不在工作区。所以"P3/P10/P11 现在到底在不在跑"只能从代码判定。
9. **线上真实桶数/pinned 数** —— 本地 `Ombre-Brain/buckets` 为空，仅有一份旧样本 `vps-scripts/breath-output.txt`（8 条核心准则 + 32 条动态 = 17298 字符）。
10. **`world_model` / `topic_pool` 的锁** —— 只确认了 `check_same_thread=False` + `sqlite3.connect`（`world_model/store.py:67`、`topic_pool/store.py:161`）；`Store`/`AttentionStore`/`OrderStore` 的锁已确认。
11. **`check-boundaries.sh` 的 CRLF 是否会让它跑不起来** —— `git ls-files --eol` = `i/lf w/crlf`，仓库 blob 是 LF，Linux checkout 大概率正常；"CRLF 让 `then\r` 解析失败"是按语义推断，**未执行验证**（本机无 bash）。
12. **两处数据竞争（`Sessions._cache` / `AttentionRegistry._items`）未复现** —— 只验证了"无锁"这一事实与读写双方确实来自不同线程。
13. **未跑测试套件**（仅 `--collect-only`：1542 collected / 0 errors）、**未跑 bridge 的 `node --test`**、**未跑 Ombre-Brain 的 pytest**。本报告中所有"通过/失败"都是静态判定。
14. **未覆盖**：`vps-scripts/{doctor,log-digest,deploy-monitor,enable-auth,set-api-key,notion-search}.py` 与 `scripts/{doctor,morning-check}.sh` 的逐行细节；`secrets/nodes.txt`、`secrets/proxy-links.txt` 的内容（按"不扩大明文暴露面"原则未打开）。
15. （已在 §十一 单列）客户端层与卫星服务的完整审计见新增章节。
