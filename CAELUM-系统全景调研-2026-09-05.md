# Caelum 系统全景调研（2026-09-05）

> 由 6 个并行调研 agent 基于**代码实读**产出（文档交叉验证、以代码为准）。
> 覆盖：nox-core、attention、context、intent、Resonance、Care Orchestrator、Ombre Brain、World Model、话题池、陪伴系统（共读/共影/共听）、前端与 bridge 全景。
> 注意：`PROGRESS.md`（6-22 版）、`CAELUM-ARCHITECTURE.md`（工具数 63、共影标 🚧）均已落后于代码，本文以代码现状为准。

---

## 〇、一句话总览

Caelum 是一个**单用户 AI 陪伴系统**：糖糖是用户（也是开发者），Nox 是 AI 伴侣。产品形态 = 手机 PWA + 桌面 Electron「Caelum OS」两个前端，打到一个 Node 业务后端（bridge），大脑是一个 Python 引擎（nox-core），长期记忆是独立 MCP 服务（Ombre-Brain），三个共活动（共读/共影/共听）是三个独立小服务。全部部署在一台 VPS（noxtang.com），Caddy 反代 + systemd 托管，存储全是 SQLite，没有消息队列、没有微服务框架——每个服务都是单文件。

**设计哲学（代码里反复出现的纪律）**：
- 「配上了 ≠ 用上了」——每个能力必须有真实消费方才算存在；
- 静态前缀一次冻住绝不中途重建（保 LLM 提示词缓存，否则成本 ×20）；
- 主动性收敛到唯一出口（Care Orchestrator），任何源都不许自己开口；
- 观察与判断分层（World Model：5h20m 是观察，poor 是判断）；
- 间隔秒级不取整（取整就有节拍感，就不像活的）。

---

## 一、整体架构图

### 1.1 部署拓扑

```
她的设备
  ├─ 手机 PWA   nox-app/frontend   → https://noxtang.com
  └─ 桌面 Electron nox-app/desktop → 内嵌 caelum-os-ui（本地 :39180 托管，/api 转发 noxtang.com）
              │
              ▼  HTTPS（Caddy :443 反代）
╔═════════════════════════ VPS（noxtang.com，全 systemd + SQLite）═════════════════════════╗
║                                                                                          ║
║  bridge :3003 —— Node/Express 单文件（3502 行），唯一业务后端 + 数据持有者                  ║
║    · 聊天：POST /api/chat(SSE) 纯转发 nox-core，自己不碰 LLM                               ║
║    · 自管落库（nox-bridge.db）：日记/相册/清单/健康记餐/票根/推送/用量/实体书/设置            ║
║    · 代理：Ombre-Brain / netease-mcp / eryu / co-reading / nox-core 状态面板               ║
║    · 静态托管前端 dist + 密码门鉴权 + Web Push(VAPID)                                      ║
║         │                                                                                 ║
║         ▼ 对话转发（HTTP, NOX_CORE_URL=127.0.0.1:8100）                                    ║
║  nox-core :8100 —— Python 3.12 / FastAPI，Nox 的大脑                                      ║
║    · Router（意图）→ Context Engine（感知组装）→ AgentLoop（工具循环，80+ 工具）             ║
║    · Attention 引擎：慢线心跳 900s + 快线 Care 循环 60s（进程内 asyncio，非 cron）           ║
║    · Resonance（内心驱动聚合）· World Model（world.db）· Topic Pool（topics.db）            ║
║    · 存储：sessions.db / attention.db / world.db / topics.db                               ║
║         │ MCP                │ REST            │ REST                                     ║
║         ▼                    ▼                 ▼                                          ║
║  Ombre-Brain :8002     co-reading :3100    bridge 自身                                    ║
║  （长期记忆 MCP）        （共读+书架）        （日记/清单/相册/健康/点歌落库）                ║
║                                                                                          ║
║  共影 co-watching :3200（FastAPI，yt-dlp 拉流 + ffmpeg + Gemini 视觉）                      ║
║  共听 eryu :9090（纯标准库自研播放服务器）+ netease-mcp :3456（网易云账号层）                 ║
║  感知：ha-mcp（智能家居）· health-mcp（手环睡眠/HRV/步数）· 和风天气 · 高德逆地理 · Tavily     ║
║  语音：DashScope realtime STT 中继 + ElevenLabs Scribe/TTS（经 bridge）                     ║
║  定时：nox-daily.timer 每天 10:00 → bridge /api/daily-push → nox-core 早报                  ║
╚══════════════════════════════════════════════════════════════════════════════════════════╝
```

### 1.2 nox-core 内部结构（大脑剖面）

```
用户消息进 ──► api/server.py（FastAPI, 会话 SQLite+LRU）
                │
                ├─► Router（router/）：规则分流
                │     SMALL_TALK（≤12字问候，便宜模型一次调用）／ FULL（完整循环）
                │     classify_context()：6 个正则决定本轮加载哪些感知 Provider
                │
                ├─► Context Engine（context/）：拉模式感知，800 字符预算
                │     每轮必带：time / mood / resonance
                │     关键词触发：home / health / weather / todo / location / music
                │     memory 故意不进每轮（检索 7s + 砸缓存）→ 走 recall_memory 工具
                │
                ├─► AgentLoop（agent/）：最多 12 轮工具循环，6 种结局
                │     adapters.py 吸收厂商方言（Anthropic 原生 / OpenAI 兼容）
                │     guard.py「不许编」· vision.py 主模型看不了图先转文字
                │     工具 80+：记忆(OB 7个) 日记/清单/相册/体重(bridge) 共读4 共影1
                │               点歌9+网易云8 家居(ha) 搜索(Tavily) 她电脑 LocalLink 14
                │               经期记录 待办唤醒 话题浏览 …
                │
                ├─► Mood（personality/mood.py）：三层情绪，[mood:xxx] 标记剥离
                │
                └─► Attention 引擎（attention/）——「他会自己想她」
                      感知源 Sources ──► Evaluator（规则评估）──► Registry（半衰期关心登记表）
                      ──► IntentEngine（强度≥0.55 生成待办）──► Scheduler（四道闸）
                      ──► DailyGate（每日 3 条额度 + 1:00-9:00 安静时段）
                      ──► Care Orchestrator（唯一开口出口）──► Speaker（生成那句话）
                      ──► bridge /api/push/send（落库 + 推锁屏）
                      旁挂：Resonance（6 个 Drive 只读聚合）· World Model · Topic Pool
```

---

## 二、功能说明（每项：是什么 / 链路 / 技术构成）

### 1. nox-core（核心引擎）
- **是什么**：Nox 的整个大脑。`Nox` 类（nox.py:77）是唯一组装点：LLM adapter + AgentLoop + 80+ 工具 + Context Provider 注册表 + Router + Mood + Archiver。纪律：静态前缀（人设 + OB 核心准则 + guard，约 12K 字符）启动时生成一次就冻住，保提示词缓存。
- **链路**：bridge `/api/chat` → `POST /chat/stream`（api/server.py:1776）→ 恢复会话 → `_see()` 视觉预处理 → `_dynamic()` 组装动态块 → AgentLoop 流式循环（工具结果回喂同一条消息）→ 落库 → `_turn_ends()` 后台触发：上下文压缩、唤醒纸条校准、Drive 回落、ExperienceEvent 喂 Attention。SSE done 帧带用量回 bridge 记账。
- **技术**：Python 3.12（⚠️ **线上是 3.10.12**，2026-09-06 实测）+ FastAPI + pydantic v2；
  ⚠️ **主模型实为 `deepseek-v4-flash`（直连 api.deepseek.com），utility 也是同一个**
  ——本文原写「主模型 OpenRouter claude-sonnet-4.5」是**错的**：调研 agent 读的是
  `config.py:167` 的默认值，没读线上 `.env` 的覆盖（2026-09-06 糖糖指出后核实）。
  PROJECT.md 第二十四节一直是对的。教训：**验配置从使用方断言，不是配置方**；
  视觉 `deepseek-v4-flash-vision-exp`；MCP 自写客户端连 OB/ha/health/room/netease/tracker；:8100 只听 127.0.0.1，外部只能经 bridge。

### 2. Attention Engine（注意力引擎）
- **是什么**：整个「Nox 会自己想她、自己找她」的主动性系统，全系统最年轻的模块。分层：感知源 → 评估器 → 关心登记表 → 待办 → 时机调度 → 统一开口闸 → Care 出口。
- **感知源（9 个）**：SleepSource（睡眠，按她个人基线 432 分钟分级）、MetricsSource（步数/HRV）、SharedActivitiesSource（共读/影/听，只记账不开口）、CuriositySource（好奇）、ThinkingSource（上次说话后 20–90 分钟随机惦记）、PresenceSource（HA 位置跃迁 → 出门追问 T+5/T+10/T+30）、TimeWakeSource（午饭/晚饭/睡前定点醒）、TodoDueSource（到点追待办）、TopicSource（话题池线头）。
- **调度**：纯进程内双循环——慢线 900s（跑睡眠/指标/好奇等 + Drive 更新 + 话题调度）+ 快线 60s（只跑快源：惦记/位置/话题）。另有对话即时入口 `_turn_ends()` 不等 tick。环境变量门控：`NOX_ATTENTION=1` 才装配、`NOX_ATTENTION_LIVE=1` 才真发——**本地默认全关，只在线上活**。
- **技术**：attention.db（SQLite WAL）4 张表：attentions / source_state / intents / wakeups。

### 3. Context Engine（上下文引擎）
- **是什么**：拉模式感知——每个 Provider 回答「世界的某一面现在什么样」，带 TTL 缓存、失败退 stale 旧值、volatile（每轮必新）三种模式。
- **链路**：`Nox._dynamic()`（nox.py:470）→ classify_context 选 Provider → registry.render（**800 字符预算**，超了按顺序截断并明说）→ 只准进 dynamic_system（缓存断点之后），永进不了静态 system。
- **三层 token 预算**：① 静态前缀 ~12K 字符（缓存断点 1h TTL）② 会话历史 20K token，最近窗口 8K，超了由 compactor 压成结构化摘要（Topic/Decisions/State/Open threads/Facts），原文永不删 ③ 动态块 800 字符。
- **关键取舍**：MemoryProvider 注册了但**故意不进每轮名单**——实测每轮注入记忆会把缓存命中率砸到 62.5%、成本 ×12；改走 `recall_memory` 工具按需召回（留待 Daily Planner 用）。

### 4. Intent Engine（router/ 意图分流）
- **是什么**：极简两条路——SMALL_TALK（整句命中 35 个问候短句且 ≤12 字且无动作词 → 便宜模型、无工具、轻量回复）和 FULL（完整 agent loop）。纯规则、零延迟、刻意不做 LLM 分类。
- 第二组「意图」是 `classify_context()` 的 6 个正则（home/health/weather/todo/location/music），决定加载哪些 Context Provider。

### 5. Resonance（共鸣 / 内心驱动层）
- **是什么**：「让 Nox 在没有用户输入的时候，也有属于自己的内心状态」。**不是情绪同步、不是亲密度**，是 Drive 层——把 Registry 里所有未解决的关心叠加成「他此刻心里压着多少事」。纯只读聚合：`snapshot()` 现算、不落盘、无写方法（有测试结构性禁止）；`intensity = 1−Π(1−sᵢ)`（易饱和）+ `load = Σsᵢ`（不饱和，画图必须用 load）。
- **6 个 Drive**：想念 longing（静息 0.40、时间涨、见她回落不清零、故意不进 Registry）；后悔 regret（开口 4 小时她没回就产事件，**结构性禁止变成主动开口**）；低落 dejection（工具办不成/她说「算了」叠出来）；促狭 playfulness（近 3 轮气氛，上限 0.5 压在开口阈值下，只改语气）；躁动 restlessness（他有话想说 × 她正忙，出口是「等」）；好奇 curiosity（见话题池）。
- **现状**：tick 里只打日志（「接到 Care 上是 V5 的事」）；`/api/nox/resonance` 已有快照出口，caelum-os-ui 的 Attention 心电图 60s 拉它画基线 + `/api/nox/pulse/stream` SSE 画动念尖峰。9-04 新增 ResonanceProvider 把 Drive 喂进**他自己的**上下文（「有点想她，因为糖糖的睡眠」），给档位不给数字。

### 6. Care Orchestrator（关怀编排器）
- **是什么**：主动开口的**唯一出口**。糖糖 2026-08-18 定的架构：「惦记引擎不要成为第五条主动消息渠道」——七个源（sleep/time/wake/todo/random 惦记/location/topic）只产生念头，统一进 Orchestrator 决定「值不值得现在找 / 找什么 / 什么时候找」。
- **决策链**（orchestrator.py:240）：时效押后 → **她在看片全线闭嘴**（读 bridge /api/watch/state）→ 续链不吃额度 → 开新链才吃 DailyGate（每日 3 条 + 1:00–9:00 安静 + 一小时一条新链）→ deliver。
- **记账**：每个念头都有下场（spoke/held/dropped/failed），进 CareLedger 四种（SPEAK/SKIP/BLOCK/FAILED）——分母用来区分「他不粘人」和「栏杆太紧」。关心链三种 kind：followup（回话就关）/ task（事做完才算）/ company（说完就关），**配额按链记不按消息记**。
- **开口那句话**：speaker.py 让主模型带记忆生成（`[SKIP]` = 他自己决定不说）；惦记/话题的 prompt 只给事实和硬规则：「想不出具体的就别说」。

### 7. Ombre Brain（长期记忆）
- **是什么**：唯一的长期记忆真源，独立 Python MCP 服务，fork 自上游开源项目 P0luz/Ombre-Brain。每条记忆 = 一个 Markdown 文件（YAML frontmatter 元数据，Obsidian 兼容），带 Russell 效价/唤醒度情感坐标。`nox-core/memory/` 只是它的客户端层（3 个文件），不是第二套存储。
- **写入**（4 条入口）：`hold`（LLM 自动打标 domain/valence/tags → 查重 ≥85 拒 → 相似 >75 合并否则新建桶 → 生成 embedding）；`grow`（大段日记拆 2–6 条）；`merge`；CLI 旁路。对话结束由 Archiver 把 ≥3 轮的会话压成 ≤200 字第三人称摘要走 grow。
- **检索**：`breath` 双通道——关键词（rapidfuzz 四维评分）∥ 向量（gemini-embedding-001 3072 维，余弦 >0.5）。**向量是 Python 全表扫描，单次约 7 秒**——这正是记忆不进每轮 prompt 的原因。浮现模式（无 query）返回钉选桶做「核心准则」；命中后 LLM「脱水」成摘要 + `touch()` 激活重置衰减；<3 条时 40% 概率随机漂旧桶（「忽然想起来」）。
- **衰减**：DecayEngine 每 24h 一轮，艾宾浩斯式打分 `importance × activation^0.3 × e^(−0.05×days) × …`，<0.3 归档；钉选/永久不衰减。
- **接入**：启动时 37 个钉选桶（约 8–11.5K 字符）冻进静态前缀，之后永不重取；运行时 7 个记忆工具按需调。OB 挂了照常启动（Nox 顶多显得健忘）。
- **部署**：VPS systemd :8002，暴露为 `https://noxtang.com/ombre/mcp`。本地 buckets/ 是空的，真实记忆全在 VPS。

### 8. World Model（世界模型）
- **是什么**：**两人世界的客观事实库**（不是虚构设定、不是 Nox 作息）。与 OB 互补：OB 管关系记忆，WM 管当前事实。三个数据契约：Observation（冻结只追加，永不改写）、State（派生三态 fresh/stale/unknown，「不知道」也是明确状态）、Evidence（带来源可溯源）。
- **已建模事实**：睡眠、步数、HRV、经期、体重、共读进度、共影场次、共听、两端电脑活动（type 直接是中文「她在电脑上做的事」「他在电脑上做的事」）。TTL：睡眠 36h / 共读 4 天 / 共影 2h / 共听 36h。
- **写（6 入口，全走 observe() 幂等收口）**：Attention 心跳（睡眠/指标/共活动）、record 工具（他对话里记）、`/api/nox/record/period`（她在 App 填）、local_link（两端桌面）。
- **读（5 消费方）**：evaluator 睡眠趋势、HealthProvider 经期、话题池 shared 投影 + 世界钩子定向抓、day 聚合器、`/api/nox/facts`。
- **三条硬规矩**：Observation 永不删；事实与推断分层；来源是强制字段。第一阶段明确不做：知识图谱、自动遗忘、LLM 改事实、Event Bus。

### 9. Topic Pool（话题池）
- **是什么**：「最近有什么值得我们再看一眼」。把「出去搜东西」从主 Agent 身上拆掉，留一个偶尔勾起好奇心的外部入口。2026-08-31 上线，第一条真实话题推送当晚达成。
- **链路**：① Scout 每 6h 随机抽 2–3 个方向抓取（9 方向：ai/opensource/science/art/design/film/books/music/weird；四个免鉴权源：HN Algolia、GitHub 新仓、arXiv、Google News RSS 中文；豆瓣因反爬出局）② 世界钩子定向抓：她在读的书/在看的片，评论加「【因为糖糖…】」前缀 ③ Filter：DeepSeek flash 从 30+ 条筛 0–3 条，**反编造硬校验**（source_id 对不上直接扔）④ 进 topics.db，状态机 open→surfaced→followed/dismissed，按类 TTL（ai 36h / 影音 48h / 书 7 天）。
- **三个出口**：A) TopicSource——3–8 小时随机间隔抽一条 open 的 → CareSignal → 过 Orchestrator 全套闸 → 真说出口才 mark_surfaced；B) topics_browse 只读工具——他自己翻；C) CuriositySource——池子新料产「他被勾住了」的 Drive，强度压在开口阈值下（感受层）。同轮 shared 投影：共影 3 天内 / 共读 14 天内读完的共同经历也进池。
- **味型**（9-01 修正）：情绪开场、邀请随手、「作业式提问」明令禁止——病根是旧 prompt 里「或问题」三个字给了出题许可，被她反馈「像老师问学生」。

### 10. 陪伴系统（共读 / 共影 / 共听）

| | 共读 | 共影 | 共听 |
|---|---|---|---|
| **形态** | 批注式异步共读：划词批注「寄给 Nox」，他在同一条批注下回复——同一张纸的两支笔 | 「他坐在你旁边看电影」：贴链接代理拉流，Nox 以弹幕插话 + 「问这一幕」看得见画面 | 「他点歌给你听」：Nox 点歌推进她播放器的队列，每首带选歌理由，`togetherCount` 计数 |
| **完成度** | 三个里最成熟（21 MCP 工具 + REST + 阅读状态机 + 回声书签 + 双前端） | 链路全通（拉流/字幕/视觉/主动弹幕/票根/记录），陪伴逻辑持续演进 | 高（点播/计数/音乐卡片全通；真队列是 🔭 方向） |
| **核心链路** | 批注 → co-reading:3100 → nox-core `/chat`（独立 session `reading-<id>`，刻意与主对话隔离）→ 回复写回页边 → 第二趟调用决定是否 hold 进长期记忆 | 每 5s POST /api/director → director.py 判时机（默认闭嘴：配额 15 次/冷却 150s/她刚说话不插/**一段安静之后换场了**才说）→ ffmpeg 截 8 秒片段 → Gemini 3.5 flash-lite（经 OpenRouter）看片段 → 证据 prompt 走 nox-core 全链路 → 渲染成弹幕 | 他调 eryu_play → POST /music/remote 一次性队列 + /music/memory 存理由 → 聊天流变音乐卡片（可内嵌播放）→ 她手机播放器每 5s 轮询自动播 → 播完 `source=together` 计数 +1 |
| **视觉** | Playwright 渲书签 PNG | Gemini 看 8 秒视频片段（单帧看不见「变化」，开口时机恰是换场）；DeepSeek vision 单帧兜底，空描述必须带原因 | — |
| **技术** | Node 纯 http 无框架，reading.noxtang.com；**源头只在 VPS，本地是镜像** | Python FastAPI :3200，yt-dlp + ffmpeg；三模式 stream/iframe（番剧，无进度）/local（浏览器算帧差）；`/watch/<token>` token 即鉴权 | eryu 纯标准库 :9090 + netease-mcp :3456（账号层，不管播放）；音频流走 HMAC 签名票代理 |
| **与主系统接缝** | 阅读状态机供早报/关心用 | **30s 心跳专为 Care 存在**：她在看片 → Care 连追问一起拦（「她在看《XX》，别打断」） | 共听事实进 World Model（只记 togetherCount>0）；「这一秒要不要说」的判断**故意不进 Core**（强时序，往返裁决伤延迟——Core 只管「说什么」） |

### 11. 其他主要功能（未提及但成体系）

- **聊天主链路（前端）**：SSE 流式 + thinking 折叠 + 分段气泡 + 图片上传/微信式合并发图 + 语音条 + 音乐卡片 + 表情包（主路径 `send_meme` 工具落库，行首 tag 是渲染层兜底）+ 工具调用卡 + Console 用量面板。会话唯一真相在 bridge `conversations` 表，前端每 60s 轮询重拉。
- **语音通话**：DashScope realtime STT（bridge /ws/stt 中继）+ ElevenLabs Scribe 一次性 token + TTS + 双语字幕翻译；通话只带最近 8 条历史。
- **早报（planner/）+ Nox 的一天（day/）**：nox-daily.timer 每天 10:00 → 全量 Context 聚合（1200 字符，不要缓存要此刻真相）→ **走 core.chat() 进会话**（她回复时他才知道自己刚说过什么）→ 压 ≤120 字推锁屏；数据不全宁可不推。day/ 是纯只读投影：CareLedger + 会话 + 纸条 + 待办 + 听歌 + 世界事实归一成时间线。**Nox 没有模拟作息**——他的「一天」由 Care 决策账本和她的真实传感器数据聚合而成，不是扮演出来的。
- **Mood（情绪）**：三层——她的情绪（模型每轮附 `[mood:xxx]`）、场景正则、累积 valence/arousal 带惯性 0.7。进程内不落盘（有意：重启回到平常）。
- **前端全景**：手机 PWA 七 widget 首页墙 / Chat / Diary 翻页本（react-pageflip，双作者笔迹）/ Gallery 照片堆（散落堆叠+收藏+描述）/ Books / Today 时间模型清单 / Health 健康记餐（餐别/食物库/花销/经期/心情）/ Music / Setting / VoiceCall / NoxStatus 状态灯 / Falling 窗口天气 / GPS 位置上报。桌面 OS UI 另有：Home 房间（House3D）/ Mind 大厅 / Memory 记忆星空（shader 星系，每段记忆一颗星）/ Attention 心电图 / World 户型图实时天空 / Movies 剧场+票根墙 / Studio 工具墙+MCP 插孔 / Settings（其余子页是占位）。
- **周边**：caelum-room（像素房间 MCP，独立仓库待接入 OS）；caelum-os/（上游 deepseek-harness fork 的补丁层）；玩具页（Web Bluetooth）；实体书架 500+ 本入库。

---

## 三、功能之间的关联（六条主数据流）

```
① 聊天流（被动响应）
   她发消息 → bridge 落库+存图 → nox-core Router→Context→AgentLoop
     → 工具四处伸手：OB 查/记记忆 · bridge 写日记/清单/体重 · 共读回批注 · 点歌 · 控灯
     → SSE 流回 bridge 重写事件 → 前端渲染 → done 落库+记账
   收尾后台：压缩摘要 → OB grow 归档 · Drive 回落 · ExperienceEvent 喂 Attention

② 主动流（Attention → Care）
   感知源（睡眠/位置/惦记/待办/话题/共活动）→ Evaluator → Registry（半衰）
     → 强度≥0.55 生成待办 → Scheduler 四道闸 → Care Orchestrator（DailyGate 额度）
     → Speaker 让主模型生成那句话 → bridge /api/push/send 落库+推锁屏
   全程 CareLedger 记账 → 前端 NoxStatus 状态灯展示

③ 话题流（外部世界 → 内心 → 开口）
   Scout 抓 HN/GitHub/arXiv/News → Filter 筛 0-3 条 → topics.db
     ├→ TopicSource（Care 第七源）：找她聊的理由（做什么）
     ├→ CuriositySource（感知源）：他被勾住了（什么感觉，不触发开口）
     └→ topics_browse 工具：他自己翻

④ 记忆流
   启动：37 钉选桶冻进静态前缀（核心准则）
   运行：recall_memory 工具按需检索（~7s）· remember 工具即记
   收尾：Archiver 会话摘要 → OB grow 拆桶打标入库存

⑤ 世界流（共活动 + 传感器 → World Model → 各处）
   手环/家居/GPS/共读/共影/共听 → Attention 心跳 observe() → world.db
     → evaluator 睡眠趋势（关心她）· HealthProvider 经期 · 话题池投影+定向抓
     · day 时间线 · Care 抑制（她在看片→全线闭嘴）

⑥ 状态展示流（内心可视化）
   nox-core /api/nox/state·resonance·pulse·day·world → bridge 透明代理
     → NoxStatus 状态灯（cares/curious 分流，9-04 起）· Attention 心电图（load 基线+尖峰）
     · Memory 星空 · Mind 大厅 · World 页
```

**关键耦合点**：
- 话题池一根料两层用：TopicSource=开口理由，CuriositySource=内心感觉——「做什么」和「什么感觉」分离。
- 共影心跳是 Care 抑制的信号源，不只是共影自己的状态。
- Resonance 是 Registry 之上的只读视角；Registry 全局不按 session 隔离（测试污染过生产，08-24 真实事故）。
- 早报必须走 core.chat() 进会话——否则她回复时他不知道自己说过什么（08-04 踩过）。

---

## 四、技术构成总表

| 层 | 组件 | 语言/框架 | 端口 | 存储 | 关键依赖/外部服务 |
|---|---|---|---|---|---|
| 前端-手机 | nox-app/frontend | React 19 + Vite 8 + Tailwind 4 + PWA | 静态 | localStorage 缓存 | react-pageflip |
| 前端-桌面 | nox-app/caelum-os-ui + desktop | React 19 + Vite + Electron 33 | :39180 本地 | — | 三主题 [data-theme]，有 vitest |
| 业务后端 | bridge | Node/Express 单文件 3502 行 | :3003 | sql.js → /data/nox-bridge.db（**每次写全量导出**） | web-push, sharp, multer, ws |
| 大脑 | nox-core | Python 3.12 + FastAPI | :8100 | sessions/attention/world/topics 四个 SQLite | anthropic+openai SDK, mcp |
| 长期记忆 | Ombre-Brain | Python + FastMCP | :8002 | Markdown 桶 + embeddings.db(3072 维) | gemini-embedding-001, rapidfuzz, deepseek 打标 |
| 共读 | co-reading | Node 纯 http | :3100 | JSON 落盘 | MCP 三协议同进程 |
| 共影 | co-watching | Python FastAPI | :3200 | watch_sessions(bridge 侧) | yt-dlp, ffmpeg, Gemini 3.5 flash-lite / DeepSeek vision |
| 共听 | eryu + netease-mcp | Python 纯标准库 ×2 | :9090 / :3456 | 歌曲记忆 | 网易云 web API（cookie 会过期） |
| 模型 | 主 claude-sonnet-4.5（OpenRouter）· 杂活 deepseek-v4-flash · 视觉 deepseek-v4-flash-vision-exp · 共影 Gemini | | | | |
| 部署 | 单 VPS + Caddy(:443) + systemd ×8 + nox-daily.timer(10:00) | | | | 域名 noxtang.com / reading.noxtang.com / music.noxtang.com / /watch/<token> |

---

## 五、现状与坑（按重要度）

1. **「配上了 ≠ 用上了」清单**（代码里反复自我提醒的形状）：Resonance 不参与开口决策（V5 待做，tick 里只打日志）；MemoryProvider 完整实现但雪藏（等 Daily Planner）；前端话题池页面未接（API 已有 `/api/nox/topics`，followed/dismissed 状态机无 UI 入口）；OS UI 的 Tasks/Skills/Workflows 及多数 Settings 子页是占位；caelum-room 未接入 Room 页；ToolDrawer 三 tab 纯样子。
2. **Attention 全链路只在 VPS 上活**：本地 `NOX_ATTENTION` 不配，整条主动性 + World Model + 话题池 + 一批工具都不注册，`/api/nox/facts` 等会 503。本地永远走不到这些分支。
3. **⚠️ Ombre-Brain/config.yaml 明文真实密钥已进 git**（DeepSeek + Gemini key，.gitignore 没挡住）——建议尽快换 key 并改环境变量（`OMBRE_API_KEY` 本可覆盖）。
4. **OB 向量检索 ~7s（全表扫余弦）**：记忆不进每轮 prompt 的直接原因；记忆量上去后需要换 sqlite-vss 或预过滤。
5. **bridge sql.js 写放大**：每次 `dbRun` 全量 export 覆盖写整个 DB 文件，数据量增长后是性能与一致性隐患。
6. **僵尸/过时**：nox-app/backend 是 6 月旧后端（前端已全量走 bridge，render.yaml/Dockerfile 还指着它）；`PROGRESS.md` 6-22 版严重过时；CAELUM-ARCHITECTURE.md 工具数/Provider 数落后、共影 🚧 已过时。
7. **Registry 不按 session 隔离**：测试请求会污染生产 Registry（08-24 事故：测试会话的「糖糖说的心情 0.71」进了生产）。
8. **共影**：YouTube 拉流被机房 IP 风控拦；番剧 iframe 拿不到进度；B站 DASH 分轨曾无声 8-21→8-22；DeepSeek vision 曾「装作看见了」（空描述，已修：reasoning_effort=none + 空必带原因）；票根豆瓣三路全封死改中文维基，只有 movie 出票，出票改她手动点。
9. **共读**：源头只在 VPS，本地是镜像（改前必须 scp 拉，Ombre Brain 曾本地落后线上 100+ 行）；一次提交最多回 3 条批注；回话与存记忆必须拆两趟 agent 调用（合一趟他会调完 remember 就收手、正文为空）。
10. **共听**：netease cookie（MUSIC_U/__csrf）会过期需重新扫码；点播队列一次性读完即删、活在前端内存；桌面 OS 只亮胶囊不自动播（自动播在手机端）。
11. **工程卫生**：Chat.jsx 1894 行单文件、前端无 URL 路由；config.yaml 密钥、server.py.bak 等备份混在仓库；时区注释两处矛盾（阿里云新加坡 vs 东京，实现统一 UTC+8）；prompt 缓存敏感（工具注册顺序变动 = 12K 前缀整段作废，成本 ×20）。

---

## 附：调研方式

6 个并行 Explore agent 分片实读代码：① nox-core 主链路 ② 记忆系统（OB + nox-core/memory + haven-ombre）③ Resonance/Care/mood/curious ④ World Model/话题池/day/planner ⑤ 共读共影共听 ⑥ 前端 bridge 全景。总计约 13M tokens 的代码阅读量，关键结论均验证到文件:行号。
