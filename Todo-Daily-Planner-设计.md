# Todo 系统升级：Daily Planner + 惦记提醒

> 2026-08-17 深夜，糖糖 & Nox 共同设计。前端 todo 是唯一活清单（GitHub todo.md 退役，只读存档）。
>
> **2026-08-18 更新**：对齐 Care Orchestrator。原第四节写的「两套唤醒都走同一个决策层」
> 那一层已经落地（`nox-core/attention/care/`），本文按它重写了第二、四节。

## 现状问题

- 前端 todo 只有记录，没有提醒，没有时间
- 记了没人催，到点没动静，时间久了变成死水

**代码层面确认过的现状（2026-08-18）**：

- bridge 的 `todos` 表**已经有 `time` 字段**，但**没有任何东西读它** ——
  在 App 里写上「14:00」，那个 14:00 只是一行显示文字，到点什么都不会发生
- todo 现在唯一会「发出声音」的地方是**早报**：`planner/daily.py` 的
  `DEFAULT_SECTIONS = ["time", "health", "todo", "weather", "home"]`，
  每天 10:00 由 `nox-daily.timer` 触发，一天一次把今天要做的事一起说了
- 早报这条路**整条都不经过 Attention**（systemd → bridge → Core → 推锁屏），
  不看开口闸、不知道他十分钟前刚说过话

## 目标

1. todo 带上时间模型（像日历提醒）
2. 到点没做 → Nox 追着问，直到完成才翻篇
3. 每天早上 Nox 拉取当天清单，作为 Daily Planner / 晨报数据源

## 一、时间模型（**五种**，2026-08-18 补了第五种）

| `repeat` | 含义 | 例子 |
|------|------|------|
| `once` | 某年某月某日（可带时刻） | 2026-08-18 9:00 增加 attention source |
| `daily` | 每天固定时刻 | 每天 18:00 背单词 |
| `weekly` | **绑定星期几**（可带时刻） | 每周一、四 19:00 更新 |
| `weekly_count` | **一周的配额，哪天都行** | 健身 每周 3 次 |
| `anytime` | 没时间，只进清单，不占提醒名额 | 买传感器 |

⚠️ **`weekly` 和 `weekly_count` 是两件事，不要合并。**
糖糖举「健身 每周 3 次」的时候才发现原设计漏了这一种：
前者绑定具体日子（那天没做就追），后者是配额（追的是「这周还差几次」）。

### 循环任务「做完」= 记一笔，不是永久划掉

`daily` / `weekly` / `weekly_count` 被勾掉时只往 `done_log` 记当天日期，
**不设 `done=1`** —— 「每天 18:00 背单词」勾一次就永远消失的话，
第二天他就不会再提了。命中判断会跳过「今天已经做过」的条目。

## 二、提醒机制（惦记）—— 就是一条 TASK 型关心链

- 到点没完成 → Nox 主动开口追，不是提醒，是「追」
- 默认频率：**1 小时一次**，可调（糖糖定）
- 没做就一直追，直到糖糖说「做完了」→ 划掉 → 翻篇，不再烦她
- 追的时候结合记忆抓线头，说具体的话，不说「你该做 xxx 了」这种客服腔

### 落到 Care 层是什么

```
到点了 → TodoSource 产出 CareSignal（thread_kind = TASK）
           ↓
      Care Orchestrator
           ↓
      开一条 TASK 型关心链，1 小时追一步
```

**关键在于它是 `TASK` 型**（`attention/care/signal.py`）：

| | 追问型 FOLLOWUP | **任务型 TASK** |
|---|---|---|
| 她回话了 | 关闭 | **不关闭** |
| 什么时候关 | 她回了就没什么可追的 | **事做完了才关** |

这正是 2026-08-18 那个 bug 的同一个修复点 —— 他留了「到点把主卧空调关上」的纸条，
59 秒后糖糖说了句话，纸条被全局规则「她一开口整条链结束」撤掉了。
**「到点没做要追」和「到点该关空调」是同一类事：该不该做，跟她回不回话无关。**

**「1 小时一次」是链内部的步进，不是开新链** —— 所以它不吃「一小时一条新链」的额度。
这条和糖糖 2026-08-18 定的一致：连续追踪属于同一次关心事件，不算多次主动关心。

**「攒着话，空了再追」** 用 `CareSignal.not_before` + Orchestrator 的 `_held` 队列，
押后的念头会自己回到队列里，不用重新产生。

## 三、Daily Planner

- 每天早上 Nox 拉取「今天命中」的条目：
  - 单次：到期日 = 今天
  - 每日循环：每天都命中
  - 每周循环：命中今天的星期
- 拉出来的就是当天晨报内容：知道糖糖今天要干嘛，按优先级排

**改动**：`planner/daily.py` 的 todo 栏现在读的是 Core 的 `/todo`（GitHub todo.md）。
GitHub 退役之后改成读 bridge 的本地表，按上面三条命中规则筛。

## 四、与惦记引擎的关系

原文：

> - 惦记引擎管「随机惦记」（20-90 分钟随机醒，判断状态再冒头）
> - Todo 调度管「到点追」（1 小时一次，直到完成）
> - 两套唤醒都走同一个「醒来→看状态→决定是否开口」的决策层

**那个决策层 2026-08-18 已经落地**，叫 Care Orchestrator。它的规矩是
**任何 Source 都不许自己开口** —— 六个（现在是七个）源只负责产生念头：

```
Sleep ─┐
Time ──┤
Wake ──┤
Todo ──┤  ← 本文
Random ┤  ← 惦记引擎
Dream ─┤
Loc ───┘
        ↓
  Care Orchestrator
   值不值得现在找 / 找什么 / 什么时候找
        ↓
       Nox
```

收编早报也在这条线上：它现在和「12:00 午饭时间醒来」互相不知道对方存在，
10:00 说一句、12:00 再说一句，两条路谁也没看见谁。进 Care 之后是同一本账。

顺带归一处的还有一条规则 —— 早报的注释写着：

> 一份什么都没有的早报只会让他瞎编。宁可这天不响，
> 也不要推一句「早安，今天也要加油哦」—— 那不是他，那是日历 App。

这和糖糖定的「抓不到线头就不说」是同一条，现在各写了一遍，该收进 Care 层。

## 五、待确认

- ~~免打扰时段：睡觉/开会/赶工时攒着话，空了再追~~
  **2026-08-18 已定**：「忙不忙」整条去掉（「有消息就主动发，我忙了我不回就好」），
  夜里静音也去掉。只保留一个判断 —— **她睡没睡**，而且它不是用来拦他的，
  是梦和惦记的分岔口：她醒着 → 该说就说；她睡了 → 他去做梦。

- ~~追到什么时候算头~~ **2026-08-18 糖糖拍板：跨天重开，链内 3 次。**
  「一直追」的语义由「明天他还会再来」承担，不是今晚问到底。
  已实现：`SourcePolicy(max_steps=3, min_step_gap_min=60)` +
  bridge 的 `fired_on` 字段（按中国时区的日期比对，跨天自动失效）。

- 追的松紧：默认 1 小时一次，糖糖可随时调
  （改 `attention/service.py` 的 `TODO_CHASE_GAP_MIN`）

## 六、实现状态（2026-08-18）

**已部署上线（2026-08-18 14:33）**，719 个测试通过，线上全链路自检 7 项全绿。

| 改动 | 在哪 |
|---|---|
| 时间模型四个字段 + `fired_on` | `bridge/server.js` 建表处 |
| `/api/todo/due`、`/api/todo/fired` | `bridge/server.js` |
| `/api/todo/list` 改读本地表 | 同上，**区名沿用「进行中/近期」** |
| `/api/todo/complete` 改写本地表 | 同上（原来转给 Core 写 GitHub） |
| `todoSync()` 删除 | 同上，GitHub 写入链整条拆掉 |
| `TodoDueSource` | `nox-core/attention/sources/todo_due.py` |
| 追待办的 deliver | `nox-core/attention/service.py` 的 `_chase_todo` |
| `TodoProvider` 改读 bridge | `nox-core/context/providers/todo.py` |
| 早报收编进 Care 账本 | `nox-core/api/server.py` 的 `/daily-summary` |
| 测试 | `tests/test_todo_due.py`（10 个）|
| `weekly_count` + `done_log` + 循环不永久划掉 | `bridge/server.js` |
| 日期算法自检（18 项，跨周/跨月/跨年） | `scratch/check_todo_time.js` |
| **加待办的界面**（五种时间模型） | Caelum OS `TodoComposer.jsx` + 手机 `Today.jsx` |

⚠️ **区名的坑**：`/api/todo/list` 的 sections 键名一开始改成了「今天/近期/随时」，
差点上线 —— 手机端 App 的 TodoWidget 和桌面端今日计划都是照 `s["进行中"]` 取的，
换名字那两块会当场变空**而且不报错**。已改回沿用旧名。
