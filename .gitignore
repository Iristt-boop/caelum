# ⚠️ 这份清单是 2026-08-26 给整个工作区接 git 时重写的。
#
# 接 git 的直接原因：Nox 现在能「一次点头连改 40 个文件」
#（多步执行，见 CAELUM-HARNESS-ARCHITECTURE.md 八之七）。
# 在那之前每改一个文件都要糖糖单独点头，最多一次错一个；
# 现在不是了，所以需要一条能撤回的路。
#
# 🔴 **加新目录之前先想一遍：里面会不会有密钥。**
# 首次提交前实测有 9 个文件带密钥形状的内容，
# 其中 5 个是旧清单拦不住的。

# ============ 密钥 ============
# 🔴 这一段只增不减。删任何一行之前先确认那个文件里没有凭证。
**/.env
**/.env.local
**/.env.*.local
LOCAL-SECRETS.md
secrets/

#: 实测扫出来带凭证的，逐个点名 ——
#: 用通配符会漏（`config.yaml` 这种名字太普通）
.mcp.json
.claude/settings.local.json
Ombre-Brain/config.yaml
vps-scripts/config-tmp.yaml
vps-scripts/set-api-key.py

#: 🔴 **源码里硬编码了真密钥的三个脚本。**
#: 首次提交前扫内容才发现的 —— 按文件名挡不住它们，
#: 它们看起来就是普通脚本。
#:
#:   scratch/tmp_full_debug.sh     NOX_TOKEN 写死在里面
#:   vps-scripts/enable-auth.py    同上
#:   vps-scripts/notion-search.py  Notion integration token
#:
#: ⚠️ 忽略只是不让它们进历史。**密钥仍然明文躺在盘上**，
#: 正确的修法是改成读环境变量 / ~/.caelum/env。
scratch/tmp_full_debug.sh
vps-scripts/enable-auth.py
vps-scripts/notion-search.py

#: 我自己排查时留下的临时文件常常带密钥（.caelum-secrets.tmp 那种）。
#: 一律不进库
*.tmp
*.key
*.pem

#: 备份目录里躺着历史版本的配置，同样可能带凭证
backups/

# ============ 独立仓库（8 个）============
# 这些目录各自有 .git。嵌套仓库在父仓库里只会变成一个尴尬的
# 「untracked embedded repo」：内容既不被父仓库跟踪、又碍事。
#
# ⚠️ 而且其中 `scratch/CcCompanion` 是个**一次提交都没有的空仓库** ——
# 不排掉的话整个 `git add -A` 会直接失败：
#   error: 'scratch/CcCompanion/' does not have a commit checked out
#   fatal: adding files failed
#
# 🔴 **代价说清楚：这些目录不在本仓库的撤销范围内。**
# 尤其 `nox-app/` —— caelum-os-ui 和 desktop 都在里面，
# 也就是说**前端的撤销要去 nox-app 那个 repo 做**，不在这儿。
nox-app/
caelum-room/
Ombre-Brain/
ai-fishing-game/
dsh-vscode-layout/
haven-ombre/
pixel-beads-generator/
stackchan-mcp/
scratch/CcCompanion/

# ============ 构建产物 / 依赖 ============
**/node_modules/
**/dist/
**/build/
**/__pycache__/
**/*.pyc
**/*.pyo
**/.venv/
**/venv/
.pytest_cache/

# ============ 运行时数据 ============
#: 数据库是她的真实数据，不该进版本控制（也太大）
**/*.db
**/*.db-shm
**/*.db-wal
**/*.log
logs/
.playwright-mcp/

#: 手工备份的历史版本（server.js.bak-20260724 那种）。
#: 接了 git 之后这套就不需要了，但先留在盘上，只是不进库
*.bak-*

# ============ 杂物 ============
.vscode/
.DS_Store
Thumbs.db
.trash-*/

# ============ 打包外带的密钥/数据（2026-09-11 补）============
# 🔴 这几个 tar 是"绕过 .gitignore 把密钥送进仓库"的通道：
#   ob.tar.gz / full-ob.tar.gz   内含 ombre-brain/config.yaml（明文 DeepSeek + Gemini key）
#   buckets-data.tar.gz          内含 buckets/ —— **她的记忆数据本身**
# 它们曾被 git 跟踪并推到 master。文件从 2026-09-11 起不再入库（留在盘上备用），
# 但**历史里还在** —— 所以对应的 key 必须轮换，见修复排期 0.4。
# 规则：vps-scripts 下只允许 .py / .sh / .md，任何压缩包一律挡掉。
vps-scripts/*.tar.gz
vps-scripts/*.tgz
vps-scripts/*.tar
vps-scripts/*.zip

#: Caddyfile 的真实快照里带着五个 MCP 的路径暗号（那五个服务应用层零鉴权）。
#: `touch-mcp/Caddyfile` 已改成模板；`scratch/Caddyfile` 是旧快照，未跟踪但躺在工作区，
#: 一次 `git add -A` 就会把暗号扫回仓库 —— 所以显式挡掉。
scratch/Caddyfile
scratch/*.Caddyfile

# ============ 本机凭据（2026-09-11 加）============
#: 真实的内存快照样本 —— 里面是她的真实记忆内容（bucket id / core_facts /
#: emotion_state / todos / summary），和 buckets-data.tar.gz 同一类。
#: 文件留在盘上做参考，但不进库。
vps-scripts/breath-output.txt

#: ESP32 / MicroPython 的 WiFi 凭据。源码里现在是 include/import 这两个文件，
#: 拿不到就退化成空串（连不上但不会把密码写进 git）。
wifi_secrets.h
wifi_secrets.py
**/wifi_secrets.h
**/wifi_secrets.py

#: scratch/ 里的 bridge 源码副本 —— 它们带着 `bridge/server.js` 里硬编码的
#: **VAPID 私钥**（那是已知欠账，见 PROJECT.md 第四节）。这些副本未跟踪，
#: 但一次 `git add -A` 就会把私钥扫进仓库。只挡这几个文件，不动 scratch 其余部分
#: （scratch/ 里有 88 个文件是有意跟踪的，整个目录 ignore 会改变她的习惯）。
scratch/local-bridge-head.js
scratch/remote-bridge-server.js
scratch/local-ob-before-myedit.py
scratch/remote-ob-server.py
scratch/vps-nox-patch-base.py
scratch/mock-ob.py
scratch/split/

#: 探针留下的 API 请求/响应转储（可能带 Authorization 头）。
scratch/z_*.json
scratch/zt_*.json
scratch/zc_*.json

# ============ 配置仓库（2026-09-11 起，乙）============
#: VPS 的 systemd unit / drop-in / Caddyfile 目标态 / crontab —— 配置那半个部署。
#: 它是**独立的 git 仓库**（和 Ombre-Brain / nox-app 一样是嵌套仓库），
#: 所以这里必须排掉：否则 git add -A 会报 "does not have a commit checked out"。
#: ?? 里面**不许有密钥**：密钥在 VPS 的 /etc/nox/*.env（600），不进版本控制。
deploy-config/

# ============ scratch 里的二进制与外部克隆（2026-09-11 补）============
#: ?? 这 6 个是**别人项目的克隆**（各自带 .git）。当普通文件加进去会变成
#: 坏掉的 gitlink/submodule 引用 —— 2026-09-11 我误提交过一次，已回滚。
scratch/books-3d-ref/
scratch/commonplace-book/
scratch/react-pageflip/
scratch/ref-bookfolio/
scratch/ref-bookopen/
scratch/ref-nootbook/
#: 图片 / 字体这类二进制：进 git 只会让仓库变胖，而且能从源重建。
scratch/*.png
scratch/*.jpg
scratch/*.jpeg
scratch/*.gif
scratch/*.webp
scratch/*.woff2
scratch/*.mp4
scratch/iris-preview/
scratch/limezu-frames/
scratch/limezu-peek/
scratch/diary-preview/

# ============ 一次性探针 / 工具产物 / 机器本地（2026-09-11 补）============
tsdown-probe/
pnpm-shim/
.zcode/
.last-dsh-backup.txt

# ============ 共听的歌：分析输入，不是成果（2026-09-11 晚）============
# ob-tools/audio/ 是共听第 ④ 层「Music Intelligence」的工作目录，**跑在糖糖电脑上**
# （VPS 没装 librosa —— 那台机器已经跑着 bridge/nox-core/ombre-brain/eryu/…，
# 再塞个吃满 CPU 的音频分析会拖累对话）。流程见 PROJECT.md 第 3570 行起：
#     fetch_playlist.py 下歌 → batch_analyze.py 本地并行分析 → 回传 + sync_analyzed.py
#
# 这个目录里有两类东西，必须分清：
#     *.mp3                下回来的**分析输入**，49 个 176.5MB，能重新下
#     *_preanalysis.json   算出来的**成果**，49 个 41KB，**Nox 真正读的就是它**
#
# 176.5MB 占了仓库 97% 的体积，而 git 存二进制既压不掉也不 diff ——
# 每次 clone、每次异地备份都要把它搬一遍。这是「中间产物不该进版本控制」的
# 标准案例。json 和 5 个脚本留着（小，而且是数据/工具，不是产物）。
#
# ⚠️ 已用 git filter-repo 把 mp3 从**全部历史**里清掉（2026-09-11 第二次重写）。
#    磁盘上的 49 个一个没少；动手前做过 176.5MB 的逐字节备份 + SHA256 清单。
ob-tools/audio/*.mp3